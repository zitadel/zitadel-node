// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import type { Authenticator } from "./authenticator.js";
import type { ApiClient } from "../api-client.js";

/**
 * Extension of {@link Authenticator} for authentication schemes that require
 * making HTTP requests (e.g. OAuth2 token exchange, OpenID Connect discovery).
 *
 * Implementations receive a shared {@link ApiClient} instance so that
 * authentication-related HTTP calls (token endpoints, discovery documents)
 * use the same transport configuration (proxy, TLS, timeouts) as regular
 * API calls.
 *
 * The {@link ApiClient} is injected by the `Zitadel` class after construction,
 * via {@link setApiClient}. Implementations must not make HTTP calls before
 * the client is injected.
 *
 * Only OAuth2 and OpenID Connect authenticators implement this interface.
 * Simple authenticators (Basic, Bearer, API Key) do not need HTTP access
 * and implement {@link Authenticator} directly.
 */
export interface HttpAwareAuthenticator extends Authenticator {
  /**
   * Inject the shared API client for making HTTP requests.
   *
   * Called by the `Zitadel` constructor after the {@link ApiClient} has been
   * created with the user's `TransportOptions`. Implementations should
   * store this reference and use it for all outbound HTTP calls (token
   * exchange, discovery, etc.).
   *
   * @param apiClient the shared API client instance
   */
  setApiClient(apiClient: ApiClient): void;
}

/**
 * Type guard to check whether an {@link Authenticator} is an
 * {@link HttpAwareAuthenticator}.
 *
 * @param auth the authenticator to check
 * @returns true if the authenticator implements HttpAwareAuthenticator
 */
export function isHttpAwareAuthenticator(
  auth: Authenticator,
): auth is HttpAwareAuthenticator {
  return typeof (auth as HttpAwareAuthenticator).setApiClient === "function";
}
