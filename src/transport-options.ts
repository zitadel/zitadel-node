// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

/**
 * Immutable HTTP transport configuration for `DefaultApiClient`.
 *
 * Controls low-level HTTP behavior such as TLS verification, proxy routing,
 * timeouts, redirect handling, and automatic header injection. These settings
 * are independent of API-level concerns (base URL, authentication headers)
 * which belong in `Configuration`.
 *
 * This class is immutable. Use {@link TransportOptions.builder} to create instances:
 *
 * ```typescript
 * const transport = TransportOptions.builder()
 *   .verifySsl(false)
 *   .proxy('http://proxy.example.com:8080')
 *   .timeout(5000)
 *   .userAgent('MyApp/1.0')
 *   .build();
 * ```
 */
export class TransportOptions {
  /** Whether TLS certificate verification is enabled. */
  public readonly verifySsl: boolean;

  /** Path to a custom CA certificate bundle for TLS verification. */
  public readonly caCertPath: string | null;

  /** HTTP or HTTPS proxy URL for all outbound requests. */
  public readonly proxy: string | null;

  /** End-to-end request timeout in milliseconds. Default 10000 (10 s). null means no timeout. */
  public readonly timeout: number | null;

  /** Whether the client follows HTTP 3xx redirects automatically. */
  public readonly followRedirects: boolean;

  /** Maximum number of consecutive redirects to follow. */
  public readonly maxRedirects: number | null;

  /** Custom User-Agent header value. */
  public readonly userAgent: string | null;

  /** Transport-level default headers included in every request. */
  public readonly defaultHeaders: Readonly<Record<string, string>>;

  /** Whether to auto-inject an X-Request-ID header with a unique UUID on every request. */
  public readonly injectRequestId: boolean;

  constructor(options: {
    verifySsl: boolean;
    caCertPath: string | null;
    proxy: string | null;
    timeout: number | null;
    followRedirects: boolean;
    maxRedirects: number | null;
    userAgent: string | null;
    defaultHeaders: Record<string, string>;
    injectRequestId: boolean;
  }) {
    this.verifySsl = options.verifySsl;
    this.caCertPath = options.caCertPath;
    this.proxy = options.proxy;
    this.timeout = options.timeout;
    this.followRedirects = options.followRedirects;
    this.maxRedirects = options.maxRedirects;
    this.userAgent = options.userAgent;
    this.defaultHeaders = Object.freeze({ ...options.defaultHeaders });
    this.injectRequestId = options.injectRequestId;
  }

  /**
   * Create a new builder for constructing TransportOptions instances.
   *
   * @returns a new builder with sensible defaults
   */
  public static builder(): TransportOptionsBuilder {
    return new TransportOptionsBuilder();
  }
}

/**
 * Builder for creating immutable {@link TransportOptions} instances.
 *
 * All fields have sensible defaults:
 * - `verifySsl` -- `true`
 * - `followRedirects` -- `true`
 * - `injectRequestId` -- `false`
 * - `timeout` -- `10000` (10 seconds)
 * - All other fields -- `null` or empty
 */
export class TransportOptionsBuilder {
  private _verifySsl: boolean = true;
  private _caCertPath: string | null = null;
  private _proxy: string | null = null;
  private _timeout: number | null = 10000;
  private _followRedirects: boolean = true;
  private _maxRedirects: number | null = null;
  private _userAgent: string | null = "openapi-typescript-client/1.0.0 (node)";
  private _defaultHeaders: Record<string, string> = {};
  private _injectRequestId: boolean = false;

  /**
   * Enable or disable TLS certificate verification.
   *
   * @param verifySsl true to verify (default), false to skip
   * @returns this builder
   */
  verifySsl(verifySsl: boolean): this {
    this._verifySsl = verifySsl;
    return this;
  }

  /**
   * Set the path to a custom CA certificate bundle.
   *
   * @param caCertPath file path to a PEM-encoded CA certificate, or null
   * @returns this builder
   */
  caCertPath(caCertPath: string | null): this {
    this._caCertPath = caCertPath;
    return this;
  }

  /**
   * Set the HTTP/HTTPS proxy URL.
   *
   * @param proxy proxy URL (e.g. "http://proxy:3128"), or null
   * @returns this builder
   */
  proxy(proxy: string | null): this {
    if (proxy != null) {
      const parsed = new URL(proxy);
      if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
        throw new Error(
          `Invalid proxy URL (must use http or https scheme): ${proxy}`,
        );
      }
      if (!parsed.hostname) {
        throw new Error(`Invalid proxy URL (missing host): ${proxy}`);
      }
    }
    this._proxy = proxy;
    return this;
  }

  /**
   * Set the end-to-end request timeout in milliseconds.
   *
   * @param timeout timeout in milliseconds, or null for no timeout
   * @returns this builder
   */
  timeout(timeout: number | null): this {
    this._timeout = timeout;
    return this;
  }

  /**
   * Enable or disable automatic redirect following.
   *
   * @param followRedirects true to follow (default), false to stop
   * @returns this builder
   */
  followRedirects(followRedirects: boolean): this {
    this._followRedirects = followRedirects;
    return this;
  }

  /**
   * Set the maximum number of redirects to follow.
   *
   * @param maxRedirects maximum redirect hops, or null for client default
   * @returns this builder
   */
  maxRedirects(maxRedirects: number | null): this {
    this._maxRedirects = maxRedirects;
    return this;
  }

  /**
   * Set a custom User-Agent header value.
   *
   * @param userAgent the user agent string, or null to omit
   * @returns this builder
   */
  userAgent(userAgent: string | null): this {
    this._userAgent = userAgent;
    return this;
  }

  /**
   * Add a single transport-level default header.
   *
   * @param name header name
   * @param value header value
   * @returns this builder
   */
  defaultHeader(name: string, value: string): this {
    this._defaultHeaders[name] = value;
    return this;
  }

  /**
   * Add multiple transport-level default headers.
   *
   * @param headers map of header names to values
   * @returns this builder
   */
  defaultHeaders(headers: Record<string, string>): this {
    Object.assign(this._defaultHeaders, headers);
    return this;
  }

  /**
   * Enable or disable automatic X-Request-ID header injection.
   *
   * @param injectRequestId true to inject, false to skip (default)
   * @returns this builder
   */
  injectRequestId(injectRequestId: boolean): this {
    this._injectRequestId = injectRequestId;
    return this;
  }

  /**
   * Build and return an immutable TransportOptions instance.
   *
   * @returns the configured transport options
   */
  build(): TransportOptions {
    return new TransportOptions({
      verifySsl: this._verifySsl,
      caCertPath: this._caCertPath,
      proxy: this._proxy,
      timeout: this._timeout,
      followRedirects: this._followRedirects,
      maxRedirects: this._maxRedirects,
      userAgent: this._userAgent,
      defaultHeaders: { ...this._defaultHeaders },
      injectRequestId: this._injectRequestId,
    });
  }
}
