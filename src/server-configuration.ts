// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

/**
 * Represents a server variable from the OpenAPI specification.
 *
 * Server variables define substitution parameters in server URL templates.
 * Each variable has a default value and may optionally restrict values to
 * an enumerated set.
 *
 * This class is immutable.
 *
 * @see ServerConfiguration
 */
export class ServerVariable {
  /** The default value used when no override is provided. */
  public readonly defaultValue: string;

  /** Human-readable description of the variable, or null if not specified. */
  public readonly description: string | null;

  /** Allowed values for this variable. An empty array means any value is accepted. */
  public readonly enumValues: readonly string[];

  /**
   * Create a new server variable.
   *
   * @param defaultValue the default value used when no override is provided
   * @param description human-readable description of the variable, or null
   * @param enumValues allowed values, or an empty array if any value is accepted
   */
  constructor(
    defaultValue: string,
    description: string | null,
    enumValues: string[],
  ) {
    this.defaultValue = defaultValue;
    this.description = description;
    this.enumValues = Object.freeze([...enumValues]);
  }
}

/**
 * Represents a single server entry from the OpenAPI specification.
 *
 * A server URL may contain template variables (e.g.
 * `"https://{env}.api.example.com/v{version}"`). Use {@link getUrl} with
 * no arguments to resolve the URL with default variable values, or pass
 * an overrides record to substitute specific variables.
 *
 * This class is immutable.
 *
 * @see ServerVariable
 */
export class ServerConfiguration {
  /** The raw URL template before variable substitution. */
  public readonly urlTemplate: string;

  /** Human-readable description of this server, or null if not specified. */
  public readonly description: string | null;

  /** The server variables and their definitions. */
  public readonly variables: Readonly<Record<string, ServerVariable>>;

  /**
   * Create a new server configuration.
   *
   * @param urlTemplate the URL template, possibly containing `{variable}` placeholders
   * @param description human-readable description of this server, or null
   * @param variables map of variable names to their definitions
   */
  constructor(
    urlTemplate: string,
    description: string | null,
    variables: Record<string, ServerVariable>,
  ) {
    this.urlTemplate = urlTemplate;
    this.description = description;
    this.variables = Object.freeze({ ...variables });
  }

  /**
   * Resolve the URL template, substituting the given variable overrides.
   *
   * Variables not present in `overrides` use their default values.
   * If a variable has an enum constraint, the override value is validated
   * against the allowed values.
   *
   * @param overrides variable name to value overrides (defaults to empty)
   * @returns the fully resolved URL
   * @throws Error if an override value is not in the variable's enum
   */
  getUrl(overrides: Record<string, string> = {}): string {
    let url = this.urlTemplate;
    for (const [varName, variable] of Object.entries(this.variables)) {
      const value = overrides[varName] ?? variable.defaultValue;

      if (
        variable.enumValues.length > 0 &&
        !variable.enumValues.includes(value)
      ) {
        throw new Error(
          `Invalid value '${value}' for server variable '${varName}'. Allowed values: ${variable.enumValues.join(", ")}`,
        );
      }

      url = url.replaceAll(`{${varName}}`, value);
    }
    return url;
  }
}
