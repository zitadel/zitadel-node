// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaTelemetryServiceInstanceInformation } from "./beta-telemetry-service-instance-information.js";
import { Expose, Type } from "class-transformer";

export class BetaTelemetryServiceReportBaseInformationRequest {
  /**
   * The system ID is a unique identifier for the ZITADEL system.
   * @example null
   */
  @Expose({ name: "systemId" })
  systemId?: string;
  /**
   * The current version of the ZITADEL system.
   * @example null
   */
  @Expose({ name: "version" })
  version?: string;
  /**
   * A list of instances in the ZITADEL system and their information.
   * @example null
   */
  @Expose({ name: "instances" })
  @Type(() => BetaTelemetryServiceInstanceInformation)
  instances?: Array<BetaTelemetryServiceInstanceInformation>;

  constructor(
    data?: Partial<BetaTelemetryServiceReportBaseInformationRequest>,
  ) {
    Object.assign(this, data);
    if (this.systemId != null && typeof this.systemId !== "string") {
      throw new TypeError(
        `systemId must be a string, got ${typeof this.systemId}`,
      );
    }
    if (this.version != null && typeof this.version !== "string") {
      throw new TypeError(
        `version must be a string, got ${typeof this.version}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaTelemetryServiceReportBaseInformationRequest)) {
      return false;
    }
    if (
      !BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(
        this.systemId,
        (other as BetaTelemetryServiceReportBaseInformationRequest).systemId,
      )
    ) {
      return false;
    }
    if (
      !BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(
        this.version,
        (other as BetaTelemetryServiceReportBaseInformationRequest).version,
      )
    ) {
      return false;
    }
    if (
      !BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(
        this.instances,
        (other as BetaTelemetryServiceReportBaseInformationRequest).instances,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaTelemetryServiceReportBaseInformationRequest.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
