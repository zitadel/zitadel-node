// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaAppServiceOIDCLocalizedMessage } from "./beta-app-service-oidc-localized-message.js";
import { Expose, Type } from "class-transformer";

export class BetaAppServiceCreateOIDCApplicationResponse {
  /** @example null */
  @Expose({ name: "clientId" })
  clientId?: string;
  /** @example null */
  @Expose({ name: "clientSecret" })
  clientSecret?: string;
  /** @example null */
  @Expose({ name: "noneCompliant" })
  noneCompliant?: boolean;
  /** @example null */
  @Expose({ name: "complianceProblems" })
  @Type(() => BetaAppServiceOIDCLocalizedMessage)
  complianceProblems?: Array<BetaAppServiceOIDCLocalizedMessage>;

  constructor(data?: Partial<BetaAppServiceCreateOIDCApplicationResponse>) {
    Object.assign(this, data);
    if (this.clientId != null && typeof this.clientId !== "string") {
      throw new TypeError(
        `clientId must be a string, got ${typeof this.clientId}`,
      );
    }
    if (this.clientSecret != null && typeof this.clientSecret !== "string") {
      throw new TypeError(
        `clientSecret must be a string, got ${typeof this.clientSecret}`,
      );
    }
    if (this.noneCompliant != null && typeof this.noneCompliant !== "boolean") {
      throw new TypeError(
        `noneCompliant must be a boolean, got ${typeof this.noneCompliant}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaAppServiceCreateOIDCApplicationResponse)) {
      return false;
    }
    if (
      !BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(
        this.clientId,
        (other as BetaAppServiceCreateOIDCApplicationResponse).clientId,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(
        this.clientSecret,
        (other as BetaAppServiceCreateOIDCApplicationResponse).clientSecret,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(
        this.noneCompliant,
        (other as BetaAppServiceCreateOIDCApplicationResponse).noneCompliant,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(
        this.complianceProblems,
        (other as BetaAppServiceCreateOIDCApplicationResponse)
          .complianceProblems,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaAppServiceCreateOIDCApplicationResponse.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
