// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceIntentFactor } from "./session-service-intent-factor.js";
import { SessionServiceOTPFactor } from "./session-service-otp-factor.js";
import { SessionServicePasswordFactor } from "./session-service-password-factor.js";
import { SessionServiceRecoveryCodeFactor } from "./session-service-recovery-code-factor.js";
import { SessionServiceTOTPFactor } from "./session-service-totp-factor.js";
import { SessionServiceUserFactor } from "./session-service-user-factor.js";
import { SessionServiceWebAuthNFactor } from "./session-service-web-auth-n-factor.js";
import { Expose, Type } from "class-transformer";

export class SessionServiceFactors {
  /** @example null */
  @Expose({ name: "user" })
  @Type(() => SessionServiceUserFactor)
  user?: SessionServiceUserFactor;
  /** @example null */
  @Expose({ name: "password" })
  @Type(() => SessionServicePasswordFactor)
  password?: SessionServicePasswordFactor;
  /** @example null */
  @Expose({ name: "webAuthN" })
  @Type(() => SessionServiceWebAuthNFactor)
  webAuthN?: SessionServiceWebAuthNFactor;
  /** @example null */
  @Expose({ name: "intent" })
  @Type(() => SessionServiceIntentFactor)
  intent?: SessionServiceIntentFactor;
  /** @example null */
  @Expose({ name: "totp" })
  @Type(() => SessionServiceTOTPFactor)
  totp?: SessionServiceTOTPFactor;
  /** @example null */
  @Expose({ name: "otpSms" })
  @Type(() => SessionServiceOTPFactor)
  otpSms?: SessionServiceOTPFactor;
  /** @example null */
  @Expose({ name: "otpEmail" })
  @Type(() => SessionServiceOTPFactor)
  otpEmail?: SessionServiceOTPFactor;
  /** @example null */
  @Expose({ name: "recoveryCode" })
  @Type(() => SessionServiceRecoveryCodeFactor)
  recoveryCode?: SessionServiceRecoveryCodeFactor;

  constructor(data?: Partial<SessionServiceFactors>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceFactors)) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.user,
        (other as SessionServiceFactors).user,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.password,
        (other as SessionServiceFactors).password,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.webAuthN,
        (other as SessionServiceFactors).webAuthN,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.intent,
        (other as SessionServiceFactors).intent,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.totp,
        (other as SessionServiceFactors).totp,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.otpSms,
        (other as SessionServiceFactors).otpSms,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.otpEmail,
        (other as SessionServiceFactors).otpEmail,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceFactors.__deepEquals(
        this.recoveryCode,
        (other as SessionServiceFactors).recoveryCode,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => SessionServiceFactors.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceFactors.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !SessionServiceFactors.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceFactors.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
