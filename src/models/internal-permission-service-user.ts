// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class InternalPermissionServiceUser {
  /**
   * ID is the unique identifier of the user.
   * @example null
   */
  @Expose({ name: "id" })
  id?: string;
  /**
   * PreferredLoginName is the preferred login name of the user. This value is unique across the whole instance.
   * @example null
   */
  @Expose({ name: "preferredLoginName" })
  preferredLoginName?: string;
  /**
   * DisplayName is the public display name of the user.  By default it's the user's given name and family name, their username or their email address.
   * @example null
   */
  @Expose({ name: "displayName" })
  displayName?: string;
  /**
   * The organization the user belong to.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;

  constructor(data?: Partial<InternalPermissionServiceUser>) {
    Object.assign(this, data);
    if (this.id != null && typeof this.id !== "string") {
      throw new TypeError(`id must be a string, got ${typeof this.id}`);
    }
    if (
      this.preferredLoginName != null &&
      typeof this.preferredLoginName !== "string"
    ) {
      throw new TypeError(
        `preferredLoginName must be a string, got ${typeof this.preferredLoginName}`,
      );
    }
    if (this.displayName != null && typeof this.displayName !== "string") {
      throw new TypeError(
        `displayName must be a string, got ${typeof this.displayName}`,
      );
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof InternalPermissionServiceUser)) {
      return false;
    }
    if (
      !InternalPermissionServiceUser.__deepEquals(
        this.id,
        (other as InternalPermissionServiceUser).id,
      )
    ) {
      return false;
    }
    if (
      !InternalPermissionServiceUser.__deepEquals(
        this.preferredLoginName,
        (other as InternalPermissionServiceUser).preferredLoginName,
      )
    ) {
      return false;
    }
    if (
      !InternalPermissionServiceUser.__deepEquals(
        this.displayName,
        (other as InternalPermissionServiceUser).displayName,
      )
    ) {
      return false;
    }
    if (
      !InternalPermissionServiceUser.__deepEquals(
        this.organizationId,
        (other as InternalPermissionServiceUser).organizationId,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        InternalPermissionServiceUser.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => InternalPermissionServiceUser.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !InternalPermissionServiceUser.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          InternalPermissionServiceUser.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
