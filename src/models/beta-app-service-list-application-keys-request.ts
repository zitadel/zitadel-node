// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaAppServiceApplicationKeysSorting } from "./beta-app-service-application-keys-sorting.js";
import { BetaAppServicePaginationRequest } from "./beta-app-service-pagination-request.js";
import { Expose, Type } from "class-transformer";

export class BetaAppServiceListApplicationKeysRequest {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"amount":"12.345"` (a JSON string) instead of the spec-required
   * `"amount":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "pagination" })
  @Type(() => BetaAppServicePaginationRequest)
  pagination?: BetaAppServicePaginationRequest;
  @Expose({ name: "sortingColumn" })
  sortingColumn?: BetaAppServiceApplicationKeysSorting;
  @Expose({ name: "applicationId" })
  applicationId?: string;
  @Expose({ name: "organizationId" })
  organizationId?: string;
  @Expose({ name: "projectId" })
  projectId?: string;

  constructor(data?: Partial<BetaAppServiceListApplicationKeysRequest>) {
    Object.assign(this, data);
    if (this.applicationId != null && typeof this.applicationId !== "string") {
      throw new TypeError(
        `applicationId must be a string, got ${typeof this.applicationId}`,
      );
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.projectId != null && typeof this.projectId !== "string") {
      throw new TypeError(
        `projectId must be a string, got ${typeof this.projectId}`,
      );
    }
    if (this.sortingColumn != null) {
      const sortingColumnValues = Object.values(
        BetaAppServiceApplicationKeysSorting,
      ).filter(
        (v) =>
          typeof (
            BetaAppServiceApplicationKeysSorting as Record<string, unknown>
          )[v as string] !== "number",
      );
      if (
        !(sortingColumnValues as readonly unknown[]).includes(
          this.sortingColumn,
        )
      ) {
        throw new Error(
          `Unknown enum value for sortingColumn: ${JSON.stringify(this.sortingColumn)}. ` +
            `Expected one of [${sortingColumnValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaAppServiceListApplicationKeysRequest)) {
      return false;
    }
    if (
      !BetaAppServiceListApplicationKeysRequest.__deepEquals(
        this.pagination,
        (other as BetaAppServiceListApplicationKeysRequest).pagination,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceListApplicationKeysRequest.__deepEquals(
        this.sortingColumn,
        (other as BetaAppServiceListApplicationKeysRequest).sortingColumn,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceListApplicationKeysRequest.__deepEquals(
        this.applicationId,
        (other as BetaAppServiceListApplicationKeysRequest).applicationId,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceListApplicationKeysRequest.__deepEquals(
        this.organizationId,
        (other as BetaAppServiceListApplicationKeysRequest).organizationId,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceListApplicationKeysRequest.__deepEquals(
        this.projectId,
        (other as BetaAppServiceListApplicationKeysRequest).projectId,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaAppServiceListApplicationKeysRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaAppServiceListApplicationKeysRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaAppServiceListApplicationKeysRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaAppServiceListApplicationKeysRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
