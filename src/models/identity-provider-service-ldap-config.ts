// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { IdentityProviderServiceLDAPAttributes } from "./identity-provider-service-ldap-attributes.js";
import { Expose, Type, Transform } from "class-transformer";
import { Temporal } from "temporal-polyfill";
import {
  durationFromProtoJson,
  durationToProtoJson,
} from "../object-serializer.js";

export class IdentityProviderServiceLDAPConfig {
  /** @example null */
  @Expose({ name: "servers" })
  servers?: Array<string>;
  /** @example null */
  @Expose({ name: "startTls" })
  startTls?: boolean;
  /** @example null */
  @Expose({ name: "baseDn" })
  baseDn?: string;
  /** @example null */
  @Expose({ name: "bindDn" })
  bindDn?: string;
  /** @example null */
  @Expose({ name: "userBase" })
  userBase?: string;
  /** @example null */
  @Expose({ name: "userObjectClasses" })
  userObjectClasses?: Array<string>;
  /** @example null */
  @Expose({ name: "userFilters" })
  userFilters?: Array<string>;
  /**
   * A Duration represents a signed, fixed-length span of time represented  as a count of seconds and fractions of seconds at nanosecond  resolution. It is independent of any calendar and concepts like \"day\"  or \"month\". It is related to Timestamp in that the difference between  two Timestamp values is a Duration and it can be added or subtracted  from a Timestamp. Range is approximately +-10,000 years.   # Examples   Example 1: Compute Duration from two Timestamps in pseudo code.       Timestamp start = ...;      Timestamp end = ...;      Duration duration = ...;       duration.seconds = end.seconds - start.seconds;      duration.nanos = end.nanos - start.nanos;       if (duration.seconds < 0 && duration.nanos > 0) {        duration.seconds += 1;        duration.nanos -= 1000000000;      } else if (duration.seconds > 0 && duration.nanos < 0) {        duration.seconds -= 1;        duration.nanos += 1000000000;      }   Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.       Timestamp start = ...;      Duration duration = ...;      Timestamp end = ...;       end.seconds = start.seconds + duration.seconds;      end.nanos = start.nanos + duration.nanos;       if (end.nanos < 0) {        end.seconds -= 1;        end.nanos += 1000000000;      } else if (end.nanos >= 1000000000) {        end.seconds += 1;        end.nanos -= 1000000000;      }   Example 3: Compute Duration from datetime.timedelta in Python.       td = datetime.timedelta(days=3, minutes=10)      duration = Duration()      duration.FromTimedelta(td)   # JSON Mapping   In JSON format, the Duration type is encoded as a string rather than an  object, where the string ends in the suffix \"s\" (indicating seconds) and  is preceded by the number of seconds, with nanoseconds expressed as  fractional seconds. For example, 3 seconds with 0 nanoseconds should be  encoded in JSON format as \"3s\", while 3 seconds and 1 nanosecond should  be expressed in JSON format as \"3.000000001s\", and 3 seconds and 1  microsecond should be expressed in JSON format as \"3.000001s\".
   * @example null
   */
  @Expose({ name: "timeout" })
  /*
   * 4.8 — `format: duration` is a google.protobuf.Duration. The native
   * type stays Temporal.Duration, but the wire form is protobuf-JSON
   * ("3600s"), not ISO-8601 ("PT1H") — Zitadel rejects the ISO form.
   */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? durationFromProtoJson(value) : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      value instanceof Temporal.Duration ? durationToProtoJson(value) : value,
    { toPlainOnly: true },
  )
  timeout?: Temporal.Duration;
  /** @example null */
  @Expose({ name: "attributes" })
  @Type(() => IdentityProviderServiceLDAPAttributes)
  attributes?: IdentityProviderServiceLDAPAttributes;
  /** @example null */
  @Expose({ name: "rootCa" })
  /** 2.1 — `format: byte` round-trips Buffer <-> base64 string at the serde boundary. */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? Buffer.from(value, "base64") : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      Buffer.isBuffer(value) ? (value as Buffer).toString("base64") : value,
    { toPlainOnly: true },
  )
  rootCa?: Buffer;

  constructor(data?: Partial<IdentityProviderServiceLDAPConfig>) {
    Object.assign(this, data);
    if (
      this.servers != null &&
      !Array.isArray(this.servers) &&
      !((this.servers as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `servers must be an array, got ${typeof this.servers}`,
      );
    }
    if (this.startTls != null && typeof this.startTls !== "boolean") {
      throw new TypeError(
        `startTls must be a boolean, got ${typeof this.startTls}`,
      );
    }
    if (this.baseDn != null && typeof this.baseDn !== "string") {
      throw new TypeError(`baseDn must be a string, got ${typeof this.baseDn}`);
    }
    if (this.bindDn != null && typeof this.bindDn !== "string") {
      throw new TypeError(`bindDn must be a string, got ${typeof this.bindDn}`);
    }
    if (this.userBase != null && typeof this.userBase !== "string") {
      throw new TypeError(
        `userBase must be a string, got ${typeof this.userBase}`,
      );
    }
    if (
      this.userObjectClasses != null &&
      !Array.isArray(this.userObjectClasses) &&
      !((this.userObjectClasses as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `userObjectClasses must be an array, got ${typeof this.userObjectClasses}`,
      );
    }
    if (
      this.userFilters != null &&
      !Array.isArray(this.userFilters) &&
      !((this.userFilters as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `userFilters must be an array, got ${typeof this.userFilters}`,
      );
    }
    /**
     * 4.8 — `format: duration` (google.protobuf.Duration). Accept a
     * protobuf-JSON string ("3600s", parsed via durationFromProtoJson,
     * which throws on bad input) or an already-constructed Duration
     * instance.
     */
    if (this.timeout != null) {
      if (typeof this.timeout === "string") {
        this.timeout = durationFromProtoJson(
          this.timeout as unknown as string,
        ) as unknown as Temporal.Duration;
      } else if (!(this.timeout instanceof Temporal.Duration)) {
        throw new TypeError(
          `timeout must be a Temporal.Duration or protobuf-JSON duration string, got ${typeof this.timeout}`,
        );
      }
    }
    /**
     * 2.1 — format: byte. The wire form is base64; the model field is a
     * Buffer. Lives outside the isString block because typeMapping
     * (ByteArray to Buffer) flips isString to false at codegen time.
     */
    if (this.rootCa != null && typeof this.rootCa === "string") {
      this.rootCa = Buffer.from(
        this.rootCa as unknown as string,
        "base64",
      ) as unknown as Buffer;
    } else if (this.rootCa != null && !Buffer.isBuffer(this.rootCa)) {
      throw new TypeError(
        `rootCa must be a Buffer or base64 string, got ${typeof this.rootCa}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof IdentityProviderServiceLDAPConfig)) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.servers,
        (other as IdentityProviderServiceLDAPConfig).servers,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.startTls,
        (other as IdentityProviderServiceLDAPConfig).startTls,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.baseDn,
        (other as IdentityProviderServiceLDAPConfig).baseDn,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.bindDn,
        (other as IdentityProviderServiceLDAPConfig).bindDn,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.userBase,
        (other as IdentityProviderServiceLDAPConfig).userBase,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.userObjectClasses,
        (other as IdentityProviderServiceLDAPConfig).userObjectClasses,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.userFilters,
        (other as IdentityProviderServiceLDAPConfig).userFilters,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.timeout,
        (other as IdentityProviderServiceLDAPConfig).timeout,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.attributes,
        (other as IdentityProviderServiceLDAPConfig).attributes,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPConfig.__deepEquals(
        this.rootCa,
        (other as IdentityProviderServiceLDAPConfig).rootCa,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        IdentityProviderServiceLDAPConfig.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => IdentityProviderServiceLDAPConfig.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !IdentityProviderServiceLDAPConfig.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          IdentityProviderServiceLDAPConfig.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
