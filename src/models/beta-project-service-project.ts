// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaProjectServiceGrantedProjectState } from "./beta-project-service-granted-project-state.js";
import { BetaProjectServicePrivateLabelingSetting } from "./beta-project-service-private-labeling-setting.js";
import { BetaProjectServiceProjectState } from "./beta-project-service-project-state.js";
import { Expose, Type } from "class-transformer";

export class BetaProjectServiceProject {
  /**
   * The unique identifier of the project.
   * @example null
   */
  @Expose({ name: "id" })
  id?: string;
  /**
   * The unique identifier of the organization the project belongs to.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * A Timestamp represents a point in time independent of any time zone or local  calendar, encoded as a count of seconds and fractions of seconds at  nanosecond resolution. The count is relative to an epoch at UTC midnight on  January 1, 1970, in the proleptic Gregorian calendar which extends the  Gregorian calendar backwards to year one.   All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap  second table is needed for interpretation, using a [24-hour linear  smear](https://developers.google.com/time/smear).   The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By  restricting to that range, we ensure that we can convert to and from [RFC  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.   # Examples   Example 1: Compute Timestamp from POSIX `time()`.       Timestamp timestamp;      timestamp.set_seconds(time(NULL));      timestamp.set_nanos(0);   Example 2: Compute Timestamp from POSIX `gettimeofday()`.       struct timeval tv;      gettimeofday(&tv, NULL);       Timestamp timestamp;      timestamp.set_seconds(tv.tv_sec);      timestamp.set_nanos(tv.tv_usec * 1000);   Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.       FILETIME ft;      GetSystemTimeAsFileTime(&ft);      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;       // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.      Timestamp timestamp;      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));   Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.       long millis = System.currentTimeMillis();       Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)          .setNanos((int) ((millis % 1000) * 1000000)).build();   Example 5: Compute Timestamp from Java `Instant.now()`.       Instant now = Instant.now();       Timestamp timestamp =          Timestamp.newBuilder().setSeconds(now.getEpochSecond())              .setNanos(now.getNano()).build();   Example 6: Compute Timestamp from current time in Python.       timestamp = Timestamp()      timestamp.GetCurrentTime()   # JSON Mapping   In JSON format, the Timestamp type is encoded as a string in the  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the  format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\"  where {year} is always expressed using four digits while {month}, {day},  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),  are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone  is required. A proto3 JSON serializer should always use UTC (as indicated by  \"Z\") when printing the Timestamp type and a proto3 JSON parser should be  able to accept both UTC and other timezones (as indicated by an offset).   For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past  01:30 UTC on January 15, 2017.   In JavaScript, one can convert a Date object to this format using the  standard  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)  method. In Python, a standard `datetime.datetime` object can be converted  to this format using  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use  the Joda Time's [`ISODateTimeFormat.dateTime()`](  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()  ) to obtain a formatter capable of generating timestamps in this format.
   * @example null
   */
  @Expose({ name: "creationDate" })
  @Type(() => Date)
  creationDate?: Date;
  /**
   * A Timestamp represents a point in time independent of any time zone or local  calendar, encoded as a count of seconds and fractions of seconds at  nanosecond resolution. The count is relative to an epoch at UTC midnight on  January 1, 1970, in the proleptic Gregorian calendar which extends the  Gregorian calendar backwards to year one.   All minutes are 60 seconds long. Leap seconds are \"smeared\" so that no leap  second table is needed for interpretation, using a [24-hour linear  smear](https://developers.google.com/time/smear).   The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By  restricting to that range, we ensure that we can convert to and from [RFC  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.   # Examples   Example 1: Compute Timestamp from POSIX `time()`.       Timestamp timestamp;      timestamp.set_seconds(time(NULL));      timestamp.set_nanos(0);   Example 2: Compute Timestamp from POSIX `gettimeofday()`.       struct timeval tv;      gettimeofday(&tv, NULL);       Timestamp timestamp;      timestamp.set_seconds(tv.tv_sec);      timestamp.set_nanos(tv.tv_usec * 1000);   Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.       FILETIME ft;      GetSystemTimeAsFileTime(&ft);      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;       // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.      Timestamp timestamp;      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));   Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.       long millis = System.currentTimeMillis();       Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)          .setNanos((int) ((millis % 1000) * 1000000)).build();   Example 5: Compute Timestamp from Java `Instant.now()`.       Instant now = Instant.now();       Timestamp timestamp =          Timestamp.newBuilder().setSeconds(now.getEpochSecond())              .setNanos(now.getNano()).build();   Example 6: Compute Timestamp from current time in Python.       timestamp = Timestamp()      timestamp.GetCurrentTime()   # JSON Mapping   In JSON format, the Timestamp type is encoded as a string in the  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the  format is \"{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z\"  where {year} is always expressed using four digits while {month}, {day},  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),  are optional. The \"Z\" suffix indicates the timezone (\"UTC\"); the timezone  is required. A proto3 JSON serializer should always use UTC (as indicated by  \"Z\") when printing the Timestamp type and a proto3 JSON parser should be  able to accept both UTC and other timezones (as indicated by an offset).   For example, \"2017-01-15T01:30:15.01Z\" encodes 15.01 seconds past  01:30 UTC on January 15, 2017.   In JavaScript, one can convert a Date object to this format using the  standard  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)  method. In Python, a standard `datetime.datetime` object can be converted  to this format using  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use  the Joda Time's [`ISODateTimeFormat.dateTime()`](  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()  ) to obtain a formatter capable of generating timestamps in this format.
   * @example null
   */
  @Expose({ name: "changeDate" })
  @Type(() => Date)
  changeDate?: Date;
  /**
   * The name of the project.
   * @example null
   */
  @Expose({ name: "name" })
  name?: string;
  /** @example null */
  @Expose({ name: "state" })
  state?: BetaProjectServiceProjectState;
  /**
   * Describes if the roles of the user should be added to the token.
   * @example null
   */
  @Expose({ name: "projectRoleAssertion" })
  projectRoleAssertion?: boolean;
  /**
   * When enabled ZITADEL will check if a user has an authorization to use this project assigned when login into an application of this project.
   * @example null
   */
  @Expose({ name: "authorizationRequired" })
  authorizationRequired?: boolean;
  /**
   * When enabled ZITADEL will check if the organization of the user, that is trying to log in, has access to this project (either owns the project or is granted).
   * @example null
   */
  @Expose({ name: "projectAccessRequired" })
  projectAccessRequired?: boolean;
  /** @example null */
  @Expose({ name: "privateLabelingSetting" })
  privateLabelingSetting?: BetaProjectServicePrivateLabelingSetting;
  /**
   * The ID of the organization the project is granted to.
   * @example null
   */
  @Expose({ name: "grantedOrganizationId" })
  grantedOrganizationId?: string;
  /**
   * The name of the organization the project is granted to.
   * @example null
   */
  @Expose({ name: "grantedOrganizationName" })
  grantedOrganizationName?: string;
  /** @example null */
  @Expose({ name: "grantedState" })
  grantedState?: BetaProjectServiceGrantedProjectState;

  constructor(data?: Partial<BetaProjectServiceProject>) {
    Object.assign(this, data);
    if (this.id != null && typeof this.id !== "string") {
      throw new TypeError(`id must be a string, got ${typeof this.id}`);
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
    if (
      this.projectRoleAssertion != null &&
      typeof this.projectRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `projectRoleAssertion must be a boolean, got ${typeof this.projectRoleAssertion}`,
      );
    }
    if (
      this.authorizationRequired != null &&
      typeof this.authorizationRequired !== "boolean"
    ) {
      throw new TypeError(
        `authorizationRequired must be a boolean, got ${typeof this.authorizationRequired}`,
      );
    }
    if (
      this.projectAccessRequired != null &&
      typeof this.projectAccessRequired !== "boolean"
    ) {
      throw new TypeError(
        `projectAccessRequired must be a boolean, got ${typeof this.projectAccessRequired}`,
      );
    }
    if (
      this.grantedOrganizationId != null &&
      typeof this.grantedOrganizationId !== "string"
    ) {
      throw new TypeError(
        `grantedOrganizationId must be a string, got ${typeof this.grantedOrganizationId}`,
      );
    }
    if (
      this.grantedOrganizationName != null &&
      typeof this.grantedOrganizationName !== "string"
    ) {
      throw new TypeError(
        `grantedOrganizationName must be a string, got ${typeof this.grantedOrganizationName}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaProjectServiceProject)) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.id,
        (other as BetaProjectServiceProject).id,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.organizationId,
        (other as BetaProjectServiceProject).organizationId,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.creationDate,
        (other as BetaProjectServiceProject).creationDate,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.changeDate,
        (other as BetaProjectServiceProject).changeDate,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.name,
        (other as BetaProjectServiceProject).name,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.state,
        (other as BetaProjectServiceProject).state,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.projectRoleAssertion,
        (other as BetaProjectServiceProject).projectRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.authorizationRequired,
        (other as BetaProjectServiceProject).authorizationRequired,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.projectAccessRequired,
        (other as BetaProjectServiceProject).projectAccessRequired,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.privateLabelingSetting,
        (other as BetaProjectServiceProject).privateLabelingSetting,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.grantedOrganizationId,
        (other as BetaProjectServiceProject).grantedOrganizationId,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.grantedOrganizationName,
        (other as BetaProjectServiceProject).grantedOrganizationName,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceProject.__deepEquals(
        this.grantedState,
        (other as BetaProjectServiceProject).grantedState,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaProjectServiceProject.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaProjectServiceProject.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaProjectServiceProject.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaProjectServiceProject.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
