// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { OrganizationServiceHashedPassword } from "./organization-service-hashed-password.js";
import { OrganizationServiceIDPLink } from "./organization-service-idp-link.js";
import { OrganizationServiceOrganization } from "./organization-service-organization.js";
import { OrganizationServicePassword } from "./organization-service-password.js";
import { OrganizationServiceSetHumanEmail } from "./organization-service-set-human-email.js";
import { OrganizationServiceSetHumanPhone } from "./organization-service-set-human-phone.js";
import { OrganizationServiceSetHumanProfile } from "./organization-service-set-human-profile.js";
import { OrganizationServiceSetMetadataEntry } from "./organization-service-set-metadata-entry.js";
import { Expose, Type } from "class-transformer";
import { Email } from "../brand.js";

export class OrganizationServiceAddHumanUserRequest {
  /**
   * optionally set your own id unique for the user.
   * @example null
   */
  @Expose({ name: "userId" })
  userId?: string;
  /**
   * optionally set a unique username, if none is provided the email will be used.
   * @example null
   */
  @Expose({ name: "username" })
  username?: string;
  /** @example null */
  @Expose({ name: "organization" })
  @Type(() => OrganizationServiceOrganization)
  organization?: OrganizationServiceOrganization;
  /** @example null */
  @Expose({ name: "profile" })
  @Type(() => OrganizationServiceSetHumanProfile)
  profile?: OrganizationServiceSetHumanProfile;
  /** @example null */
  @Expose({ name: "email" })
  @Type(() => OrganizationServiceSetHumanEmail)
  email?: OrganizationServiceSetHumanEmail;
  /** @example null */
  @Expose({ name: "phone" })
  @Type(() => OrganizationServiceSetHumanPhone)
  phone?: OrganizationServiceSetHumanPhone;
  /** @example null */
  @Expose({ name: "metadata" })
  @Type(() => OrganizationServiceSetMetadataEntry)
  metadata?: Array<OrganizationServiceSetMetadataEntry>;
  /** @example null */
  @Expose({ name: "idpLinks" })
  @Type(() => OrganizationServiceIDPLink)
  idpLinks?: Array<OrganizationServiceIDPLink>;
  /**
   * An Implementation of RFC 6238 is used, with HMAC-SHA-1 and time-step of 30 seconds.  Currently no other options are supported, and if anything different is used the validation will fail.
   * @example null
   */
  @Expose({ name: "totpSecret" })
  totpSecret?: string;
  /** @example null */
  @Expose({ name: "hashedPassword" })
  @Type(() => OrganizationServiceHashedPassword)
  hashedPassword?: OrganizationServiceHashedPassword;
  /** @example null */
  @Expose({ name: "password" })
  @Type(() => OrganizationServicePassword)
  password?: OrganizationServicePassword;

  constructor(data?: Partial<OrganizationServiceAddHumanUserRequest>) {
    Object.assign(this, data);
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.username != null && typeof this.username !== "string") {
      throw new TypeError(
        `username must be a string, got ${typeof this.username}`,
      );
    }
    if (this.totpSecret != null && typeof this.totpSecret !== "string") {
      throw new TypeError(
        `totpSecret must be a string, got ${typeof this.totpSecret}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof OrganizationServiceAddHumanUserRequest)) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.userId,
        (other as OrganizationServiceAddHumanUserRequest).userId,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.username,
        (other as OrganizationServiceAddHumanUserRequest).username,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.organization,
        (other as OrganizationServiceAddHumanUserRequest).organization,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.profile,
        (other as OrganizationServiceAddHumanUserRequest).profile,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.email,
        (other as OrganizationServiceAddHumanUserRequest).email,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.phone,
        (other as OrganizationServiceAddHumanUserRequest).phone,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.metadata,
        (other as OrganizationServiceAddHumanUserRequest).metadata,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.idpLinks,
        (other as OrganizationServiceAddHumanUserRequest).idpLinks,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.totpSecret,
        (other as OrganizationServiceAddHumanUserRequest).totpSecret,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.hashedPassword,
        (other as OrganizationServiceAddHumanUserRequest).hashedPassword,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddHumanUserRequest.__deepEquals(
        this.password,
        (other as OrganizationServiceAddHumanUserRequest).password,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        OrganizationServiceAddHumanUserRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          OrganizationServiceAddHumanUserRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !OrganizationServiceAddHumanUserRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          OrganizationServiceAddHumanUserRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
