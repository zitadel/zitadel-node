// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaAppServiceLoginVersion } from "./beta-app-service-login-version.js";
import { BetaAppServiceOIDCAppType } from "./beta-app-service-oidc-app-type.js";
import { BetaAppServiceOIDCAuthMethodType } from "./beta-app-service-oidc-auth-method-type.js";
import { BetaAppServiceOIDCGrantType } from "./beta-app-service-oidc-grant-type.js";
import { BetaAppServiceOIDCLocalizedMessage } from "./beta-app-service-oidc-localized-message.js";
import { BetaAppServiceOIDCResponseType } from "./beta-app-service-oidc-response-type.js";
import { BetaAppServiceOIDCTokenType } from "./beta-app-service-oidc-token-type.js";
import { BetaAppServiceOIDCVersion } from "./beta-app-service-oidc-version.js";
import { Expose, Type, Transform } from "class-transformer";
import { Temporal } from "temporal-polyfill";
import {
  durationFromProtoJson,
  durationToProtoJson,
} from "../object-serializer.js";

export class BetaAppServiceOIDCConfig {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"weightKg":"12.345"` (a JSON string) instead of the spec-required
   * `"weightKg":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "redirectUris" })
  redirectUris?: Array<string>;
  @Expose({ name: "responseTypes" })
  responseTypes?: Array<BetaAppServiceOIDCResponseType>;
  @Expose({ name: "grantTypes" })
  grantTypes?: Array<BetaAppServiceOIDCGrantType>;
  @Expose({ name: "appType" })
  appType?: BetaAppServiceOIDCAppType;
  @Expose({ name: "clientId" })
  clientId?: string;
  @Expose({ name: "authMethodType" })
  authMethodType?: BetaAppServiceOIDCAuthMethodType;
  @Expose({ name: "postLogoutRedirectUris" })
  postLogoutRedirectUris?: Array<string>;
  @Expose({ name: "version" })
  version?: BetaAppServiceOIDCVersion;
  @Expose({ name: "noneCompliant" })
  noneCompliant?: boolean;
  @Expose({ name: "complianceProblems" })
  @Type(() => BetaAppServiceOIDCLocalizedMessage)
  complianceProblems?: Array<BetaAppServiceOIDCLocalizedMessage>;
  @Expose({ name: "devMode" })
  devMode?: boolean;
  @Expose({ name: "accessTokenType" })
  accessTokenType?: BetaAppServiceOIDCTokenType;
  @Expose({ name: "accessTokenRoleAssertion" })
  accessTokenRoleAssertion?: boolean;
  @Expose({ name: "idTokenRoleAssertion" })
  idTokenRoleAssertion?: boolean;
  @Expose({ name: "idTokenUserinfoAssertion" })
  idTokenUserinfoAssertion?: boolean;
  /**
   * A Duration represents a signed, fixed-length span of time represented  as a count of seconds and fractions of seconds at nanosecond  resolution. It is independent of any calendar and concepts like "day"  or "month". It is related to Timestamp in that the difference between  two Timestamp values is a Duration and it can be added or subtracted  from a Timestamp. Range is approximately +-10,000 years.   # Examples   Example 1: Compute Duration from two Timestamps in pseudo code.       Timestamp start = ...;      Timestamp end = ...;      Duration duration = ...;       duration.seconds = end.seconds - start.seconds;      duration.nanos = end.nanos - start.nanos;       if (duration.seconds < 0 && duration.nanos > 0) {        duration.seconds += 1;        duration.nanos -= 1000000000;      } else if (duration.seconds > 0 && duration.nanos < 0) {        duration.seconds -= 1;        duration.nanos += 1000000000;      }   Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.       Timestamp start = ...;      Duration duration = ...;      Timestamp end = ...;       end.seconds = start.seconds + duration.seconds;      end.nanos = start.nanos + duration.nanos;       if (end.nanos < 0) {        end.seconds -= 1;        end.nanos += 1000000000;      } else if (end.nanos >= 1000000000) {        end.seconds += 1;        end.nanos -= 1000000000;      }   Example 3: Compute Duration from datetime.timedelta in Python.       td = datetime.timedelta(days=3, minutes=10)      duration = Duration()      duration.FromTimedelta(td)   # JSON Mapping   In JSON format, the Duration type is encoded as a string rather than an  object, where the string ends in the suffix "s" (indicating seconds) and  is preceded by the number of seconds, with nanoseconds expressed as  fractional seconds. For example, 3 seconds with 0 nanoseconds should be  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should  be expressed in JSON format as "3.000000001s", and 3 seconds and 1  microsecond should be expressed in JSON format as "3.000001s".
   */
  @Expose({ name: "clockSkew" })
  /*
   * 4.8 — `format: duration` is a google.protobuf.Duration. The native
   * type stays Temporal.Duration, but the wire form is protobuf-JSON
   * ("3600s"), not ISO-8601 ("PT1H") — Zitadel rejects the ISO form.
   */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? durationFromProtoJson(value) : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      value instanceof Temporal.Duration ? durationToProtoJson(value) : value,
    { toPlainOnly: true },
  )
  clockSkew?: Temporal.Duration;
  @Expose({ name: "additionalOrigins" })
  additionalOrigins?: Array<string>;
  @Expose({ name: "allowedOrigins" })
  allowedOrigins?: Array<string>;
  @Expose({ name: "skipNativeAppSuccessPage" })
  skipNativeAppSuccessPage?: boolean;
  @Expose({ name: "backChannelLogoutUri" })
  backChannelLogoutUri?: string;
  @Expose({ name: "loginVersion" })
  @Type(() => BetaAppServiceLoginVersion)
  loginVersion?: BetaAppServiceLoginVersion;

  constructor(data?: Partial<BetaAppServiceOIDCConfig>) {
    Object.assign(this, data);
    if (
      this.redirectUris != null &&
      !Array.isArray(this.redirectUris) &&
      !((this.redirectUris as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `redirectUris must be an array, got ${typeof this.redirectUris}`,
      );
    }
    if (this.clientId != null && typeof this.clientId !== "string") {
      throw new TypeError(
        `clientId must be a string, got ${typeof this.clientId}`,
      );
    }
    if (
      this.postLogoutRedirectUris != null &&
      !Array.isArray(this.postLogoutRedirectUris) &&
      !((this.postLogoutRedirectUris as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `postLogoutRedirectUris must be an array, got ${typeof this.postLogoutRedirectUris}`,
      );
    }
    if (this.noneCompliant != null && typeof this.noneCompliant !== "boolean") {
      throw new TypeError(
        `noneCompliant must be a boolean, got ${typeof this.noneCompliant}`,
      );
    }
    if (this.devMode != null && typeof this.devMode !== "boolean") {
      throw new TypeError(
        `devMode must be a boolean, got ${typeof this.devMode}`,
      );
    }
    if (
      this.accessTokenRoleAssertion != null &&
      typeof this.accessTokenRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `accessTokenRoleAssertion must be a boolean, got ${typeof this.accessTokenRoleAssertion}`,
      );
    }
    if (
      this.idTokenRoleAssertion != null &&
      typeof this.idTokenRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `idTokenRoleAssertion must be a boolean, got ${typeof this.idTokenRoleAssertion}`,
      );
    }
    if (
      this.idTokenUserinfoAssertion != null &&
      typeof this.idTokenUserinfoAssertion !== "boolean"
    ) {
      throw new TypeError(
        `idTokenUserinfoAssertion must be a boolean, got ${typeof this.idTokenUserinfoAssertion}`,
      );
    }
    /**
     * 4.8 — `format: duration` (google.protobuf.Duration). Accept a
     * protobuf-JSON string ("3600s", parsed via durationFromProtoJson,
     * which throws on bad input) or an already-constructed Duration
     * instance.
     */
    if (this.clockSkew != null) {
      if (typeof this.clockSkew === "string") {
        this.clockSkew = durationFromProtoJson(
          this.clockSkew as unknown as string,
        ) as unknown as Temporal.Duration;
      } else if (!(this.clockSkew instanceof Temporal.Duration)) {
        throw new TypeError(
          `clockSkew must be a Temporal.Duration or protobuf-JSON duration string, got ${typeof this.clockSkew}`,
        );
      }
    }
    if (
      this.additionalOrigins != null &&
      !Array.isArray(this.additionalOrigins) &&
      !((this.additionalOrigins as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `additionalOrigins must be an array, got ${typeof this.additionalOrigins}`,
      );
    }
    if (
      this.allowedOrigins != null &&
      !Array.isArray(this.allowedOrigins) &&
      !((this.allowedOrigins as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `allowedOrigins must be an array, got ${typeof this.allowedOrigins}`,
      );
    }
    if (
      this.skipNativeAppSuccessPage != null &&
      typeof this.skipNativeAppSuccessPage !== "boolean"
    ) {
      throw new TypeError(
        `skipNativeAppSuccessPage must be a boolean, got ${typeof this.skipNativeAppSuccessPage}`,
      );
    }
    if (
      this.backChannelLogoutUri != null &&
      typeof this.backChannelLogoutUri !== "string"
    ) {
      throw new TypeError(
        `backChannelLogoutUri must be a string, got ${typeof this.backChannelLogoutUri}`,
      );
    }
    if (this.responseTypes != null) {
      const responseTypesValues = Object.values(
        BetaAppServiceOIDCResponseType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCResponseType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (
        !(responseTypesValues as readonly unknown[]).includes(
          this.responseTypes,
        )
      ) {
        throw new Error(
          `Unknown enum value for responseTypes: ${JSON.stringify(this.responseTypes)}. ` +
            `Expected one of [${responseTypesValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.responseTypes != null && Array.isArray(this.responseTypes)) {
      const responseTypesValues = Object.values(
        BetaAppServiceOIDCResponseType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCResponseType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      for (const __v of this.responseTypes as readonly unknown[]) {
        if (!(responseTypesValues as readonly unknown[]).includes(__v)) {
          throw new Error(
            `Unknown enum value for responseTypes: ${JSON.stringify(__v)}. ` +
              `Expected one of [${responseTypesValues.map((v) => JSON.stringify(v)).join(", ")}].`,
          );
        }
      }
    }
    if (this.grantTypes != null) {
      const grantTypesValues = Object.values(
        BetaAppServiceOIDCGrantType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCGrantType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (!(grantTypesValues as readonly unknown[]).includes(this.grantTypes)) {
        throw new Error(
          `Unknown enum value for grantTypes: ${JSON.stringify(this.grantTypes)}. ` +
            `Expected one of [${grantTypesValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.grantTypes != null && Array.isArray(this.grantTypes)) {
      const grantTypesValues = Object.values(
        BetaAppServiceOIDCGrantType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCGrantType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      for (const __v of this.grantTypes as readonly unknown[]) {
        if (!(grantTypesValues as readonly unknown[]).includes(__v)) {
          throw new Error(
            `Unknown enum value for grantTypes: ${JSON.stringify(__v)}. ` +
              `Expected one of [${grantTypesValues.map((v) => JSON.stringify(v)).join(", ")}].`,
          );
        }
      }
    }
    if (this.appType != null) {
      const appTypeValues = Object.values(BetaAppServiceOIDCAppType).filter(
        (v) =>
          typeof (BetaAppServiceOIDCAppType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (!(appTypeValues as readonly unknown[]).includes(this.appType)) {
        throw new Error(
          `Unknown enum value for appType: ${JSON.stringify(this.appType)}. ` +
            `Expected one of [${appTypeValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.authMethodType != null) {
      const authMethodTypeValues = Object.values(
        BetaAppServiceOIDCAuthMethodType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCAuthMethodType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (
        !(authMethodTypeValues as readonly unknown[]).includes(
          this.authMethodType,
        )
      ) {
        throw new Error(
          `Unknown enum value for authMethodType: ${JSON.stringify(this.authMethodType)}. ` +
            `Expected one of [${authMethodTypeValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.version != null) {
      const versionValues = Object.values(BetaAppServiceOIDCVersion).filter(
        (v) =>
          typeof (BetaAppServiceOIDCVersion as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (!(versionValues as readonly unknown[]).includes(this.version)) {
        throw new Error(
          `Unknown enum value for version: ${JSON.stringify(this.version)}. ` +
            `Expected one of [${versionValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.accessTokenType != null) {
      const accessTokenTypeValues = Object.values(
        BetaAppServiceOIDCTokenType,
      ).filter(
        (v) =>
          typeof (BetaAppServiceOIDCTokenType as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (
        !(accessTokenTypeValues as readonly unknown[]).includes(
          this.accessTokenType,
        )
      ) {
        throw new Error(
          `Unknown enum value for accessTokenType: ${JSON.stringify(this.accessTokenType)}. ` +
            `Expected one of [${accessTokenTypeValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaAppServiceOIDCConfig)) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.redirectUris,
        (other as BetaAppServiceOIDCConfig).redirectUris,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.responseTypes,
        (other as BetaAppServiceOIDCConfig).responseTypes,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.grantTypes,
        (other as BetaAppServiceOIDCConfig).grantTypes,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.appType,
        (other as BetaAppServiceOIDCConfig).appType,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.clientId,
        (other as BetaAppServiceOIDCConfig).clientId,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.authMethodType,
        (other as BetaAppServiceOIDCConfig).authMethodType,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.postLogoutRedirectUris,
        (other as BetaAppServiceOIDCConfig).postLogoutRedirectUris,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.version,
        (other as BetaAppServiceOIDCConfig).version,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.noneCompliant,
        (other as BetaAppServiceOIDCConfig).noneCompliant,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.complianceProblems,
        (other as BetaAppServiceOIDCConfig).complianceProblems,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.devMode,
        (other as BetaAppServiceOIDCConfig).devMode,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.accessTokenType,
        (other as BetaAppServiceOIDCConfig).accessTokenType,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.accessTokenRoleAssertion,
        (other as BetaAppServiceOIDCConfig).accessTokenRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.idTokenRoleAssertion,
        (other as BetaAppServiceOIDCConfig).idTokenRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.idTokenUserinfoAssertion,
        (other as BetaAppServiceOIDCConfig).idTokenUserinfoAssertion,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.clockSkew,
        (other as BetaAppServiceOIDCConfig).clockSkew,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.additionalOrigins,
        (other as BetaAppServiceOIDCConfig).additionalOrigins,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.allowedOrigins,
        (other as BetaAppServiceOIDCConfig).allowedOrigins,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.skipNativeAppSuccessPage,
        (other as BetaAppServiceOIDCConfig).skipNativeAppSuccessPage,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.backChannelLogoutUri,
        (other as BetaAppServiceOIDCConfig).backChannelLogoutUri,
      )
    ) {
      return false;
    }
    if (
      !BetaAppServiceOIDCConfig.__deepEquals(
        this.loginVersion,
        (other as BetaAppServiceOIDCConfig).loginVersion,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaAppServiceOIDCConfig.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaAppServiceOIDCConfig.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaAppServiceOIDCConfig.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaAppServiceOIDCConfig.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
