// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { ProjectServicePrivateLabelingSetting } from "./project-service-private-labeling-setting.js";
import { Expose, Type } from "class-transformer";

export class ProjectServiceCreateProjectRequest {
  /**
   * OrganizationID is the unique identifier of the organization the project belongs to.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * ProjectID is the unique identifier of the new project. This field is optional.  If omitted, the system will generate a unique ID for you. This is the  recommended way. The generated ID will be returned in the response.
   * @example null
   */
  @Expose({ name: "projectId" })
  projectId?: string;
  /**
   * Name of the project. This might be presented to users, e.g. in sign-in flows.
   * @example null
   */
  @Expose({ name: "name" })
  name?: string;
  /**
   * ProjectRoleAssertion is a  setting that can be enabled to have role information  included in the user info endpoint.  It is also dependent on your application settings to include it in tokens and other types.
   * @example null
   */
  @Expose({ name: "projectRoleAssertion" })
  projectRoleAssertion?: boolean;
  /**
   * AuthorizationRequired is a boolean flag that can be enabled to check if a user has  an authorization to use this project assigned when login into an application of this project.
   * @example null
   */
  @Expose({ name: "authorizationRequired" })
  authorizationRequired?: boolean;
  /**
   * ProjectAccessRequired is a boolean flag that can be enabled to check if the organization  of the user, that is trying to log in,  has access to this project (either owns the project or is granted).
   * @example null
   */
  @Expose({ name: "projectAccessRequired" })
  projectAccessRequired?: boolean;
  /** @example null */
  @Expose({ name: "privateLabelingSetting" })
  privateLabelingSetting?: ProjectServicePrivateLabelingSetting;

  constructor(data?: Partial<ProjectServiceCreateProjectRequest>) {
    Object.assign(this, data);
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.projectId != null && typeof this.projectId !== "string") {
      throw new TypeError(
        `projectId must be a string, got ${typeof this.projectId}`,
      );
    }
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
    if (
      this.projectRoleAssertion != null &&
      typeof this.projectRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `projectRoleAssertion must be a boolean, got ${typeof this.projectRoleAssertion}`,
      );
    }
    if (
      this.authorizationRequired != null &&
      typeof this.authorizationRequired !== "boolean"
    ) {
      throw new TypeError(
        `authorizationRequired must be a boolean, got ${typeof this.authorizationRequired}`,
      );
    }
    if (
      this.projectAccessRequired != null &&
      typeof this.projectAccessRequired !== "boolean"
    ) {
      throw new TypeError(
        `projectAccessRequired must be a boolean, got ${typeof this.projectAccessRequired}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof ProjectServiceCreateProjectRequest)) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.organizationId,
        (other as ProjectServiceCreateProjectRequest).organizationId,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.projectId,
        (other as ProjectServiceCreateProjectRequest).projectId,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.name,
        (other as ProjectServiceCreateProjectRequest).name,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.projectRoleAssertion,
        (other as ProjectServiceCreateProjectRequest).projectRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.authorizationRequired,
        (other as ProjectServiceCreateProjectRequest).authorizationRequired,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.projectAccessRequired,
        (other as ProjectServiceCreateProjectRequest).projectAccessRequired,
      )
    ) {
      return false;
    }
    if (
      !ProjectServiceCreateProjectRequest.__deepEquals(
        this.privateLabelingSetting,
        (other as ProjectServiceCreateProjectRequest).privateLabelingSetting,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        ProjectServiceCreateProjectRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          ProjectServiceCreateProjectRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !ProjectServiceCreateProjectRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          ProjectServiceCreateProjectRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
