// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaOrganizationServiceHashedPassword } from "./beta-organization-service-hashed-password.js";
import { BetaOrganizationServiceIDPLink } from "./beta-organization-service-idp-link.js";
import { BetaOrganizationServiceOrganization } from "./beta-organization-service-organization.js";
import { BetaOrganizationServicePassword } from "./beta-organization-service-password.js";
import { BetaOrganizationServiceSetHumanEmail } from "./beta-organization-service-set-human-email.js";
import { BetaOrganizationServiceSetHumanPhone } from "./beta-organization-service-set-human-phone.js";
import { BetaOrganizationServiceSetHumanProfile } from "./beta-organization-service-set-human-profile.js";
import { BetaOrganizationServiceSetMetadataEntry } from "./beta-organization-service-set-metadata-entry.js";
import { Expose, Type } from "class-transformer";
import { Email } from "../brand.js";

export class BetaOrganizationServiceAddHumanUserRequest {
  /**
   * optionally set your own id unique for the user.
   * @example null
   */
  @Expose({ name: "userId" })
  userId?: string;
  /**
   * optionally set a unique username, if none is provided the email will be used.
   * @example null
   */
  @Expose({ name: "username" })
  username?: string;
  /** @example null */
  @Expose({ name: "organization" })
  @Type(() => BetaOrganizationServiceOrganization)
  organization?: BetaOrganizationServiceOrganization;
  /** @example null */
  @Expose({ name: "profile" })
  @Type(() => BetaOrganizationServiceSetHumanProfile)
  profile?: BetaOrganizationServiceSetHumanProfile;
  /** @example null */
  @Expose({ name: "email" })
  @Type(() => BetaOrganizationServiceSetHumanEmail)
  email?: BetaOrganizationServiceSetHumanEmail;
  /** @example null */
  @Expose({ name: "phone" })
  @Type(() => BetaOrganizationServiceSetHumanPhone)
  phone?: BetaOrganizationServiceSetHumanPhone;
  /** @example null */
  @Expose({ name: "metadata" })
  @Type(() => BetaOrganizationServiceSetMetadataEntry)
  metadata?: Array<BetaOrganizationServiceSetMetadataEntry>;
  /** @example null */
  @Expose({ name: "idpLinks" })
  @Type(() => BetaOrganizationServiceIDPLink)
  idpLinks?: Array<BetaOrganizationServiceIDPLink>;
  /**
   * An Implementation of RFC 6238 is used, with HMAC-SHA-1 and time-step of 30 seconds.  Currently no other options are supported, and if anything different is used the validation will fail.
   * @example null
   */
  @Expose({ name: "totpSecret" })
  totpSecret?: string;
  /** @example null */
  @Expose({ name: "hashedPassword" })
  @Type(() => BetaOrganizationServiceHashedPassword)
  hashedPassword?: BetaOrganizationServiceHashedPassword;
  /** @example null */
  @Expose({ name: "password" })
  @Type(() => BetaOrganizationServicePassword)
  password?: BetaOrganizationServicePassword;

  constructor(data?: Partial<BetaOrganizationServiceAddHumanUserRequest>) {
    Object.assign(this, data);
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.username != null && typeof this.username !== "string") {
      throw new TypeError(
        `username must be a string, got ${typeof this.username}`,
      );
    }
    if (this.totpSecret != null && typeof this.totpSecret !== "string") {
      throw new TypeError(
        `totpSecret must be a string, got ${typeof this.totpSecret}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaOrganizationServiceAddHumanUserRequest)) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.userId,
        (other as BetaOrganizationServiceAddHumanUserRequest).userId,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.username,
        (other as BetaOrganizationServiceAddHumanUserRequest).username,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.organization,
        (other as BetaOrganizationServiceAddHumanUserRequest).organization,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.profile,
        (other as BetaOrganizationServiceAddHumanUserRequest).profile,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.email,
        (other as BetaOrganizationServiceAddHumanUserRequest).email,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.phone,
        (other as BetaOrganizationServiceAddHumanUserRequest).phone,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.metadata,
        (other as BetaOrganizationServiceAddHumanUserRequest).metadata,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.idpLinks,
        (other as BetaOrganizationServiceAddHumanUserRequest).idpLinks,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.totpSecret,
        (other as BetaOrganizationServiceAddHumanUserRequest).totpSecret,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.hashedPassword,
        (other as BetaOrganizationServiceAddHumanUserRequest).hashedPassword,
      )
    ) {
      return false;
    }
    if (
      !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(
        this.password,
        (other as BetaOrganizationServiceAddHumanUserRequest).password,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaOrganizationServiceAddHumanUserRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaOrganizationServiceAddHumanUserRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaOrganizationServiceAddHumanUserRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaOrganizationServiceAddHumanUserRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
