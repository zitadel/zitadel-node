// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaUserServiceDetails } from "./beta-user-service-details.js";
import { BetaUserServiceHumanUser } from "./beta-user-service-human-user.js";
import { BetaUserServiceMachineUser } from "./beta-user-service-machine-user.js";
import { BetaUserServiceUserState } from "./beta-user-service-user-state.js";
import { Expose, Type } from "class-transformer";

export class BetaUserServiceUser {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"amount":"12.345"` (a JSON string) instead of the spec-required
   * `"amount":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "userId" })
  userId?: string;
  @Expose({ name: "details" })
  @Type(() => BetaUserServiceDetails)
  details?: BetaUserServiceDetails;
  @Expose({ name: "state" })
  state?: BetaUserServiceUserState;
  @Expose({ name: "username" })
  username?: string;
  @Expose({ name: "loginNames" })
  loginNames?: Array<string>;
  @Expose({ name: "preferredLoginName" })
  preferredLoginName?: string;
  @Expose({ name: "human" })
  @Type(() => BetaUserServiceHumanUser)
  human?: BetaUserServiceHumanUser;
  @Expose({ name: "machine" })
  @Type(() => BetaUserServiceMachineUser)
  machine?: BetaUserServiceMachineUser;

  constructor(data?: Partial<BetaUserServiceUser>) {
    Object.assign(this, data);
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.username != null && typeof this.username !== "string") {
      throw new TypeError(
        `username must be a string, got ${typeof this.username}`,
      );
    }
    if (
      this.loginNames != null &&
      !Array.isArray(this.loginNames) &&
      !((this.loginNames as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `loginNames must be an array, got ${typeof this.loginNames}`,
      );
    }
    if (
      this.preferredLoginName != null &&
      typeof this.preferredLoginName !== "string"
    ) {
      throw new TypeError(
        `preferredLoginName must be a string, got ${typeof this.preferredLoginName}`,
      );
    }
    if (this.state != null) {
      const stateValues = Object.values(BetaUserServiceUserState).filter(
        (v) =>
          typeof (BetaUserServiceUserState as Record<string, unknown>)[
            v as string
          ] !== "number",
      );
      if (!(stateValues as readonly unknown[]).includes(this.state)) {
        throw new Error(
          `Unknown enum value for state: ${JSON.stringify(this.state)}. ` +
            `Expected one of [${stateValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaUserServiceUser)) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.userId,
        (other as BetaUserServiceUser).userId,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.details,
        (other as BetaUserServiceUser).details,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.state,
        (other as BetaUserServiceUser).state,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.username,
        (other as BetaUserServiceUser).username,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.loginNames,
        (other as BetaUserServiceUser).loginNames,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.preferredLoginName,
        (other as BetaUserServiceUser).preferredLoginName,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.human,
        (other as BetaUserServiceUser).human,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceUser.__deepEquals(
        this.machine,
        (other as BetaUserServiceUser).machine,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaUserServiceUser.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaUserServiceUser.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaUserServiceUser.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaUserServiceUser.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
