/* tslint:disable */
/* eslint-disable */
/**
 * Zitadel SDK
 * The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { BetaActionServiceRESTWebhook } from './beta-action-service-rest-webhook.js';
import {
    BetaActionServiceRESTWebhookFromJSON,
    BetaActionServiceRESTWebhookFromJSONTyped,
    BetaActionServiceRESTWebhookToJSON,
    BetaActionServiceRESTWebhookToJSONTyped,
} from './beta-action-service-rest-webhook.js';
import type { BetaActionServiceRESTCall } from './beta-action-service-rest-call.js';
import {
    BetaActionServiceRESTCallFromJSON,
    BetaActionServiceRESTCallFromJSONTyped,
    BetaActionServiceRESTCallToJSON,
    BetaActionServiceRESTCallToJSONTyped,
} from './beta-action-service-rest-call.js';

/**
 * 
 * @export
 * @interface BetaActionServiceUpdateTargetRequest
 */
export interface BetaActionServiceUpdateTargetRequest {
    /**
     * 
     * @type {string}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    id?: string;
    /**
     * 
     * @type {string}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    name?: string | null;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    timeout?: string;
    /**
     * 
     * @type {string}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    endpoint?: string | null;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    expirationSigningKey?: string;
    /**
     * 
     * @type {object}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    restAsync?: object;
    /**
     * 
     * @type {BetaActionServiceRESTCall}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    restCall?: BetaActionServiceRESTCall;
    /**
     * 
     * @type {BetaActionServiceRESTWebhook}
     * @memberof BetaActionServiceUpdateTargetRequest
     */
    restWebhook?: BetaActionServiceRESTWebhook;
}

/**
 * Check if a given object implements the BetaActionServiceUpdateTargetRequest interface.
 */
export function instanceOfBetaActionServiceUpdateTargetRequest(value: object): value is BetaActionServiceUpdateTargetRequest {
    return true;
}

export function BetaActionServiceUpdateTargetRequestFromJSON(json: any): BetaActionServiceUpdateTargetRequest {
    return BetaActionServiceUpdateTargetRequestFromJSONTyped(json, false);
}

export function BetaActionServiceUpdateTargetRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): BetaActionServiceUpdateTargetRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'] == null ? undefined : json['id'],
        'name': json['name'] == null ? undefined : json['name'],
        'timeout': json['timeout'] == null ? undefined : json['timeout'],
        'endpoint': json['endpoint'] == null ? undefined : json['endpoint'],
        'expirationSigningKey': json['expirationSigningKey'] == null ? undefined : json['expirationSigningKey'],
        'restAsync': json['restAsync'] == null ? undefined : json['restAsync'],
        'restCall': json['restCall'] == null ? undefined : BetaActionServiceRESTCallFromJSON(json['restCall']),
        'restWebhook': json['restWebhook'] == null ? undefined : BetaActionServiceRESTWebhookFromJSON(json['restWebhook']),
    };
}

export function BetaActionServiceUpdateTargetRequestToJSON(json: any): BetaActionServiceUpdateTargetRequest {
    return BetaActionServiceUpdateTargetRequestToJSONTyped(json, false);
}

export function BetaActionServiceUpdateTargetRequestToJSONTyped(value?: BetaActionServiceUpdateTargetRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'name': value['name'],
        'timeout': value['timeout'],
        'endpoint': value['endpoint'],
        'expirationSigningKey': value['expirationSigningKey'],
        'restAsync': value['restAsync'],
        'restCall': BetaActionServiceRESTCallToJSON(value['restCall']),
        'restWebhook': BetaActionServiceRESTWebhookToJSON(value['restWebhook']),
    };
}

