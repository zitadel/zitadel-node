// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { OrganizationServiceAdmin } from "./organization-service-admin.js";
import { Expose, Type } from "class-transformer";

export class OrganizationServiceAddOrganizationRequest {
  /**
   * Name is the unique name of the organization to be created.  This must be unique across the instance.
   * @example null
   */
  @Expose({ name: "name" })
  name?: string;
  /**
   * Specify users to be assigned as organization admins.  If no users are specified here, the organization will be created without any admin users.  The organization can still be managed by any instance administrator.  If no roles are specified for a user, they will be assigned the role ORG_OWNER.
   * @example null
   */
  @Expose({ name: "admins" })
  @Type(() => OrganizationServiceAdmin)
  admins?: Array<OrganizationServiceAdmin>;
  /**
   * OrganizationID is the unique identifier of the organization. This field is optional.  If omitted, the system will generate one,  which is the recommended way. The generated ID will be returned in the response.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * Optionally, set a unique id for the organization. If omitted, the system will generate one,  which is the recommended way. The generated ID will be returned in the response.   Deprecated: use 'organization_id' field instead.  If both org_id and organization_id are set, organization_id will take precedence.
   * @example null
   * @deprecated This property is deprecated.
   */
  @Expose({ name: "orgId" })
  orgId?: string;

  constructor(data?: Partial<OrganizationServiceAddOrganizationRequest>) {
    Object.assign(this, data);
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.orgId != null && typeof this.orgId !== "string") {
      throw new TypeError(`orgId must be a string, got ${typeof this.orgId}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof OrganizationServiceAddOrganizationRequest)) {
      return false;
    }
    if (
      !OrganizationServiceAddOrganizationRequest.__deepEquals(
        this.name,
        (other as OrganizationServiceAddOrganizationRequest).name,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddOrganizationRequest.__deepEquals(
        this.admins,
        (other as OrganizationServiceAddOrganizationRequest).admins,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddOrganizationRequest.__deepEquals(
        this.organizationId,
        (other as OrganizationServiceAddOrganizationRequest).organizationId,
      )
    ) {
      return false;
    }
    if (
      !OrganizationServiceAddOrganizationRequest.__deepEquals(
        this.orgId,
        (other as OrganizationServiceAddOrganizationRequest).orgId,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        OrganizationServiceAddOrganizationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          OrganizationServiceAddOrganizationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !OrganizationServiceAddOrganizationRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          OrganizationServiceAddOrganizationRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
