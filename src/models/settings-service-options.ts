// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SettingsServiceAutoLinkingOption } from "./settings-service-auto-linking-option.js";
import { Expose, Type } from "class-transformer";

export class SettingsServiceOptions {
  /**
   * Enable if users should be able to link an existing ZITADEL user with an  external account.
   * @example null
   */
  @Expose({ name: "isLinkingAllowed" })
  isLinkingAllowed?: boolean;
  /**
   * Enable if users should be able to create a new account in ZITADEL when  using an external account.
   * @example null
   */
  @Expose({ name: "isCreationAllowed" })
  isCreationAllowed?: boolean;
  /**
   * Enable if a new account in ZITADEL should be created automatically when  login with an external account.
   * @example null
   */
  @Expose({ name: "isAutoCreation" })
  isAutoCreation?: boolean;
  /**
   * Enable if a the ZITADEL account fields should be updated automatically on  each login.
   * @example null
   */
  @Expose({ name: "isAutoUpdate" })
  isAutoUpdate?: boolean;
  /** @example null */
  @Expose({ name: "autoLinking" })
  autoLinking?: SettingsServiceAutoLinkingOption;

  constructor(data?: Partial<SettingsServiceOptions>) {
    Object.assign(this, data);
    if (
      this.isLinkingAllowed != null &&
      typeof this.isLinkingAllowed !== "boolean"
    ) {
      throw new TypeError(
        `isLinkingAllowed must be a boolean, got ${typeof this.isLinkingAllowed}`,
      );
    }
    if (
      this.isCreationAllowed != null &&
      typeof this.isCreationAllowed !== "boolean"
    ) {
      throw new TypeError(
        `isCreationAllowed must be a boolean, got ${typeof this.isCreationAllowed}`,
      );
    }
    if (
      this.isAutoCreation != null &&
      typeof this.isAutoCreation !== "boolean"
    ) {
      throw new TypeError(
        `isAutoCreation must be a boolean, got ${typeof this.isAutoCreation}`,
      );
    }
    if (this.isAutoUpdate != null && typeof this.isAutoUpdate !== "boolean") {
      throw new TypeError(
        `isAutoUpdate must be a boolean, got ${typeof this.isAutoUpdate}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SettingsServiceOptions)) {
      return false;
    }
    if (
      !SettingsServiceOptions.__deepEquals(
        this.isLinkingAllowed,
        (other as SettingsServiceOptions).isLinkingAllowed,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceOptions.__deepEquals(
        this.isCreationAllowed,
        (other as SettingsServiceOptions).isCreationAllowed,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceOptions.__deepEquals(
        this.isAutoCreation,
        (other as SettingsServiceOptions).isAutoCreation,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceOptions.__deepEquals(
        this.isAutoUpdate,
        (other as SettingsServiceOptions).isAutoUpdate,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceOptions.__deepEquals(
        this.autoLinking,
        (other as SettingsServiceOptions).autoLinking,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => SettingsServiceOptions.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SettingsServiceOptions.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !SettingsServiceOptions.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SettingsServiceOptions.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
