// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceCreationDateQuery } from "./session-service-creation-date-query.js";
import { SessionServiceCreatorQuery } from "./session-service-creator-query.js";
import { SessionServiceExpirationDateQuery } from "./session-service-expiration-date-query.js";
import { SessionServiceIDsQuery } from "./session-service-ids-query.js";
import { SessionServiceUserAgentQuery } from "./session-service-user-agent-query.js";
import { SessionServiceUserIDQuery } from "./session-service-user-id-query.js";
import { Expose, Type } from "class-transformer";

export class SessionServiceSearchQuery {
  /** @example null */
  @Expose({ name: "creationDateQuery" })
  @Type(() => SessionServiceCreationDateQuery)
  creationDateQuery?: SessionServiceCreationDateQuery;
  /** @example null */
  @Expose({ name: "creatorQuery" })
  @Type(() => SessionServiceCreatorQuery)
  creatorQuery?: SessionServiceCreatorQuery;
  /** @example null */
  @Expose({ name: "expirationDateQuery" })
  @Type(() => SessionServiceExpirationDateQuery)
  expirationDateQuery?: SessionServiceExpirationDateQuery;
  /** @example null */
  @Expose({ name: "idsQuery" })
  @Type(() => SessionServiceIDsQuery)
  idsQuery?: SessionServiceIDsQuery;
  /** @example null */
  @Expose({ name: "userAgentQuery" })
  @Type(() => SessionServiceUserAgentQuery)
  userAgentQuery?: SessionServiceUserAgentQuery;
  /** @example null */
  @Expose({ name: "userIdQuery" })
  @Type(() => SessionServiceUserIDQuery)
  userIdQuery?: SessionServiceUserIDQuery;

  constructor(data?: Partial<SessionServiceSearchQuery>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceSearchQuery)) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.creationDateQuery,
        (other as SessionServiceSearchQuery).creationDateQuery,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.creatorQuery,
        (other as SessionServiceSearchQuery).creatorQuery,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.expirationDateQuery,
        (other as SessionServiceSearchQuery).expirationDateQuery,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.idsQuery,
        (other as SessionServiceSearchQuery).idsQuery,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.userAgentQuery,
        (other as SessionServiceSearchQuery).userAgentQuery,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSearchQuery.__deepEquals(
        this.userIdQuery,
        (other as SessionServiceSearchQuery).userIdQuery,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => SessionServiceSearchQuery.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceSearchQuery.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !SessionServiceSearchQuery.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceSearchQuery.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
