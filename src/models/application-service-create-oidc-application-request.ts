// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { ApplicationServiceLoginVersion } from "./application-service-login-version.js";
import { ApplicationServiceOIDCApplicationType } from "./application-service-oidc-application-type.js";
import { ApplicationServiceOIDCAuthMethodType } from "./application-service-oidc-auth-method-type.js";
import { ApplicationServiceOIDCGrantType } from "./application-service-oidc-grant-type.js";
import { ApplicationServiceOIDCResponseType } from "./application-service-oidc-response-type.js";
import { ApplicationServiceOIDCTokenType } from "./application-service-oidc-token-type.js";
import { ApplicationServiceOIDCVersion } from "./application-service-oidc-version.js";
import { Expose, Type, Transform } from "class-transformer";
import { Temporal } from "temporal-polyfill";
import {
  durationFromProtoJson,
  durationToProtoJson,
} from "../object-serializer.js";

export class ApplicationServiceCreateOIDCApplicationRequest {
  /**
   * RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows,  where the authorization code or tokens will be sent to.  The redirect_uri parameter in the authorization request must exactly match one of these URIs.
   * @example null
   */
  @Expose({ name: "redirectUris" })
  redirectUris?: Array<string>;
  /**
   * ResponseTypes define whether a code, id_token token or just id_token will be returned.  The response_type parameter in the authorization request must exactly match one of these values.
   * @example null
   */
  @Expose({ name: "responseTypes" })
  responseTypes?: Array<ApplicationServiceOIDCResponseType>;
  /**
   * GrantTypes define the flow type the application is allowed to use.  The grant_type parameter in the token request must exactly match one of these values.  Minimum one grant type must be provided, but multiple grant types can be provided to allow  different flows, e.g. authorization code flow and refresh token flow.
   * @example null
   */
  @Expose({ name: "grantTypes" })
  grantTypes?: Array<ApplicationServiceOIDCGrantType>;
  /** @example null */
  @Expose({ name: "applicationType" })
  applicationType?: ApplicationServiceOIDCApplicationType;
  /** @example null */
  @Expose({ name: "authMethodType" })
  authMethodType?: ApplicationServiceOIDCAuthMethodType;
  /**
   * PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout.  The post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.
   * @example null
   */
  @Expose({ name: "postLogoutRedirectUris" })
  postLogoutRedirectUris?: Array<string>;
  /** @example null */
  @Expose({ name: "version" })
  version?: ApplicationServiceOIDCVersion;
  /**
   * DevelopmentMode can be enabled for development purposes. This allows the use of  OIDC non-compliant and potentially insecure settings, such as the use of  HTTP redirect URIs or wildcard redirect URIs.
   * @example null
   */
  @Expose({ name: "developmentMode" })
  developmentMode?: boolean;
  /** @example null */
  @Expose({ name: "accessTokenType" })
  accessTokenType?: ApplicationServiceOIDCTokenType;
  /**
   * If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.  Ensure that the access token is a JWT token and not a bearer token. And either request the roles  by scope or enable the user role assertion on the project.
   * @example null
   */
  @Expose({ name: "accessTokenRoleAssertion" })
  accessTokenRoleAssertion?: boolean;
  /**
   * If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token.  Ensure that either the roles are requested by scope or enable the user role assertion on the  project.
   * @example null
   */
  @Expose({ name: "idTokenRoleAssertion" })
  idTokenRoleAssertion?: boolean;
  /**
   * If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes  are added to the id token even if an access token is issued. This can be required by some applications  that do not call the userinfo endpoint after authentication or directly use the id_token for retrieving  user information.  Attention: this violates the OIDC specification, which states that these claims must only be  requested from the userinfo endpoint if an access token is issued. This is to prevent  leaking of personal information in the id token, which is often stored in the browser and  therefore more vulnerable.
   * @example null
   */
  @Expose({ name: "idTokenUserinfoAssertion" })
  idTokenUserinfoAssertion?: boolean;
  /**
   * A Duration represents a signed, fixed-length span of time represented  as a count of seconds and fractions of seconds at nanosecond  resolution. It is independent of any calendar and concepts like \"day\"  or \"month\". It is related to Timestamp in that the difference between  two Timestamp values is a Duration and it can be added or subtracted  from a Timestamp. Range is approximately +-10,000 years.   # Examples   Example 1: Compute Duration from two Timestamps in pseudo code.       Timestamp start = ...;      Timestamp end = ...;      Duration duration = ...;       duration.seconds = end.seconds - start.seconds;      duration.nanos = end.nanos - start.nanos;       if (duration.seconds < 0 && duration.nanos > 0) {        duration.seconds += 1;        duration.nanos -= 1000000000;      } else if (duration.seconds > 0 && duration.nanos < 0) {        duration.seconds -= 1;        duration.nanos += 1000000000;      }   Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.       Timestamp start = ...;      Duration duration = ...;      Timestamp end = ...;       end.seconds = start.seconds + duration.seconds;      end.nanos = start.nanos + duration.nanos;       if (end.nanos < 0) {        end.seconds -= 1;        end.nanos += 1000000000;      } else if (end.nanos >= 1000000000) {        end.seconds += 1;        end.nanos -= 1000000000;      }   Example 3: Compute Duration from datetime.timedelta in Python.       td = datetime.timedelta(days=3, minutes=10)      duration = Duration()      duration.FromTimedelta(td)   # JSON Mapping   In JSON format, the Duration type is encoded as a string rather than an  object, where the string ends in the suffix \"s\" (indicating seconds) and  is preceded by the number of seconds, with nanoseconds expressed as  fractional seconds. For example, 3 seconds with 0 nanoseconds should be  encoded in JSON format as \"3s\", while 3 seconds and 1 nanosecond should  be expressed in JSON format as \"3.000000001s\", and 3 seconds and 1  microsecond should be expressed in JSON format as \"3.000001s\".
   * @example null
   */
  @Expose({ name: "clockSkew" })
  /*
   * 4.8 — `format: duration` is a google.protobuf.Duration. The native
   * type stays Temporal.Duration, but the wire form is protobuf-JSON
   * ("3600s"), not ISO-8601 ("PT1H") — Zitadel rejects the ISO form.
   */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? durationFromProtoJson(value) : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      value instanceof Temporal.Duration ? durationToProtoJson(value) : value,
    { toPlainOnly: true },
  )
  clockSkew?: Temporal.Duration;
  /**
   * AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used  additional to the redirect_uris.  This is useful if the application is used from an origin different to the redirect_uris,  e.g. if the application is a SPA served in a native app, where the redirect_uri is a custom scheme,  but the application is served from a https origin.
   * @example null
   */
  @Expose({ name: "additionalOrigins" })
  additionalOrigins?: Array<string>;
  /**
   * For native apps a successful login usually shows a success page with a link to open the application again.  SkipNativeAppSuccessPage can be used to skip this page and open the application directly.
   * @example null
   */
  @Expose({ name: "skipNativeAppSuccessPage" })
  skipNativeAppSuccessPage?: boolean;
  /**
   * BackChannelLogoutURI is used to notify the application about terminated sessions according  to the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).
   * @example null
   */
  @Expose({ name: "backChannelLogoutUri" })
  backChannelLogoutUri?: string;
  /** @example null */
  @Expose({ name: "loginVersion" })
  @Type(() => ApplicationServiceLoginVersion)
  loginVersion?: ApplicationServiceLoginVersion;

  constructor(data?: Partial<ApplicationServiceCreateOIDCApplicationRequest>) {
    Object.assign(this, data);
    if (
      this.redirectUris != null &&
      !Array.isArray(this.redirectUris) &&
      !((this.redirectUris as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `redirectUris must be an array, got ${typeof this.redirectUris}`,
      );
    }
    if (
      this.postLogoutRedirectUris != null &&
      !Array.isArray(this.postLogoutRedirectUris) &&
      !((this.postLogoutRedirectUris as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `postLogoutRedirectUris must be an array, got ${typeof this.postLogoutRedirectUris}`,
      );
    }
    if (
      this.developmentMode != null &&
      typeof this.developmentMode !== "boolean"
    ) {
      throw new TypeError(
        `developmentMode must be a boolean, got ${typeof this.developmentMode}`,
      );
    }
    if (
      this.accessTokenRoleAssertion != null &&
      typeof this.accessTokenRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `accessTokenRoleAssertion must be a boolean, got ${typeof this.accessTokenRoleAssertion}`,
      );
    }
    if (
      this.idTokenRoleAssertion != null &&
      typeof this.idTokenRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `idTokenRoleAssertion must be a boolean, got ${typeof this.idTokenRoleAssertion}`,
      );
    }
    if (
      this.idTokenUserinfoAssertion != null &&
      typeof this.idTokenUserinfoAssertion !== "boolean"
    ) {
      throw new TypeError(
        `idTokenUserinfoAssertion must be a boolean, got ${typeof this.idTokenUserinfoAssertion}`,
      );
    }
    /**
     * 4.8 — `format: duration` (google.protobuf.Duration). Accept a
     * protobuf-JSON string ("3600s", parsed via durationFromProtoJson,
     * which throws on bad input) or an already-constructed Duration
     * instance.
     */
    if (this.clockSkew != null) {
      if (typeof this.clockSkew === "string") {
        this.clockSkew = durationFromProtoJson(
          this.clockSkew as unknown as string,
        ) as unknown as Temporal.Duration;
      } else if (!(this.clockSkew instanceof Temporal.Duration)) {
        throw new TypeError(
          `clockSkew must be a Temporal.Duration or protobuf-JSON duration string, got ${typeof this.clockSkew}`,
        );
      }
    }
    if (
      this.additionalOrigins != null &&
      !Array.isArray(this.additionalOrigins) &&
      !((this.additionalOrigins as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `additionalOrigins must be an array, got ${typeof this.additionalOrigins}`,
      );
    }
    if (
      this.skipNativeAppSuccessPage != null &&
      typeof this.skipNativeAppSuccessPage !== "boolean"
    ) {
      throw new TypeError(
        `skipNativeAppSuccessPage must be a boolean, got ${typeof this.skipNativeAppSuccessPage}`,
      );
    }
    if (
      this.backChannelLogoutUri != null &&
      typeof this.backChannelLogoutUri !== "string"
    ) {
      throw new TypeError(
        `backChannelLogoutUri must be a string, got ${typeof this.backChannelLogoutUri}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof ApplicationServiceCreateOIDCApplicationRequest)) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.redirectUris,
        (other as ApplicationServiceCreateOIDCApplicationRequest).redirectUris,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.responseTypes,
        (other as ApplicationServiceCreateOIDCApplicationRequest).responseTypes,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.grantTypes,
        (other as ApplicationServiceCreateOIDCApplicationRequest).grantTypes,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.applicationType,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .applicationType,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.authMethodType,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .authMethodType,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.postLogoutRedirectUris,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .postLogoutRedirectUris,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.version,
        (other as ApplicationServiceCreateOIDCApplicationRequest).version,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.developmentMode,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .developmentMode,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.accessTokenType,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .accessTokenType,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.accessTokenRoleAssertion,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .accessTokenRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.idTokenRoleAssertion,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .idTokenRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.idTokenUserinfoAssertion,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .idTokenUserinfoAssertion,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.clockSkew,
        (other as ApplicationServiceCreateOIDCApplicationRequest).clockSkew,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.additionalOrigins,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .additionalOrigins,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.skipNativeAppSuccessPage,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .skipNativeAppSuccessPage,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.backChannelLogoutUri,
        (other as ApplicationServiceCreateOIDCApplicationRequest)
          .backChannelLogoutUri,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
        this.loginVersion,
        (other as ApplicationServiceCreateOIDCApplicationRequest).loginVersion,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          ApplicationServiceCreateOIDCApplicationRequest.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
