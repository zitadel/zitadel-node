// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { AuthorizationServiceIDFilter } from "./authorization-service-id-filter.js";
import { AuthorizationServiceInIDsFilter } from "./authorization-service-in-ids-filter.js";
import { AuthorizationServiceProjectNameQuery } from "./authorization-service-project-name-query.js";
import { AuthorizationServiceRoleKeyQuery } from "./authorization-service-role-key-query.js";
import { AuthorizationServiceStateQuery } from "./authorization-service-state-query.js";
import { AuthorizationServiceUserDisplayNameQuery } from "./authorization-service-user-display-name-query.js";
import { AuthorizationServiceUserPreferredLoginNameQuery } from "./authorization-service-user-preferred-login-name-query.js";
import { Expose, Type } from "class-transformer";

export class AuthorizationServiceAuthorizationsSearchFilter {
  /** @example null */
  @Expose({ name: "authorizationIds" })
  @Type(() => AuthorizationServiceInIDsFilter)
  authorizationIds?: AuthorizationServiceInIDsFilter;
  /** @example null */
  @Expose({ name: "inUserIds" })
  @Type(() => AuthorizationServiceInIDsFilter)
  inUserIds?: AuthorizationServiceInIDsFilter;
  /** @example null */
  @Expose({ name: "organizationId" })
  @Type(() => AuthorizationServiceIDFilter)
  organizationId?: AuthorizationServiceIDFilter;
  /** @example null */
  @Expose({ name: "projectGrantId" })
  @Type(() => AuthorizationServiceIDFilter)
  projectGrantId?: AuthorizationServiceIDFilter;
  /** @example null */
  @Expose({ name: "projectId" })
  @Type(() => AuthorizationServiceIDFilter)
  projectId?: AuthorizationServiceIDFilter;
  /** @example null */
  @Expose({ name: "projectName" })
  @Type(() => AuthorizationServiceProjectNameQuery)
  projectName?: AuthorizationServiceProjectNameQuery;
  /** @example null */
  @Expose({ name: "roleKey" })
  @Type(() => AuthorizationServiceRoleKeyQuery)
  roleKey?: AuthorizationServiceRoleKeyQuery;
  /** @example null */
  @Expose({ name: "state" })
  @Type(() => AuthorizationServiceStateQuery)
  state?: AuthorizationServiceStateQuery;
  /** @example null */
  @Expose({ name: "userDisplayName" })
  @Type(() => AuthorizationServiceUserDisplayNameQuery)
  userDisplayName?: AuthorizationServiceUserDisplayNameQuery;
  /** @example null */
  @Expose({ name: "userOrganizationId" })
  @Type(() => AuthorizationServiceIDFilter)
  userOrganizationId?: AuthorizationServiceIDFilter;
  /** @example null */
  @Expose({ name: "userPreferredLoginName" })
  @Type(() => AuthorizationServiceUserPreferredLoginNameQuery)
  userPreferredLoginName?: AuthorizationServiceUserPreferredLoginNameQuery;

  constructor(data?: Partial<AuthorizationServiceAuthorizationsSearchFilter>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof AuthorizationServiceAuthorizationsSearchFilter)) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.authorizationIds,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .authorizationIds,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.inUserIds,
        (other as AuthorizationServiceAuthorizationsSearchFilter).inUserIds,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.organizationId,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .organizationId,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.projectGrantId,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .projectGrantId,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.projectId,
        (other as AuthorizationServiceAuthorizationsSearchFilter).projectId,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.projectName,
        (other as AuthorizationServiceAuthorizationsSearchFilter).projectName,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.roleKey,
        (other as AuthorizationServiceAuthorizationsSearchFilter).roleKey,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.state,
        (other as AuthorizationServiceAuthorizationsSearchFilter).state,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.userDisplayName,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .userDisplayName,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.userOrganizationId,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .userOrganizationId,
      )
    ) {
      return false;
    }
    if (
      !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
        this.userPreferredLoginName,
        (other as AuthorizationServiceAuthorizationsSearchFilter)
          .userPreferredLoginName,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          AuthorizationServiceAuthorizationsSearchFilter.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
