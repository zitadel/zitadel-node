// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class IdentityProviderServiceJWTConfig {
  /**
   * The endpoint where the JWT can be extracted.
   * @example null
   */
  @Expose({ name: "jwtEndpoint" })
  jwtEndpoint?: string;
  /**
   * The issuer of the JWT (for validation).
   * @example null
   */
  @Expose({ name: "issuer" })
  issuer?: string;
  /**
   * The endpoint to the key (JWK) which is used to sign the JWT with.
   * @example null
   */
  @Expose({ name: "keysEndpoint" })
  keysEndpoint?: string;
  /**
   * The name of the header where the JWT is sent in, default is authorization.
   * @example null
   */
  @Expose({ name: "headerName" })
  headerName?: string;

  constructor(data?: Partial<IdentityProviderServiceJWTConfig>) {
    Object.assign(this, data);
    if (this.jwtEndpoint != null && typeof this.jwtEndpoint !== "string") {
      throw new TypeError(
        `jwtEndpoint must be a string, got ${typeof this.jwtEndpoint}`,
      );
    }
    if (this.issuer != null && typeof this.issuer !== "string") {
      throw new TypeError(`issuer must be a string, got ${typeof this.issuer}`);
    }
    if (this.keysEndpoint != null && typeof this.keysEndpoint !== "string") {
      throw new TypeError(
        `keysEndpoint must be a string, got ${typeof this.keysEndpoint}`,
      );
    }
    if (this.headerName != null && typeof this.headerName !== "string") {
      throw new TypeError(
        `headerName must be a string, got ${typeof this.headerName}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof IdentityProviderServiceJWTConfig)) {
      return false;
    }
    if (
      !IdentityProviderServiceJWTConfig.__deepEquals(
        this.jwtEndpoint,
        (other as IdentityProviderServiceJWTConfig).jwtEndpoint,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceJWTConfig.__deepEquals(
        this.issuer,
        (other as IdentityProviderServiceJWTConfig).issuer,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceJWTConfig.__deepEquals(
        this.keysEndpoint,
        (other as IdentityProviderServiceJWTConfig).keysEndpoint,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceJWTConfig.__deepEquals(
        this.headerName,
        (other as IdentityProviderServiceJWTConfig).headerName,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        IdentityProviderServiceJWTConfig.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => IdentityProviderServiceJWTConfig.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !IdentityProviderServiceJWTConfig.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          IdentityProviderServiceJWTConfig.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
