// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class BetaSettingsServiceTheme {
  /**
   * hex value for primary color
   * @example null
   */
  @Expose({ name: "primaryColor" })
  primaryColor?: string;
  /**
   * hex value for background color
   * @example null
   */
  @Expose({ name: "backgroundColor" })
  backgroundColor?: string;
  /**
   * hex value for warning color
   * @example null
   */
  @Expose({ name: "warnColor" })
  warnColor?: string;
  /**
   * hex value for font color
   * @example null
   */
  @Expose({ name: "fontColor" })
  fontColor?: string;
  /**
   * url where the logo is served
   * @example null
   */
  @Expose({ name: "logoUrl" })
  logoUrl?: string;
  /**
   * url where the icon is served
   * @example null
   */
  @Expose({ name: "iconUrl" })
  iconUrl?: string;

  constructor(data?: Partial<BetaSettingsServiceTheme>) {
    Object.assign(this, data);
    if (this.primaryColor != null && typeof this.primaryColor !== "string") {
      throw new TypeError(
        `primaryColor must be a string, got ${typeof this.primaryColor}`,
      );
    }
    if (
      this.backgroundColor != null &&
      typeof this.backgroundColor !== "string"
    ) {
      throw new TypeError(
        `backgroundColor must be a string, got ${typeof this.backgroundColor}`,
      );
    }
    if (this.warnColor != null && typeof this.warnColor !== "string") {
      throw new TypeError(
        `warnColor must be a string, got ${typeof this.warnColor}`,
      );
    }
    if (this.fontColor != null && typeof this.fontColor !== "string") {
      throw new TypeError(
        `fontColor must be a string, got ${typeof this.fontColor}`,
      );
    }
    if (this.logoUrl != null && typeof this.logoUrl !== "string") {
      throw new TypeError(
        `logoUrl must be a string, got ${typeof this.logoUrl}`,
      );
    }
    if (this.iconUrl != null && typeof this.iconUrl !== "string") {
      throw new TypeError(
        `iconUrl must be a string, got ${typeof this.iconUrl}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaSettingsServiceTheme)) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.primaryColor,
        (other as BetaSettingsServiceTheme).primaryColor,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.backgroundColor,
        (other as BetaSettingsServiceTheme).backgroundColor,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.warnColor,
        (other as BetaSettingsServiceTheme).warnColor,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.fontColor,
        (other as BetaSettingsServiceTheme).fontColor,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.logoUrl,
        (other as BetaSettingsServiceTheme).logoUrl,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceTheme.__deepEquals(
        this.iconUrl,
        (other as BetaSettingsServiceTheme).iconUrl,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaSettingsServiceTheme.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaSettingsServiceTheme.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaSettingsServiceTheme.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaSettingsServiceTheme.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
