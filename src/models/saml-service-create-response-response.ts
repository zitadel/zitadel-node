// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SAMLServiceDetails } from "./saml-service-details.js";
import { SAMLServicePostResponse } from "./saml-service-post-response.js";
import { Expose, Type } from "class-transformer";

export class SAMLServiceCreateResponseResponse {
  /** @example null */
  @Expose({ name: "details" })
  @Type(() => SAMLServiceDetails)
  details?: SAMLServiceDetails;
  /**
   * URL including the Assertion Consumer Service where the user should be redirected or has to call per POST, depending on the binding. Contains details for the application to obtain the response on success, or error details on failure. Note that this field must be treated as credentials, as the contained SAMLResponse or code can be used on behalve of the user.
   * @example null
   */
  @Expose({ name: "url" })
  url?: string;
  /** @example null */
  @Expose({ name: "post" })
  @Type(() => SAMLServicePostResponse)
  post?: SAMLServicePostResponse;
  /** @example null */
  @Expose({ name: "redirect" })
  redirect?: object;

  constructor(data?: Partial<SAMLServiceCreateResponseResponse>) {
    Object.assign(this, data);
    if (this.url != null && typeof this.url !== "string") {
      throw new TypeError(`url must be a string, got ${typeof this.url}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SAMLServiceCreateResponseResponse)) {
      return false;
    }
    if (
      !SAMLServiceCreateResponseResponse.__deepEquals(
        this.details,
        (other as SAMLServiceCreateResponseResponse).details,
      )
    ) {
      return false;
    }
    if (
      !SAMLServiceCreateResponseResponse.__deepEquals(
        this.url,
        (other as SAMLServiceCreateResponseResponse).url,
      )
    ) {
      return false;
    }
    if (
      !SAMLServiceCreateResponseResponse.__deepEquals(
        this.post,
        (other as SAMLServiceCreateResponseResponse).post,
      )
    ) {
      return false;
    }
    if (
      !SAMLServiceCreateResponseResponse.__deepEquals(
        this.redirect,
        (other as SAMLServiceCreateResponseResponse).redirect,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SAMLServiceCreateResponseResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SAMLServiceCreateResponseResponse.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SAMLServiceCreateResponseResponse.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SAMLServiceCreateResponseResponse.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
