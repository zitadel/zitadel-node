// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { UserServiceDetails } from "./user-service-details.js";
import { UserServiceFormData } from "./user-service-form-data.js";
import { UserServiceIDPIntent } from "./user-service-idp-intent.js";
import { Expose, Type, Transform } from "class-transformer";

export class UserServiceStartIdentityProviderIntentResponse {
  /** @example null */
  @Expose({ name: "details" })
  @Type(() => UserServiceDetails)
  details?: UserServiceDetails;
  /** @example null */
  @Expose({ name: "authUrl" })
  authUrl?: string;
  /** @example null */
  @Expose({ name: "formData" })
  @Type(() => UserServiceFormData)
  formData?: UserServiceFormData;
  /** @example null */
  @Expose({ name: "idpIntent" })
  @Type(() => UserServiceIDPIntent)
  idpIntent?: UserServiceIDPIntent;
  /**
   * POST call information  Deprecated: Use form_data instead
   * @example null
   */
  @Expose({ name: "postForm" })
  /** 2.1 — `format: byte` round-trips Buffer <-> base64 string at the serde boundary. */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? Buffer.from(value, "base64") : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      Buffer.isBuffer(value) ? (value as Buffer).toString("base64") : value,
    { toPlainOnly: true },
  )
  postForm?: Buffer;

  constructor(data?: Partial<UserServiceStartIdentityProviderIntentResponse>) {
    Object.assign(this, data);
    if (this.authUrl != null && typeof this.authUrl !== "string") {
      throw new TypeError(
        `authUrl must be a string, got ${typeof this.authUrl}`,
      );
    }
    /**
     * 2.1 — format: byte. The wire form is base64; the model field is a
     * Buffer. Lives outside the isString block because typeMapping
     * (ByteArray to Buffer) flips isString to false at codegen time.
     */
    if (this.postForm != null && typeof this.postForm === "string") {
      this.postForm = Buffer.from(
        this.postForm as unknown as string,
        "base64",
      ) as unknown as Buffer;
    } else if (this.postForm != null && !Buffer.isBuffer(this.postForm)) {
      throw new TypeError(
        `postForm must be a Buffer or base64 string, got ${typeof this.postForm}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof UserServiceStartIdentityProviderIntentResponse)) {
      return false;
    }
    if (
      !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
        this.details,
        (other as UserServiceStartIdentityProviderIntentResponse).details,
      )
    ) {
      return false;
    }
    if (
      !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
        this.authUrl,
        (other as UserServiceStartIdentityProviderIntentResponse).authUrl,
      )
    ) {
      return false;
    }
    if (
      !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
        this.formData,
        (other as UserServiceStartIdentityProviderIntentResponse).formData,
      )
    ) {
      return false;
    }
    if (
      !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
        this.idpIntent,
        (other as UserServiceStartIdentityProviderIntentResponse).idpIntent,
      )
    ) {
      return false;
    }
    if (
      !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
        this.postForm,
        (other as UserServiceStartIdentityProviderIntentResponse).postForm,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        UserServiceStartIdentityProviderIntentResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          UserServiceStartIdentityProviderIntentResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !UserServiceStartIdentityProviderIntentResponse.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          UserServiceStartIdentityProviderIntentResponse.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
