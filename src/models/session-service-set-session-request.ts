// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceChecks } from "./session-service-checks.js";
import { SessionServiceRequestChallenges } from "./session-service-request-challenges.js";
import { Expose, Type, Transform } from "class-transformer";
import { Temporal } from "temporal-polyfill";
import {
  durationFromProtoJson,
  durationToProtoJson,
} from "../object-serializer.js";

export class SessionServiceSetSessionRequest {
  /**
   * The unique identifier of the session to be updated.
   * @example null
   */
  @Expose({ name: "sessionId" })
  sessionId?: string;
  /**
   * Deprecated: the session token is no longer required when updating a session  and will be ignored when provided.
   * @example null
   * @deprecated This property is deprecated.
   */
  @Expose({ name: "sessionToken" })
  sessionToken?: string;
  /** @example null */
  @Expose({ name: "checks" })
  @Type(() => SessionServiceChecks)
  checks?: SessionServiceChecks;
  /**
   * Additional custom key value pairs to be stored on the session.  Existing keys will be overwritten. To delete a key, set its value to an empty byte array.  Note that metadata keys cannot be changed once the session has been created.  You need to create a new entry and delete the old one instead.
   * @example null
   */
  @Expose({ name: "metadata" })
  metadata?: { [key: string]: Buffer };
  /** @example null */
  @Expose({ name: "challenges" })
  @Type(() => SessionServiceRequestChallenges)
  challenges?: SessionServiceRequestChallenges;
  /**
   * A Duration represents a signed, fixed-length span of time represented  as a count of seconds and fractions of seconds at nanosecond  resolution. It is independent of any calendar and concepts like \"day\"  or \"month\". It is related to Timestamp in that the difference between  two Timestamp values is a Duration and it can be added or subtracted  from a Timestamp. Range is approximately +-10,000 years.   # Examples   Example 1: Compute Duration from two Timestamps in pseudo code.       Timestamp start = ...;      Timestamp end = ...;      Duration duration = ...;       duration.seconds = end.seconds - start.seconds;      duration.nanos = end.nanos - start.nanos;       if (duration.seconds < 0 && duration.nanos > 0) {        duration.seconds += 1;        duration.nanos -= 1000000000;      } else if (duration.seconds > 0 && duration.nanos < 0) {        duration.seconds -= 1;        duration.nanos += 1000000000;      }   Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.       Timestamp start = ...;      Duration duration = ...;      Timestamp end = ...;       end.seconds = start.seconds + duration.seconds;      end.nanos = start.nanos + duration.nanos;       if (end.nanos < 0) {        end.seconds -= 1;        end.nanos += 1000000000;      } else if (end.nanos >= 1000000000) {        end.seconds += 1;        end.nanos -= 1000000000;      }   Example 3: Compute Duration from datetime.timedelta in Python.       td = datetime.timedelta(days=3, minutes=10)      duration = Duration()      duration.FromTimedelta(td)   # JSON Mapping   In JSON format, the Duration type is encoded as a string rather than an  object, where the string ends in the suffix \"s\" (indicating seconds) and  is preceded by the number of seconds, with nanoseconds expressed as  fractional seconds. For example, 3 seconds with 0 nanoseconds should be  encoded in JSON format as \"3s\", while 3 seconds and 1 nanosecond should  be expressed in JSON format as \"3.000000001s\", and 3 seconds and 1  microsecond should be expressed in JSON format as \"3.000001s\".
   * @example null
   */
  @Expose({ name: "lifetime" })
  /*
   * 4.8 — `format: duration` is a google.protobuf.Duration. The native
   * type stays Temporal.Duration, but the wire form is protobuf-JSON
   * ("3600s"), not ISO-8601 ("PT1H") — Zitadel rejects the ISO form.
   */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? durationFromProtoJson(value) : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      value instanceof Temporal.Duration ? durationToProtoJson(value) : value,
    { toPlainOnly: true },
  )
  lifetime?: Temporal.Duration;

  constructor(data?: Partial<SessionServiceSetSessionRequest>) {
    Object.assign(this, data);
    if (this.sessionId != null && typeof this.sessionId !== "string") {
      throw new TypeError(
        `sessionId must be a string, got ${typeof this.sessionId}`,
      );
    }
    if (this.sessionToken != null && typeof this.sessionToken !== "string") {
      throw new TypeError(
        `sessionToken must be a string, got ${typeof this.sessionToken}`,
      );
    }
    /**
     * 4.8 — `format: duration` (google.protobuf.Duration). Accept a
     * protobuf-JSON string ("3600s", parsed via durationFromProtoJson,
     * which throws on bad input) or an already-constructed Duration
     * instance.
     */
    if (this.lifetime != null) {
      if (typeof this.lifetime === "string") {
        this.lifetime = durationFromProtoJson(
          this.lifetime as unknown as string,
        ) as unknown as Temporal.Duration;
      } else if (!(this.lifetime instanceof Temporal.Duration)) {
        throw new TypeError(
          `lifetime must be a Temporal.Duration or protobuf-JSON duration string, got ${typeof this.lifetime}`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceSetSessionRequest)) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.sessionId,
        (other as SessionServiceSetSessionRequest).sessionId,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.sessionToken,
        (other as SessionServiceSetSessionRequest).sessionToken,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.checks,
        (other as SessionServiceSetSessionRequest).checks,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.metadata,
        (other as SessionServiceSetSessionRequest).metadata,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.challenges,
        (other as SessionServiceSetSessionRequest).challenges,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceSetSessionRequest.__deepEquals(
        this.lifetime,
        (other as SessionServiceSetSessionRequest).lifetime,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SessionServiceSetSessionRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceSetSessionRequest.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SessionServiceSetSessionRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceSetSessionRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
