// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { UserServiceHuman } from "./user-service-human.js";
import { UserServiceMachine } from "./user-service-machine.js";
import { Expose, Type } from "class-transformer";

export class UserServiceCreateUserRequest {
  /**
   * The unique identifier of the organization the user belongs to.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * The ID is a unique identifier for the user in the instance.  If not specified, it will be generated.  You can set your own user id that is unique within the instance.  This is useful in migration scenarios, for example if the user already has an ID in another Zitadel system.  If not specified, it will be generated.  It can't be changed after creation.
   * @example null
   */
  @Expose({ name: "userId" })
  userId?: string;
  /**
   * The username is a unique identifier for the user in the organization.  If not specified, Zitadel sets the username to the email for users of type human and to the user_id for users of type machine.  It is used to identify the user in the organization and can be used for login.
   * @example null
   */
  @Expose({ name: "username" })
  username?: string;
  /** @example null */
  @Expose({ name: "human" })
  @Type(() => UserServiceHuman)
  human?: UserServiceHuman;
  /** @example null */
  @Expose({ name: "machine" })
  @Type(() => UserServiceMachine)
  machine?: UserServiceMachine;

  constructor(data?: Partial<UserServiceCreateUserRequest>) {
    Object.assign(this, data);
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.username != null && typeof this.username !== "string") {
      throw new TypeError(
        `username must be a string, got ${typeof this.username}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof UserServiceCreateUserRequest)) {
      return false;
    }
    if (
      !UserServiceCreateUserRequest.__deepEquals(
        this.organizationId,
        (other as UserServiceCreateUserRequest).organizationId,
      )
    ) {
      return false;
    }
    if (
      !UserServiceCreateUserRequest.__deepEquals(
        this.userId,
        (other as UserServiceCreateUserRequest).userId,
      )
    ) {
      return false;
    }
    if (
      !UserServiceCreateUserRequest.__deepEquals(
        this.username,
        (other as UserServiceCreateUserRequest).username,
      )
    ) {
      return false;
    }
    if (
      !UserServiceCreateUserRequest.__deepEquals(
        this.human,
        (other as UserServiceCreateUserRequest).human,
      )
    ) {
      return false;
    }
    if (
      !UserServiceCreateUserRequest.__deepEquals(
        this.machine,
        (other as UserServiceCreateUserRequest).machine,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        UserServiceCreateUserRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => UserServiceCreateUserRequest.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !UserServiceCreateUserRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          UserServiceCreateUserRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
