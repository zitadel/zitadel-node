// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceAny } from "./session-service-any.js";
import { Expose, Type } from "class-transformer";

/**
 * Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation
 */
export class SessionServiceConnectError {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"amount":"12.345"` (a JSON string) instead of the spec-required
   * `"amount":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  /**
   * The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
   */
  @Expose({ name: "code" })
  code?: SessionServiceConnectErrorCodeEnum;
  /**
   * A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
   */
  @Expose({ name: "message" })
  message?: string;
  /**
   * A list of messages that carry the error details. There is no limit on the number of messages.
   */
  @Expose({ name: "details" })
  @Type(() => SessionServiceAny)
  details?: Array<SessionServiceAny>;

  /**
   * The schema-declared wire-key names. Its PRESENCE marks this model as
   * declaring `additionalProperties`: undeclared wire keys are free-form data
   * that must SURVIVE a round-trip. ObjectSerializer reads this set and
   * re-attaches any json key NOT listed here after plainToInstance (which, with
   * excludeExtraneousValues, would otherwise silently drop them — data loss).
   * Models without this static intentionally discard extras.
   */
  static readonly __additionalPropertiesDeclaredKeys: ReadonlySet<string> =
    new Set(["code", "message", "details"]);

  [key: string]: unknown;

  constructor(data?: Partial<SessionServiceConnectError>) {
    Object.assign(this, data);
    if (this.message != null && typeof this.message !== "string") {
      throw new TypeError(
        `message must be a string, got ${typeof this.message}`,
      );
    }
    if (this.code != null) {
      const codeValues = Object.values(
        SessionServiceConnectErrorCodeEnum,
      ).filter(
        (v) =>
          typeof (
            SessionServiceConnectErrorCodeEnum as Record<string, unknown>
          )[v as string] !== "number",
      );
      if (!(codeValues as readonly unknown[]).includes(this.code)) {
        throw new TypeError(
          `Unknown enum value for code: ${JSON.stringify(this.code)}. ` +
            `Expected one of [${codeValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields (including the
   * additionalProperties index keys). Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceConnectError)) {
      return false;
    }
    if (
      !SessionServiceConnectError.__deepEquals(
        this.code,
        (other as SessionServiceConnectError).code,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceConnectError.__deepEquals(
        this.message,
        (other as SessionServiceConnectError).message,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceConnectError.__deepEquals(
        this.details,
        (other as SessionServiceConnectError).details,
      )
    ) {
      return false;
    }
    {
      const declared = new Set<string>(["code", "message", "details"]);
      const selfRec = this as unknown as Record<string, unknown>;
      const otherRec = other as unknown as Record<string, unknown>;
      const selfKeys = Object.keys(selfRec).filter((k) => !declared.has(k));
      const otherKeys = Object.keys(otherRec).filter((k) => !declared.has(k));
      if (selfKeys.length !== otherKeys.length) {
        return false;
      }
      for (const k of selfKeys) {
        if (!Object.prototype.hasOwnProperty.call(otherRec, k)) {
          return false;
        }
        if (!SessionServiceConnectError.__deepEquals(selfRec[k], otherRec[k])) {
          return false;
        }
      }
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SessionServiceConnectError.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceConnectError.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SessionServiceConnectError.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceConnectError.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}

export enum SessionServiceConnectErrorCodeEnum {
  Canceled = "canceled",
  Unknown = "unknown",
  InvalidArgument = "invalid_argument",
  DeadlineExceeded = "deadline_exceeded",
  NotFound = "not_found",
  AlreadyExists = "already_exists",
  PermissionDenied = "permission_denied",
  ResourceExhausted = "resource_exhausted",
  FailedPrecondition = "failed_precondition",
  Aborted = "aborted",
  OutOfRange = "out_of_range",
  Unimplemented = "unimplemented",
  Internal = "internal",
  Unavailable = "unavailable",
  DataLoss = "data_loss",
  Unauthenticated = "unauthenticated",
}
