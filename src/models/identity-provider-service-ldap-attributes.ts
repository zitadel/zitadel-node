// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class IdentityProviderServiceLDAPAttributes {
  /** @example null */
  @Expose({ name: "idAttribute" })
  idAttribute?: string;
  /** @example null */
  @Expose({ name: "firstNameAttribute" })
  firstNameAttribute?: string;
  /** @example null */
  @Expose({ name: "lastNameAttribute" })
  lastNameAttribute?: string;
  /** @example null */
  @Expose({ name: "displayNameAttribute" })
  displayNameAttribute?: string;
  /** @example null */
  @Expose({ name: "nickNameAttribute" })
  nickNameAttribute?: string;
  /** @example null */
  @Expose({ name: "preferredUsernameAttribute" })
  preferredUsernameAttribute?: string;
  /** @example null */
  @Expose({ name: "emailAttribute" })
  emailAttribute?: string;
  /** @example null */
  @Expose({ name: "emailVerifiedAttribute" })
  emailVerifiedAttribute?: string;
  /** @example null */
  @Expose({ name: "phoneAttribute" })
  phoneAttribute?: string;
  /** @example null */
  @Expose({ name: "phoneVerifiedAttribute" })
  phoneVerifiedAttribute?: string;
  /** @example null */
  @Expose({ name: "preferredLanguageAttribute" })
  preferredLanguageAttribute?: string;
  /** @example null */
  @Expose({ name: "avatarUrlAttribute" })
  avatarUrlAttribute?: string;
  /** @example null */
  @Expose({ name: "profileAttribute" })
  profileAttribute?: string;
  /** @example null */
  @Expose({ name: "rootCa" })
  rootCa?: string;

  constructor(data?: Partial<IdentityProviderServiceLDAPAttributes>) {
    Object.assign(this, data);
    if (this.idAttribute != null && typeof this.idAttribute !== "string") {
      throw new TypeError(
        `idAttribute must be a string, got ${typeof this.idAttribute}`,
      );
    }
    if (
      this.firstNameAttribute != null &&
      typeof this.firstNameAttribute !== "string"
    ) {
      throw new TypeError(
        `firstNameAttribute must be a string, got ${typeof this.firstNameAttribute}`,
      );
    }
    if (
      this.lastNameAttribute != null &&
      typeof this.lastNameAttribute !== "string"
    ) {
      throw new TypeError(
        `lastNameAttribute must be a string, got ${typeof this.lastNameAttribute}`,
      );
    }
    if (
      this.displayNameAttribute != null &&
      typeof this.displayNameAttribute !== "string"
    ) {
      throw new TypeError(
        `displayNameAttribute must be a string, got ${typeof this.displayNameAttribute}`,
      );
    }
    if (
      this.nickNameAttribute != null &&
      typeof this.nickNameAttribute !== "string"
    ) {
      throw new TypeError(
        `nickNameAttribute must be a string, got ${typeof this.nickNameAttribute}`,
      );
    }
    if (
      this.preferredUsernameAttribute != null &&
      typeof this.preferredUsernameAttribute !== "string"
    ) {
      throw new TypeError(
        `preferredUsernameAttribute must be a string, got ${typeof this.preferredUsernameAttribute}`,
      );
    }
    if (
      this.emailAttribute != null &&
      typeof this.emailAttribute !== "string"
    ) {
      throw new TypeError(
        `emailAttribute must be a string, got ${typeof this.emailAttribute}`,
      );
    }
    if (
      this.emailVerifiedAttribute != null &&
      typeof this.emailVerifiedAttribute !== "string"
    ) {
      throw new TypeError(
        `emailVerifiedAttribute must be a string, got ${typeof this.emailVerifiedAttribute}`,
      );
    }
    if (
      this.phoneAttribute != null &&
      typeof this.phoneAttribute !== "string"
    ) {
      throw new TypeError(
        `phoneAttribute must be a string, got ${typeof this.phoneAttribute}`,
      );
    }
    if (
      this.phoneVerifiedAttribute != null &&
      typeof this.phoneVerifiedAttribute !== "string"
    ) {
      throw new TypeError(
        `phoneVerifiedAttribute must be a string, got ${typeof this.phoneVerifiedAttribute}`,
      );
    }
    if (
      this.preferredLanguageAttribute != null &&
      typeof this.preferredLanguageAttribute !== "string"
    ) {
      throw new TypeError(
        `preferredLanguageAttribute must be a string, got ${typeof this.preferredLanguageAttribute}`,
      );
    }
    if (
      this.avatarUrlAttribute != null &&
      typeof this.avatarUrlAttribute !== "string"
    ) {
      throw new TypeError(
        `avatarUrlAttribute must be a string, got ${typeof this.avatarUrlAttribute}`,
      );
    }
    if (
      this.profileAttribute != null &&
      typeof this.profileAttribute !== "string"
    ) {
      throw new TypeError(
        `profileAttribute must be a string, got ${typeof this.profileAttribute}`,
      );
    }
    if (this.rootCa != null && typeof this.rootCa !== "string") {
      throw new TypeError(`rootCa must be a string, got ${typeof this.rootCa}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof IdentityProviderServiceLDAPAttributes)) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.idAttribute,
        (other as IdentityProviderServiceLDAPAttributes).idAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.firstNameAttribute,
        (other as IdentityProviderServiceLDAPAttributes).firstNameAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.lastNameAttribute,
        (other as IdentityProviderServiceLDAPAttributes).lastNameAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.displayNameAttribute,
        (other as IdentityProviderServiceLDAPAttributes).displayNameAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.nickNameAttribute,
        (other as IdentityProviderServiceLDAPAttributes).nickNameAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.preferredUsernameAttribute,
        (other as IdentityProviderServiceLDAPAttributes)
          .preferredUsernameAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.emailAttribute,
        (other as IdentityProviderServiceLDAPAttributes).emailAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.emailVerifiedAttribute,
        (other as IdentityProviderServiceLDAPAttributes).emailVerifiedAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.phoneAttribute,
        (other as IdentityProviderServiceLDAPAttributes).phoneAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.phoneVerifiedAttribute,
        (other as IdentityProviderServiceLDAPAttributes).phoneVerifiedAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.preferredLanguageAttribute,
        (other as IdentityProviderServiceLDAPAttributes)
          .preferredLanguageAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.avatarUrlAttribute,
        (other as IdentityProviderServiceLDAPAttributes).avatarUrlAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.profileAttribute,
        (other as IdentityProviderServiceLDAPAttributes).profileAttribute,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceLDAPAttributes.__deepEquals(
        this.rootCa,
        (other as IdentityProviderServiceLDAPAttributes).rootCa,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        IdentityProviderServiceLDAPAttributes.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          IdentityProviderServiceLDAPAttributes.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !IdentityProviderServiceLDAPAttributes.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          IdentityProviderServiceLDAPAttributes.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
