// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class OIDCServiceDeviceAuthorizationRequest {
  /**
   * The unique identifier of the device authorization request to be used for authorizing or denying the request.
   * @example null
   */
  @Expose({ name: "id" })
  id?: string;
  /**
   * The client_id of the application that initiated the device authorization request.
   * @example null
   */
  @Expose({ name: "clientId" })
  clientId?: string;
  /**
   * The scopes requested by the application.
   * @example null
   */
  @Expose({ name: "scope" })
  scope?: Array<string>;
  /**
   * Name of the client application.
   * @example null
   */
  @Expose({ name: "appName" })
  appName?: string;
  /**
   * Name of the project the client application is part of.
   * @example null
   */
  @Expose({ name: "projectName" })
  projectName?: string;

  constructor(data?: Partial<OIDCServiceDeviceAuthorizationRequest>) {
    Object.assign(this, data);
    if (this.id != null && typeof this.id !== "string") {
      throw new TypeError(`id must be a string, got ${typeof this.id}`);
    }
    if (this.clientId != null && typeof this.clientId !== "string") {
      throw new TypeError(
        `clientId must be a string, got ${typeof this.clientId}`,
      );
    }
    if (
      this.scope != null &&
      !Array.isArray(this.scope) &&
      !((this.scope as unknown) instanceof Set)
    ) {
      throw new TypeError(`scope must be an array, got ${typeof this.scope}`);
    }
    if (this.appName != null && typeof this.appName !== "string") {
      throw new TypeError(
        `appName must be a string, got ${typeof this.appName}`,
      );
    }
    if (this.projectName != null && typeof this.projectName !== "string") {
      throw new TypeError(
        `projectName must be a string, got ${typeof this.projectName}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof OIDCServiceDeviceAuthorizationRequest)) {
      return false;
    }
    if (
      !OIDCServiceDeviceAuthorizationRequest.__deepEquals(
        this.id,
        (other as OIDCServiceDeviceAuthorizationRequest).id,
      )
    ) {
      return false;
    }
    if (
      !OIDCServiceDeviceAuthorizationRequest.__deepEquals(
        this.clientId,
        (other as OIDCServiceDeviceAuthorizationRequest).clientId,
      )
    ) {
      return false;
    }
    if (
      !OIDCServiceDeviceAuthorizationRequest.__deepEquals(
        this.scope,
        (other as OIDCServiceDeviceAuthorizationRequest).scope,
      )
    ) {
      return false;
    }
    if (
      !OIDCServiceDeviceAuthorizationRequest.__deepEquals(
        this.appName,
        (other as OIDCServiceDeviceAuthorizationRequest).appName,
      )
    ) {
      return false;
    }
    if (
      !OIDCServiceDeviceAuthorizationRequest.__deepEquals(
        this.projectName,
        (other as OIDCServiceDeviceAuthorizationRequest).projectName,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        OIDCServiceDeviceAuthorizationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          OIDCServiceDeviceAuthorizationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !OIDCServiceDeviceAuthorizationRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          OIDCServiceDeviceAuthorizationRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
