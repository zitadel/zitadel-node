// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { FeatureServiceImprovedPerformance } from "./feature-service-improved-performance.js";
import { FeatureServiceLoginV2 } from "./feature-service-login-v2.js";
import { Expose, Type } from "class-transformer";

export class FeatureServiceSetInstanceFeaturesRequest {
  /** @example null */
  @Expose({ name: "loginDefaultOrg" })
  loginDefaultOrg?: boolean;
  /** @example null */
  @Expose({ name: "userSchema" })
  userSchema?: boolean;
  /**
   * Deprecated: the flag has been removed and `urn:ietf:params:oauth:grant-type:token-exchange`  grant type for the OIDC token endpoint is enabled by default.  Token exchange can be used to request tokens with a lesser scope or impersonate other users.  See the security policy to allow impersonation on an instance.  This field is only kept for backward compatibility and will be removed in the next major version of Zitadel.  Setting the field will have no effect.
   * @example null
   * @deprecated This property is deprecated.
   */
  @Expose({ name: "oidcTokenExchange" })
  oidcTokenExchange?: boolean;
  /** @example null */
  @Expose({ name: "improvedPerformance" })
  improvedPerformance?: Array<FeatureServiceImprovedPerformance>;
  /** @example null */
  @Expose({ name: "debugOidcParentError" })
  debugOidcParentError?: boolean;
  /** @example null */
  @Expose({ name: "oidcSingleV1SessionTermination" })
  oidcSingleV1SessionTermination?: boolean;
  /**
   * Deprecated: the flag has been removed and OIDC Back-Channel Logout is always enabled.  This field is only kept for backward compatibility and will be removed in the next major version of Zitadel.  Setting the field will have no effect.
   * @example null
   * @deprecated This property is deprecated.
   */
  @Expose({ name: "enableBackChannelLogout" })
  enableBackChannelLogout?: boolean;
  /** @example null */
  @Expose({ name: "loginV2" })
  @Type(() => FeatureServiceLoginV2)
  loginV2?: FeatureServiceLoginV2;
  /** @example null */
  @Expose({ name: "permissionCheckV2" })
  permissionCheckV2?: boolean;
  /** @example null */
  @Expose({ name: "consoleUseV2UserApi" })
  consoleUseV2UserApi?: boolean;

  constructor(data?: Partial<FeatureServiceSetInstanceFeaturesRequest>) {
    Object.assign(this, data);
    if (
      this.loginDefaultOrg != null &&
      typeof this.loginDefaultOrg !== "boolean"
    ) {
      throw new TypeError(
        `loginDefaultOrg must be a boolean, got ${typeof this.loginDefaultOrg}`,
      );
    }
    if (this.userSchema != null && typeof this.userSchema !== "boolean") {
      throw new TypeError(
        `userSchema must be a boolean, got ${typeof this.userSchema}`,
      );
    }
    if (
      this.oidcTokenExchange != null &&
      typeof this.oidcTokenExchange !== "boolean"
    ) {
      throw new TypeError(
        `oidcTokenExchange must be a boolean, got ${typeof this.oidcTokenExchange}`,
      );
    }
    if (
      this.debugOidcParentError != null &&
      typeof this.debugOidcParentError !== "boolean"
    ) {
      throw new TypeError(
        `debugOidcParentError must be a boolean, got ${typeof this.debugOidcParentError}`,
      );
    }
    if (
      this.oidcSingleV1SessionTermination != null &&
      typeof this.oidcSingleV1SessionTermination !== "boolean"
    ) {
      throw new TypeError(
        `oidcSingleV1SessionTermination must be a boolean, got ${typeof this.oidcSingleV1SessionTermination}`,
      );
    }
    if (
      this.enableBackChannelLogout != null &&
      typeof this.enableBackChannelLogout !== "boolean"
    ) {
      throw new TypeError(
        `enableBackChannelLogout must be a boolean, got ${typeof this.enableBackChannelLogout}`,
      );
    }
    if (
      this.permissionCheckV2 != null &&
      typeof this.permissionCheckV2 !== "boolean"
    ) {
      throw new TypeError(
        `permissionCheckV2 must be a boolean, got ${typeof this.permissionCheckV2}`,
      );
    }
    if (
      this.consoleUseV2UserApi != null &&
      typeof this.consoleUseV2UserApi !== "boolean"
    ) {
      throw new TypeError(
        `consoleUseV2UserApi must be a boolean, got ${typeof this.consoleUseV2UserApi}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof FeatureServiceSetInstanceFeaturesRequest)) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.loginDefaultOrg,
        (other as FeatureServiceSetInstanceFeaturesRequest).loginDefaultOrg,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.userSchema,
        (other as FeatureServiceSetInstanceFeaturesRequest).userSchema,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.oidcTokenExchange,
        (other as FeatureServiceSetInstanceFeaturesRequest).oidcTokenExchange,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.improvedPerformance,
        (other as FeatureServiceSetInstanceFeaturesRequest).improvedPerformance,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.debugOidcParentError,
        (other as FeatureServiceSetInstanceFeaturesRequest)
          .debugOidcParentError,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.oidcSingleV1SessionTermination,
        (other as FeatureServiceSetInstanceFeaturesRequest)
          .oidcSingleV1SessionTermination,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.enableBackChannelLogout,
        (other as FeatureServiceSetInstanceFeaturesRequest)
          .enableBackChannelLogout,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.loginV2,
        (other as FeatureServiceSetInstanceFeaturesRequest).loginV2,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.permissionCheckV2,
        (other as FeatureServiceSetInstanceFeaturesRequest).permissionCheckV2,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(
        this.consoleUseV2UserApi,
        (other as FeatureServiceSetInstanceFeaturesRequest).consoleUseV2UserApi,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        FeatureServiceSetInstanceFeaturesRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          FeatureServiceSetInstanceFeaturesRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !FeatureServiceSetInstanceFeaturesRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          FeatureServiceSetInstanceFeaturesRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
