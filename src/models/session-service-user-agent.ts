// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceHeaderValues } from "./session-service-header-values.js";
import { Expose, Type } from "class-transformer";

export class SessionServiceUserAgent {
  /**
   * FingerprintID is a unique identifier for the user agent's fingerprint.  It can be used to group sessions by device or browser.
   * @example null
   */
  @Expose({ name: "fingerprintId" })
  fingerprintId?: string;
  /**
   * IP is the IP address from which the session was created.
   * @example null
   */
  @Expose({ name: "ip" })
  ip?: string;
  /**
   * Description is a human-readable description of the user agent.
   * @example null
   */
  @Expose({ name: "description" })
  description?: string;
  /** @example null */
  @Expose({ name: "header" })
  @Type(() => SessionServiceHeaderValues)
  header?: { [key: string]: SessionServiceHeaderValues };

  constructor(data?: Partial<SessionServiceUserAgent>) {
    Object.assign(this, data);
    if (this.fingerprintId != null && typeof this.fingerprintId !== "string") {
      throw new TypeError(
        `fingerprintId must be a string, got ${typeof this.fingerprintId}`,
      );
    }
    if (this.ip != null && typeof this.ip !== "string") {
      throw new TypeError(`ip must be a string, got ${typeof this.ip}`);
    }
    if (this.description != null && typeof this.description !== "string") {
      throw new TypeError(
        `description must be a string, got ${typeof this.description}`,
      );
    }
    /**
     * 4.6 — Map-of-Model deep deserialise. When a field is typed
     * `Record<string, SessionServiceHeaderValues>`, the wire
     * payload arrives as a plain JSON object whose values are bag-of-keys, not
     * `SessionServiceHeaderValues` instances. Walk each entry
     * and route it back through the model's constructor so the resulting record
     * actually contains typed model instances.
     */
    if (
      this.header != null &&
      typeof this.header === "object" &&
      !Array.isArray(this.header)
    ) {
      const __raw_header = this.header as unknown as Record<string, unknown>;
      const __out_header: Record<string, SessionServiceHeaderValues> = {};
      for (const __k of Object.keys(__raw_header)) {
        const __v = __raw_header[__k];
        if (__v instanceof SessionServiceHeaderValues) {
          __out_header[__k] = __v;
        } else if (__v != null && typeof __v === "object") {
          __out_header[__k] = new SessionServiceHeaderValues(
            __v as Partial<SessionServiceHeaderValues>,
          );
        }
      }
      this.header = __out_header as unknown as {
        [key: string]: SessionServiceHeaderValues;
      };
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceUserAgent)) {
      return false;
    }
    if (
      !SessionServiceUserAgent.__deepEquals(
        this.fingerprintId,
        (other as SessionServiceUserAgent).fingerprintId,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceUserAgent.__deepEquals(
        this.ip,
        (other as SessionServiceUserAgent).ip,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceUserAgent.__deepEquals(
        this.description,
        (other as SessionServiceUserAgent).description,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceUserAgent.__deepEquals(
        this.header,
        (other as SessionServiceUserAgent).header,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => SessionServiceUserAgent.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceUserAgent.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !SessionServiceUserAgent.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceUserAgent.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
