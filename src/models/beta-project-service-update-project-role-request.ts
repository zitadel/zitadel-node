// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class BetaProjectServiceUpdateProjectRoleRequest {
  /**
   * ID of the project.
   * @example null
   */
  @Expose({ name: "projectId" })
  projectId?: string;
  /**
   * The key is the only relevant attribute for ZITADEL regarding the authorization checks.
   * @example null
   */
  @Expose({ name: "roleKey" })
  roleKey?: string;
  /**
   * Name displayed for the role.
   * @example null
   */
  @Expose({ name: "displayName" })
  displayName?: string;
  /**
   * The group is only used for display purposes. That you have better handling, like giving all the roles from a group to a user.
   * @example null
   */
  @Expose({ name: "group" })
  group?: string;

  constructor(data?: Partial<BetaProjectServiceUpdateProjectRoleRequest>) {
    Object.assign(this, data);
    if (this.projectId != null && typeof this.projectId !== "string") {
      throw new TypeError(
        `projectId must be a string, got ${typeof this.projectId}`,
      );
    }
    if (this.roleKey != null && typeof this.roleKey !== "string") {
      throw new TypeError(
        `roleKey must be a string, got ${typeof this.roleKey}`,
      );
    }
    if (this.displayName != null && typeof this.displayName !== "string") {
      throw new TypeError(
        `displayName must be a string, got ${typeof this.displayName}`,
      );
    }
    if (this.group != null && typeof this.group !== "string") {
      throw new TypeError(`group must be a string, got ${typeof this.group}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaProjectServiceUpdateProjectRoleRequest)) {
      return false;
    }
    if (
      !BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(
        this.projectId,
        (other as BetaProjectServiceUpdateProjectRoleRequest).projectId,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(
        this.roleKey,
        (other as BetaProjectServiceUpdateProjectRoleRequest).roleKey,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(
        this.displayName,
        (other as BetaProjectServiceUpdateProjectRoleRequest).displayName,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(
        this.group,
        (other as BetaProjectServiceUpdateProjectRoleRequest).group,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaProjectServiceUpdateProjectRoleRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
