// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaSettingsServiceResourceOwnerType } from "./beta-settings-service-resource-owner-type.js";
import { Expose } from "class-transformer";

export class BetaSettingsServiceLegalAndSupportSettings {
  /** @example null */
  @Expose({ name: "tosLink" })
  tosLink?: string;
  /** @example null */
  @Expose({ name: "privacyPolicyLink" })
  privacyPolicyLink?: string;
  /** @example null */
  @Expose({ name: "helpLink" })
  helpLink?: string;
  /** @example null */
  @Expose({ name: "supportEmail" })
  supportEmail?: string;
  /** @example null */
  @Expose({ name: "resourceOwnerType" })
  resourceOwnerType?: BetaSettingsServiceResourceOwnerType;
  /** @example null */
  @Expose({ name: "docsLink" })
  docsLink?: string;
  /** @example null */
  @Expose({ name: "customLink" })
  customLink?: string;
  /** @example null */
  @Expose({ name: "customLinkText" })
  customLinkText?: string;

  constructor(data?: Partial<BetaSettingsServiceLegalAndSupportSettings>) {
    Object.assign(this, data);
    if (this.tosLink != null && typeof this.tosLink !== "string") {
      throw new TypeError(
        `tosLink must be a string, got ${typeof this.tosLink}`,
      );
    }
    if (
      this.privacyPolicyLink != null &&
      typeof this.privacyPolicyLink !== "string"
    ) {
      throw new TypeError(
        `privacyPolicyLink must be a string, got ${typeof this.privacyPolicyLink}`,
      );
    }
    if (this.helpLink != null && typeof this.helpLink !== "string") {
      throw new TypeError(
        `helpLink must be a string, got ${typeof this.helpLink}`,
      );
    }
    if (this.supportEmail != null && typeof this.supportEmail !== "string") {
      throw new TypeError(
        `supportEmail must be a string, got ${typeof this.supportEmail}`,
      );
    }
    if (this.docsLink != null && typeof this.docsLink !== "string") {
      throw new TypeError(
        `docsLink must be a string, got ${typeof this.docsLink}`,
      );
    }
    if (this.customLink != null && typeof this.customLink !== "string") {
      throw new TypeError(
        `customLink must be a string, got ${typeof this.customLink}`,
      );
    }
    if (
      this.customLinkText != null &&
      typeof this.customLinkText !== "string"
    ) {
      throw new TypeError(
        `customLinkText must be a string, got ${typeof this.customLinkText}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaSettingsServiceLegalAndSupportSettings)) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.tosLink,
        (other as BetaSettingsServiceLegalAndSupportSettings).tosLink,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.privacyPolicyLink,
        (other as BetaSettingsServiceLegalAndSupportSettings).privacyPolicyLink,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.helpLink,
        (other as BetaSettingsServiceLegalAndSupportSettings).helpLink,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.supportEmail,
        (other as BetaSettingsServiceLegalAndSupportSettings).supportEmail,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.resourceOwnerType,
        (other as BetaSettingsServiceLegalAndSupportSettings).resourceOwnerType,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.docsLink,
        (other as BetaSettingsServiceLegalAndSupportSettings).docsLink,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.customLink,
        (other as BetaSettingsServiceLegalAndSupportSettings).customLink,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(
        this.customLinkText,
        (other as BetaSettingsServiceLegalAndSupportSettings).customLinkText,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaSettingsServiceLegalAndSupportSettings.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaSettingsServiceLegalAndSupportSettings.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaSettingsServiceLegalAndSupportSettings.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaSettingsServiceLegalAndSupportSettings.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
