// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaSessionServiceIntentFactor } from "./beta-session-service-intent-factor.js";
import { BetaSessionServiceOTPFactor } from "./beta-session-service-otp-factor.js";
import { BetaSessionServicePasswordFactor } from "./beta-session-service-password-factor.js";
import { BetaSessionServiceTOTPFactor } from "./beta-session-service-totp-factor.js";
import { BetaSessionServiceUserFactor } from "./beta-session-service-user-factor.js";
import { BetaSessionServiceWebAuthNFactor } from "./beta-session-service-web-auth-n-factor.js";
import { Expose, Type } from "class-transformer";

export class BetaSessionServiceFactors {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"weightKg":"12.345"` (a JSON string) instead of the spec-required
   * `"weightKg":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "user" })
  @Type(() => BetaSessionServiceUserFactor)
  user?: BetaSessionServiceUserFactor;
  @Expose({ name: "password" })
  @Type(() => BetaSessionServicePasswordFactor)
  password?: BetaSessionServicePasswordFactor;
  @Expose({ name: "webAuthN" })
  @Type(() => BetaSessionServiceWebAuthNFactor)
  webAuthN?: BetaSessionServiceWebAuthNFactor;
  @Expose({ name: "intent" })
  @Type(() => BetaSessionServiceIntentFactor)
  intent?: BetaSessionServiceIntentFactor;
  @Expose({ name: "totp" })
  @Type(() => BetaSessionServiceTOTPFactor)
  totp?: BetaSessionServiceTOTPFactor;
  @Expose({ name: "otpSms" })
  @Type(() => BetaSessionServiceOTPFactor)
  otpSms?: BetaSessionServiceOTPFactor;
  @Expose({ name: "otpEmail" })
  @Type(() => BetaSessionServiceOTPFactor)
  otpEmail?: BetaSessionServiceOTPFactor;

  constructor(data?: Partial<BetaSessionServiceFactors>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaSessionServiceFactors)) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.user,
        (other as BetaSessionServiceFactors).user,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.password,
        (other as BetaSessionServiceFactors).password,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.webAuthN,
        (other as BetaSessionServiceFactors).webAuthN,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.intent,
        (other as BetaSessionServiceFactors).intent,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.totp,
        (other as BetaSessionServiceFactors).totp,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.otpSms,
        (other as BetaSessionServiceFactors).otpSms,
      )
    ) {
      return false;
    }
    if (
      !BetaSessionServiceFactors.__deepEquals(
        this.otpEmail,
        (other as BetaSessionServiceFactors).otpEmail,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaSessionServiceFactors.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaSessionServiceFactors.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaSessionServiceFactors.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaSessionServiceFactors.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
