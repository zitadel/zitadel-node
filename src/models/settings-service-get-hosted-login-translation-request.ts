// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class SettingsServiceGetHostedLoginTranslationRequest {
  /**
   * The locale of the translations to be returned.  Needs to be a BCP 47 language tag (e.g. \"en\", \"de\", \"fr-CH\").
   * @example null
   */
  @Expose({ name: "locale" })
  locale?: string;
  /**
   * if set to true, higher levels are ignored, if false higher levels are merged into the file
   * @example null
   */
  @Expose({ name: "ignoreInheritance" })
  ignoreInheritance?: boolean;
  /** @example null */
  @Expose({ name: "instance" })
  instance?: boolean;
  /** @example null */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /** @example null */
  @Expose({ name: "system" })
  system?: boolean;

  constructor(data?: Partial<SettingsServiceGetHostedLoginTranslationRequest>) {
    Object.assign(this, data);
    if (this.locale != null && typeof this.locale !== "string") {
      throw new TypeError(`locale must be a string, got ${typeof this.locale}`);
    }
    if (
      this.ignoreInheritance != null &&
      typeof this.ignoreInheritance !== "boolean"
    ) {
      throw new TypeError(
        `ignoreInheritance must be a boolean, got ${typeof this.ignoreInheritance}`,
      );
    }
    if (this.instance != null && typeof this.instance !== "boolean") {
      throw new TypeError(
        `instance must be a boolean, got ${typeof this.instance}`,
      );
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.system != null && typeof this.system !== "boolean") {
      throw new TypeError(
        `system must be a boolean, got ${typeof this.system}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SettingsServiceGetHostedLoginTranslationRequest)) {
      return false;
    }
    if (
      !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
        this.locale,
        (other as SettingsServiceGetHostedLoginTranslationRequest).locale,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
        this.ignoreInheritance,
        (other as SettingsServiceGetHostedLoginTranslationRequest)
          .ignoreInheritance,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
        this.instance,
        (other as SettingsServiceGetHostedLoginTranslationRequest).instance,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
        this.organizationId,
        (other as SettingsServiceGetHostedLoginTranslationRequest)
          .organizationId,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
        this.system,
        (other as SettingsServiceGetHostedLoginTranslationRequest).system,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SettingsServiceGetHostedLoginTranslationRequest.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
