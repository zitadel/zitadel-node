// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { IdentityProviderServiceSAMLBinding } from "./identity-provider-service-saml-binding.js";
import { IdentityProviderServiceSAMLNameIDFormat } from "./identity-provider-service-saml-name-id-format.js";
import { IdentityProviderServiceSAMLSignatureAlgorithm } from "./identity-provider-service-saml-signature-algorithm.js";
import { Expose, Type, Transform } from "class-transformer";

export class IdentityProviderServiceSAMLConfig {
  /**
   * Metadata of the SAML identity provider.
   * @example null
   */
  @Expose({ name: "metadataXml" })
  /** 2.1 — `format: byte` round-trips Buffer <-> base64 string at the serde boundary. */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? Buffer.from(value, "base64") : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      Buffer.isBuffer(value) ? (value as Buffer).toString("base64") : value,
    { toPlainOnly: true },
  )
  metadataXml?: Buffer;
  /** @example null */
  @Expose({ name: "binding" })
  binding?: IdentityProviderServiceSAMLBinding;
  /**
   * Boolean which defines if the authentication requests are signed.
   * @example null
   */
  @Expose({ name: "withSignedRequest" })
  withSignedRequest?: boolean;
  /** @example null */
  @Expose({ name: "nameIdFormat" })
  nameIdFormat?: IdentityProviderServiceSAMLNameIDFormat;
  /**
   * Optional name of the attribute, which will be used to map the user  in case the nameid-format returned is  `urn:oasis:names:tc:SAML:2.0:nameid-format:transient`.
   * @example null
   */
  @Expose({ name: "transientMappingAttributeName" })
  transientMappingAttributeName?: string;
  /**
   * Boolean weather federated logout is enabled. If enabled, ZITADEL will send a logout request to the identity provider,  if the user terminates the session in ZITADEL. Be sure to provide a SLO endpoint as part of the metadata.
   * @example null
   */
  @Expose({ name: "federatedLogoutEnabled" })
  federatedLogoutEnabled?: boolean;
  /** @example null */
  @Expose({ name: "signatureAlgorithm" })
  signatureAlgorithm?: IdentityProviderServiceSAMLSignatureAlgorithm;

  constructor(data?: Partial<IdentityProviderServiceSAMLConfig>) {
    Object.assign(this, data);
    /**
     * 2.1 — format: byte. The wire form is base64; the model field is a
     * Buffer. Lives outside the isString block because typeMapping
     * (ByteArray to Buffer) flips isString to false at codegen time.
     */
    if (this.metadataXml != null && typeof this.metadataXml === "string") {
      this.metadataXml = Buffer.from(
        this.metadataXml as unknown as string,
        "base64",
      ) as unknown as Buffer;
    } else if (this.metadataXml != null && !Buffer.isBuffer(this.metadataXml)) {
      throw new TypeError(
        `metadataXml must be a Buffer or base64 string, got ${typeof this.metadataXml}`,
      );
    }
    if (
      this.withSignedRequest != null &&
      typeof this.withSignedRequest !== "boolean"
    ) {
      throw new TypeError(
        `withSignedRequest must be a boolean, got ${typeof this.withSignedRequest}`,
      );
    }
    if (
      this.transientMappingAttributeName != null &&
      typeof this.transientMappingAttributeName !== "string"
    ) {
      throw new TypeError(
        `transientMappingAttributeName must be a string, got ${typeof this.transientMappingAttributeName}`,
      );
    }
    if (
      this.federatedLogoutEnabled != null &&
      typeof this.federatedLogoutEnabled !== "boolean"
    ) {
      throw new TypeError(
        `federatedLogoutEnabled must be a boolean, got ${typeof this.federatedLogoutEnabled}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof IdentityProviderServiceSAMLConfig)) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.metadataXml,
        (other as IdentityProviderServiceSAMLConfig).metadataXml,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.binding,
        (other as IdentityProviderServiceSAMLConfig).binding,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.withSignedRequest,
        (other as IdentityProviderServiceSAMLConfig).withSignedRequest,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.nameIdFormat,
        (other as IdentityProviderServiceSAMLConfig).nameIdFormat,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.transientMappingAttributeName,
        (other as IdentityProviderServiceSAMLConfig)
          .transientMappingAttributeName,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.federatedLogoutEnabled,
        (other as IdentityProviderServiceSAMLConfig).federatedLogoutEnabled,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceSAMLConfig.__deepEquals(
        this.signatureAlgorithm,
        (other as IdentityProviderServiceSAMLConfig).signatureAlgorithm,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        IdentityProviderServiceSAMLConfig.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => IdentityProviderServiceSAMLConfig.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !IdentityProviderServiceSAMLConfig.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          IdentityProviderServiceSAMLConfig.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
