// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { ApplicationServiceCreateAPIApplicationRequest } from "./application-service-create-api-application-request.js";
import { ApplicationServiceCreateOIDCApplicationRequest } from "./application-service-create-oidc-application-request.js";
import { ApplicationServiceCreateSAMLApplicationRequest } from "./application-service-create-saml-application-request.js";
import { Expose, Type } from "class-transformer";

export class ApplicationServiceCreateApplicationRequest {
  /**
   * The ID of the project the application will be created in.
   * @example null
   */
  @Expose({ name: "projectId" })
  projectId?: string;
  /**
   * Optionally, provide the unique ID of the new application. If omitted, the system will generate one for you,  which is the recommended way. The generated ID will be returned in the response.
   * @example null
   */
  @Expose({ name: "applicationId" })
  applicationId?: string;
  /**
   * Publicly visible name of the application. This might be presented to users if they sign in.
   * @example null
   */
  @Expose({ name: "name" })
  name?: string;
  /** @example null */
  @Expose({ name: "apiConfiguration" })
  @Type(() => ApplicationServiceCreateAPIApplicationRequest)
  apiConfiguration?: ApplicationServiceCreateAPIApplicationRequest;
  /** @example null */
  @Expose({ name: "oidcConfiguration" })
  @Type(() => ApplicationServiceCreateOIDCApplicationRequest)
  oidcConfiguration?: ApplicationServiceCreateOIDCApplicationRequest;
  /** @example null */
  @Expose({ name: "samlConfiguration" })
  @Type(() => ApplicationServiceCreateSAMLApplicationRequest)
  samlConfiguration?: ApplicationServiceCreateSAMLApplicationRequest;

  constructor(data?: Partial<ApplicationServiceCreateApplicationRequest>) {
    Object.assign(this, data);
    if (this.projectId != null && typeof this.projectId !== "string") {
      throw new TypeError(
        `projectId must be a string, got ${typeof this.projectId}`,
      );
    }
    if (this.applicationId != null && typeof this.applicationId !== "string") {
      throw new TypeError(
        `applicationId must be a string, got ${typeof this.applicationId}`,
      );
    }
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof ApplicationServiceCreateApplicationRequest)) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.projectId,
        (other as ApplicationServiceCreateApplicationRequest).projectId,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.applicationId,
        (other as ApplicationServiceCreateApplicationRequest).applicationId,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.name,
        (other as ApplicationServiceCreateApplicationRequest).name,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.apiConfiguration,
        (other as ApplicationServiceCreateApplicationRequest).apiConfiguration,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.oidcConfiguration,
        (other as ApplicationServiceCreateApplicationRequest).oidcConfiguration,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateApplicationRequest.__deepEquals(
        this.samlConfiguration,
        (other as ApplicationServiceCreateApplicationRequest).samlConfiguration,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        ApplicationServiceCreateApplicationRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          ApplicationServiceCreateApplicationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !ApplicationServiceCreateApplicationRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          ApplicationServiceCreateApplicationRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
