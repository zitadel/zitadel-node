// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { ApplicationServiceOIDCLocalizedMessage } from "./application-service-oidc-localized-message.js";
import { Expose, Type } from "class-transformer";

export class ApplicationServiceCreateOIDCApplicationResponse {
  /**
   * The unique OAuth2/OIDC client_id used for authentication of the application,  e.g. at the token endpoint.
   * @example null
   */
  @Expose({ name: "clientId" })
  clientId?: string;
  /**
   * In case of using the OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_BASIC  or OIDCAuthMethodType.OIDC_AUTH_METHOD_TYPE_CLIENT_SECRET_POST the client_secret is generated and returned.  It must be stored safely, as it will not be possible to retrieve it again.  A new client_secret can be generated using the GenerateClientSecret endpoint.
   * @example null
   */
  @Expose({ name: "clientSecret" })
  clientSecret?: string;
  /**
   * NonCompliant specifies whether the config is OIDC compliant. A production configuration SHOULD be compliant.  Non-compliant configurations can run into interoperability issues with OIDC libraries and tools.  Compliance problems are listed in the compliance_problems field.
   * @example null
   */
  @Expose({ name: "nonCompliant" })
  nonCompliant?: boolean;
  /**
   * ComplianceProblems lists the problems for non-compliant configurations.  In case of a compliant configuration, this list is empty.
   * @example null
   */
  @Expose({ name: "complianceProblems" })
  @Type(() => ApplicationServiceOIDCLocalizedMessage)
  complianceProblems?: Array<ApplicationServiceOIDCLocalizedMessage>;

  constructor(data?: Partial<ApplicationServiceCreateOIDCApplicationResponse>) {
    Object.assign(this, data);
    if (this.clientId != null && typeof this.clientId !== "string") {
      throw new TypeError(
        `clientId must be a string, got ${typeof this.clientId}`,
      );
    }
    if (this.clientSecret != null && typeof this.clientSecret !== "string") {
      throw new TypeError(
        `clientSecret must be a string, got ${typeof this.clientSecret}`,
      );
    }
    if (this.nonCompliant != null && typeof this.nonCompliant !== "boolean") {
      throw new TypeError(
        `nonCompliant must be a boolean, got ${typeof this.nonCompliant}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof ApplicationServiceCreateOIDCApplicationResponse)) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
        this.clientId,
        (other as ApplicationServiceCreateOIDCApplicationResponse).clientId,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
        this.clientSecret,
        (other as ApplicationServiceCreateOIDCApplicationResponse).clientSecret,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
        this.nonCompliant,
        (other as ApplicationServiceCreateOIDCApplicationResponse).nonCompliant,
      )
    ) {
      return false;
    }
    if (
      !ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
        this.complianceProblems,
        (other as ApplicationServiceCreateOIDCApplicationResponse)
          .complianceProblems,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          ApplicationServiceCreateOIDCApplicationResponse.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
