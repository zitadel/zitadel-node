// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SessionServiceCheckIDPIntent } from "./session-service-check-idp-intent.js";
import { SessionServiceCheckOTP } from "./session-service-check-otp.js";
import { SessionServiceCheckPassword } from "./session-service-check-password.js";
import { SessionServiceCheckRecoveryCode } from "./session-service-check-recovery-code.js";
import { SessionServiceCheckTOTP } from "./session-service-check-totp.js";
import { SessionServiceCheckUser } from "./session-service-check-user.js";
import { SessionServiceCheckWebAuthN } from "./session-service-check-web-auth-n.js";
import { Expose, Type } from "class-transformer";

export class SessionServiceChecks {
  /** @example null */
  @Expose({ name: "user" })
  @Type(() => SessionServiceCheckUser)
  user?: SessionServiceCheckUser;
  /** @example null */
  @Expose({ name: "password" })
  @Type(() => SessionServiceCheckPassword)
  password?: SessionServiceCheckPassword;
  /** @example null */
  @Expose({ name: "webAuthN" })
  @Type(() => SessionServiceCheckWebAuthN)
  webAuthN?: SessionServiceCheckWebAuthN;
  /** @example null */
  @Expose({ name: "idpIntent" })
  @Type(() => SessionServiceCheckIDPIntent)
  idpIntent?: SessionServiceCheckIDPIntent;
  /** @example null */
  @Expose({ name: "totp" })
  @Type(() => SessionServiceCheckTOTP)
  totp?: SessionServiceCheckTOTP;
  /** @example null */
  @Expose({ name: "otpSms" })
  @Type(() => SessionServiceCheckOTP)
  otpSms?: SessionServiceCheckOTP;
  /** @example null */
  @Expose({ name: "otpEmail" })
  @Type(() => SessionServiceCheckOTP)
  otpEmail?: SessionServiceCheckOTP;
  /** @example null */
  @Expose({ name: "recoveryCode" })
  @Type(() => SessionServiceCheckRecoveryCode)
  recoveryCode?: SessionServiceCheckRecoveryCode;

  constructor(data?: Partial<SessionServiceChecks>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceChecks)) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.user,
        (other as SessionServiceChecks).user,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.password,
        (other as SessionServiceChecks).password,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.webAuthN,
        (other as SessionServiceChecks).webAuthN,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.idpIntent,
        (other as SessionServiceChecks).idpIntent,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.totp,
        (other as SessionServiceChecks).totp,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.otpSms,
        (other as SessionServiceChecks).otpSms,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.otpEmail,
        (other as SessionServiceChecks).otpEmail,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceChecks.__deepEquals(
        this.recoveryCode,
        (other as SessionServiceChecks).recoveryCode,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => SessionServiceChecks.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceChecks.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !SessionServiceChecks.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceChecks.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
