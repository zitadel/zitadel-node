// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaProjectServiceAdmin } from "./beta-project-service-admin.js";
import { BetaProjectServicePrivateLabelingSetting } from "./beta-project-service-private-labeling-setting.js";
import { Expose, Type } from "class-transformer";

export class BetaProjectServiceCreateProjectRequest {
  /**
   * The unique identifier of the organization the project belongs to.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * The unique identifier of the project.
   * @example null
   */
  @Expose({ name: "id" })
  id?: string;
  /**
   * Name of the project.
   * @example null
   */
  @Expose({ name: "name" })
  name?: string;
  /**
   * Enable this setting to provide role information to your application. For OpenID Connect, the roles can be requested from the UserInfo endpoint or sent in the ID or Access Token, based on your application's configuration.
   * @example null
   */
  @Expose({ name: "projectRoleAssertion" })
  projectRoleAssertion?: boolean;
  /**
   * Deny authentication if the user has no authorization assigned to this project. Authorizations to the project without assigned a specific role to the user are allowed.
   * @example null
   */
  @Expose({ name: "authorizationRequired" })
  authorizationRequired?: boolean;
  /**
   * Before a user can be authenticated, it is verified that their affiliated organization has been granted access to this project. Authentication is not permitted for users from unauthorized organizations.
   * @example null
   */
  @Expose({ name: "projectAccessRequired" })
  projectAccessRequired?: boolean;
  /** @example null */
  @Expose({ name: "privateLabelingSetting" })
  privateLabelingSetting?: BetaProjectServicePrivateLabelingSetting;
  /**
   * List of users and Project Member roles (PROJECT_OWNER, by default) to be assigned to those users.
   * @example null
   */
  @Expose({ name: "admins" })
  @Type(() => BetaProjectServiceAdmin)
  admins?: Array<BetaProjectServiceAdmin>;

  constructor(data?: Partial<BetaProjectServiceCreateProjectRequest>) {
    Object.assign(this, data);
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (this.id != null && typeof this.id !== "string") {
      throw new TypeError(`id must be a string, got ${typeof this.id}`);
    }
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
    if (
      this.projectRoleAssertion != null &&
      typeof this.projectRoleAssertion !== "boolean"
    ) {
      throw new TypeError(
        `projectRoleAssertion must be a boolean, got ${typeof this.projectRoleAssertion}`,
      );
    }
    if (
      this.authorizationRequired != null &&
      typeof this.authorizationRequired !== "boolean"
    ) {
      throw new TypeError(
        `authorizationRequired must be a boolean, got ${typeof this.authorizationRequired}`,
      );
    }
    if (
      this.projectAccessRequired != null &&
      typeof this.projectAccessRequired !== "boolean"
    ) {
      throw new TypeError(
        `projectAccessRequired must be a boolean, got ${typeof this.projectAccessRequired}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaProjectServiceCreateProjectRequest)) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.organizationId,
        (other as BetaProjectServiceCreateProjectRequest).organizationId,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.id,
        (other as BetaProjectServiceCreateProjectRequest).id,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.name,
        (other as BetaProjectServiceCreateProjectRequest).name,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.projectRoleAssertion,
        (other as BetaProjectServiceCreateProjectRequest).projectRoleAssertion,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.authorizationRequired,
        (other as BetaProjectServiceCreateProjectRequest).authorizationRequired,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.projectAccessRequired,
        (other as BetaProjectServiceCreateProjectRequest).projectAccessRequired,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.privateLabelingSetting,
        (other as BetaProjectServiceCreateProjectRequest)
          .privateLabelingSetting,
      )
    ) {
      return false;
    }
    if (
      !BetaProjectServiceCreateProjectRequest.__deepEquals(
        this.admins,
        (other as BetaProjectServiceCreateProjectRequest).admins,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaProjectServiceCreateProjectRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaProjectServiceCreateProjectRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaProjectServiceCreateProjectRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaProjectServiceCreateProjectRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
