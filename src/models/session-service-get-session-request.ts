// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class SessionServiceGetSessionRequest {
  /**
   * The unique identifier of the session to be retrieved.
   * @example null
   */
  @Expose({ name: "sessionId" })
  sessionId?: string;
  /**
   * The current token of the session, previously returned on the create / update request.  The token is required unless either of the following conditions is met:  - the caller created the session  - the authenticated user requests their own session (checked user)  - the security token provided in the authorization header has the same user agent as the session  - the caller is granted the permission session.read permission on either the instance or on the checked user's organization
   * @example null
   */
  @Expose({ name: "sessionToken" })
  sessionToken?: string;

  constructor(data?: Partial<SessionServiceGetSessionRequest>) {
    Object.assign(this, data);
    if (this.sessionId != null && typeof this.sessionId !== "string") {
      throw new TypeError(
        `sessionId must be a string, got ${typeof this.sessionId}`,
      );
    }
    if (this.sessionToken != null && typeof this.sessionToken !== "string") {
      throw new TypeError(
        `sessionToken must be a string, got ${typeof this.sessionToken}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SessionServiceGetSessionRequest)) {
      return false;
    }
    if (
      !SessionServiceGetSessionRequest.__deepEquals(
        this.sessionId,
        (other as SessionServiceGetSessionRequest).sessionId,
      )
    ) {
      return false;
    }
    if (
      !SessionServiceGetSessionRequest.__deepEquals(
        this.sessionToken,
        (other as SessionServiceGetSessionRequest).sessionToken,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SessionServiceGetSessionRequest.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SessionServiceGetSessionRequest.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SessionServiceGetSessionRequest.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SessionServiceGetSessionRequest.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
