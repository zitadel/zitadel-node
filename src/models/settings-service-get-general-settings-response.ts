// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class SettingsServiceGetGeneralSettingsResponse {
  /**
   * The unique identifier of the default organization.  The default organization is used to assign new users to an organization if no other organization is specified.  Deprecated: use default_organization_id instead.
   * @example null
   */
  @Expose({ name: "defaultOrgId" })
  defaultOrgId?: string;
  /**
   * The default language is use if no other language is specified or detected.  The format is a BCP 47 language tag (e.g. \"en\", \"de\", \"fr-CH\").
   * @example null
   */
  @Expose({ name: "defaultLanguage" })
  defaultLanguage?: string;
  /**
   * The list of supported languages.  Note that the instance might restrict the languages further  only allowing a subset of these languages to be used.  The format is a BCP 47 language tag (e.g. \"en\", \"de\", \"fr-CH\").
   * @example null
   */
  @Expose({ name: "supportedLanguages" })
  supportedLanguages?: Array<string>;
  /**
   * The unique identifier of the default organization.  The default organization is used to assign new users to an organization if no other organization is specified.
   * @example null
   */
  @Expose({ name: "defaultOrganizationId" })
  defaultOrganizationId?: string;
  /**
   * The list of allowed languages for the instance.  This is a subset of the supported languages to be used in the instance  e.g. for user selection during registration or language detection in the UI.  The format is a BCP 47 language tag (e.g. \"en\", \"de\", \"fr-CH\").
   * @example null
   */
  @Expose({ name: "allowedLanguages" })
  allowedLanguages?: Array<string>;

  constructor(data?: Partial<SettingsServiceGetGeneralSettingsResponse>) {
    Object.assign(this, data);
    if (this.defaultOrgId != null && typeof this.defaultOrgId !== "string") {
      throw new TypeError(
        `defaultOrgId must be a string, got ${typeof this.defaultOrgId}`,
      );
    }
    if (
      this.defaultLanguage != null &&
      typeof this.defaultLanguage !== "string"
    ) {
      throw new TypeError(
        `defaultLanguage must be a string, got ${typeof this.defaultLanguage}`,
      );
    }
    if (
      this.supportedLanguages != null &&
      !Array.isArray(this.supportedLanguages) &&
      !((this.supportedLanguages as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `supportedLanguages must be an array, got ${typeof this.supportedLanguages}`,
      );
    }
    if (
      this.defaultOrganizationId != null &&
      typeof this.defaultOrganizationId !== "string"
    ) {
      throw new TypeError(
        `defaultOrganizationId must be a string, got ${typeof this.defaultOrganizationId}`,
      );
    }
    if (
      this.allowedLanguages != null &&
      !Array.isArray(this.allowedLanguages) &&
      !((this.allowedLanguages as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `allowedLanguages must be an array, got ${typeof this.allowedLanguages}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SettingsServiceGetGeneralSettingsResponse)) {
      return false;
    }
    if (
      !SettingsServiceGetGeneralSettingsResponse.__deepEquals(
        this.defaultOrgId,
        (other as SettingsServiceGetGeneralSettingsResponse).defaultOrgId,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetGeneralSettingsResponse.__deepEquals(
        this.defaultLanguage,
        (other as SettingsServiceGetGeneralSettingsResponse).defaultLanguage,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetGeneralSettingsResponse.__deepEquals(
        this.supportedLanguages,
        (other as SettingsServiceGetGeneralSettingsResponse).supportedLanguages,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetGeneralSettingsResponse.__deepEquals(
        this.defaultOrganizationId,
        (other as SettingsServiceGetGeneralSettingsResponse)
          .defaultOrganizationId,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceGetGeneralSettingsResponse.__deepEquals(
        this.allowedLanguages,
        (other as SettingsServiceGetGeneralSettingsResponse).allowedLanguages,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SettingsServiceGetGeneralSettingsResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          SettingsServiceGetGeneralSettingsResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SettingsServiceGetGeneralSettingsResponse.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SettingsServiceGetGeneralSettingsResponse.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
