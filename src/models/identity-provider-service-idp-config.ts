// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { IdentityProviderServiceAppleConfig } from "./identity-provider-service-apple-config.js";
import { IdentityProviderServiceAzureADConfig } from "./identity-provider-service-azure-ad-config.js";
import { IdentityProviderServiceGenericOIDCConfig } from "./identity-provider-service-generic-oidc-config.js";
import { IdentityProviderServiceGitHubConfig } from "./identity-provider-service-git-hub-config.js";
import { IdentityProviderServiceGitHubEnterpriseServerConfig } from "./identity-provider-service-git-hub-enterprise-server-config.js";
import { IdentityProviderServiceGitLabConfig } from "./identity-provider-service-git-lab-config.js";
import { IdentityProviderServiceGitLabSelfHostedConfig } from "./identity-provider-service-git-lab-self-hosted-config.js";
import { IdentityProviderServiceGoogleConfig } from "./identity-provider-service-google-config.js";
import { IdentityProviderServiceJWTConfig } from "./identity-provider-service-jwt-config.js";
import { IdentityProviderServiceLDAPConfig } from "./identity-provider-service-ldap-config.js";
import { IdentityProviderServiceOAuthConfig } from "./identity-provider-service-o-auth-config.js";
import { IdentityProviderServiceOptions } from "./identity-provider-service-options.js";
import { IdentityProviderServiceSAMLConfig } from "./identity-provider-service-saml-config.js";
import { Expose, Type } from "class-transformer";

export class IdentityProviderServiceIDPConfig {
  /** @example null */
  @Expose({ name: "options" })
  @Type(() => IdentityProviderServiceOptions)
  options?: IdentityProviderServiceOptions;
  /** @example null */
  @Expose({ name: "apple" })
  @Type(() => IdentityProviderServiceAppleConfig)
  apple?: IdentityProviderServiceAppleConfig;
  /** @example null */
  @Expose({ name: "azureAd" })
  @Type(() => IdentityProviderServiceAzureADConfig)
  azureAd?: IdentityProviderServiceAzureADConfig;
  /** @example null */
  @Expose({ name: "github" })
  @Type(() => IdentityProviderServiceGitHubConfig)
  github?: IdentityProviderServiceGitHubConfig;
  /** @example null */
  @Expose({ name: "githubEs" })
  @Type(() => IdentityProviderServiceGitHubEnterpriseServerConfig)
  githubEs?: IdentityProviderServiceGitHubEnterpriseServerConfig;
  /** @example null */
  @Expose({ name: "gitlab" })
  @Type(() => IdentityProviderServiceGitLabConfig)
  gitlab?: IdentityProviderServiceGitLabConfig;
  /** @example null */
  @Expose({ name: "gitlabSelfHosted" })
  @Type(() => IdentityProviderServiceGitLabSelfHostedConfig)
  gitlabSelfHosted?: IdentityProviderServiceGitLabSelfHostedConfig;
  /** @example null */
  @Expose({ name: "google" })
  @Type(() => IdentityProviderServiceGoogleConfig)
  google?: IdentityProviderServiceGoogleConfig;
  /** @example null */
  @Expose({ name: "jwt" })
  @Type(() => IdentityProviderServiceJWTConfig)
  jwt?: IdentityProviderServiceJWTConfig;
  /** @example null */
  @Expose({ name: "ldap" })
  @Type(() => IdentityProviderServiceLDAPConfig)
  ldap?: IdentityProviderServiceLDAPConfig;
  /** @example null */
  @Expose({ name: "oauth" })
  @Type(() => IdentityProviderServiceOAuthConfig)
  oauth?: IdentityProviderServiceOAuthConfig;
  /** @example null */
  @Expose({ name: "oidc" })
  @Type(() => IdentityProviderServiceGenericOIDCConfig)
  oidc?: IdentityProviderServiceGenericOIDCConfig;
  /** @example null */
  @Expose({ name: "saml" })
  @Type(() => IdentityProviderServiceSAMLConfig)
  saml?: IdentityProviderServiceSAMLConfig;

  constructor(data?: Partial<IdentityProviderServiceIDPConfig>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof IdentityProviderServiceIDPConfig)) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.options,
        (other as IdentityProviderServiceIDPConfig).options,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.apple,
        (other as IdentityProviderServiceIDPConfig).apple,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.azureAd,
        (other as IdentityProviderServiceIDPConfig).azureAd,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.github,
        (other as IdentityProviderServiceIDPConfig).github,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.githubEs,
        (other as IdentityProviderServiceIDPConfig).githubEs,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.gitlab,
        (other as IdentityProviderServiceIDPConfig).gitlab,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.gitlabSelfHosted,
        (other as IdentityProviderServiceIDPConfig).gitlabSelfHosted,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.google,
        (other as IdentityProviderServiceIDPConfig).google,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.jwt,
        (other as IdentityProviderServiceIDPConfig).jwt,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.ldap,
        (other as IdentityProviderServiceIDPConfig).ldap,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.oauth,
        (other as IdentityProviderServiceIDPConfig).oauth,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.oidc,
        (other as IdentityProviderServiceIDPConfig).oidc,
      )
    ) {
      return false;
    }
    if (
      !IdentityProviderServiceIDPConfig.__deepEquals(
        this.saml,
        (other as IdentityProviderServiceIDPConfig).saml,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        IdentityProviderServiceIDPConfig.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => IdentityProviderServiceIDPConfig.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !IdentityProviderServiceIDPConfig.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          IdentityProviderServiceIDPConfig.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
