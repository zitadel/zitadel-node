// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { SettingsServiceResourceOwnerType } from "./settings-service-resource-owner-type.js";
import { SettingsServiceTheme } from "./settings-service-theme.js";
import { SettingsServiceThemeMode } from "./settings-service-theme-mode.js";
import { Expose, Type } from "class-transformer";

export class SettingsServiceBrandingSettings {
  /** @example null */
  @Expose({ name: "lightTheme" })
  @Type(() => SettingsServiceTheme)
  lightTheme?: SettingsServiceTheme;
  /** @example null */
  @Expose({ name: "darkTheme" })
  @Type(() => SettingsServiceTheme)
  darkTheme?: SettingsServiceTheme;
  /**
   * The url where the font is served.
   * @example null
   */
  @Expose({ name: "fontUrl" })
  fontUrl?: string;
  /**
   * If enabled, the organization suffix will be hidden on the login form if the scope \\\"urn:zitadel:iam:org:domain:primary:{domainname}\\\" is used.
   * @example null
   */
  @Expose({ name: "hideLoginNameSuffix" })
  hideLoginNameSuffix?: boolean;
  /**
   * If enabled, the Zitadel logo will not be displayed on the login screen.
   * @example null
   */
  @Expose({ name: "disableWatermark" })
  disableWatermark?: boolean;
  /** @example null */
  @Expose({ name: "resourceOwnerType" })
  resourceOwnerType?: SettingsServiceResourceOwnerType;
  /** @example null */
  @Expose({ name: "themeMode" })
  themeMode?: SettingsServiceThemeMode;

  constructor(data?: Partial<SettingsServiceBrandingSettings>) {
    Object.assign(this, data);
    if (this.fontUrl != null && typeof this.fontUrl !== "string") {
      throw new TypeError(
        `fontUrl must be a string, got ${typeof this.fontUrl}`,
      );
    }
    if (
      this.hideLoginNameSuffix != null &&
      typeof this.hideLoginNameSuffix !== "boolean"
    ) {
      throw new TypeError(
        `hideLoginNameSuffix must be a boolean, got ${typeof this.hideLoginNameSuffix}`,
      );
    }
    if (
      this.disableWatermark != null &&
      typeof this.disableWatermark !== "boolean"
    ) {
      throw new TypeError(
        `disableWatermark must be a boolean, got ${typeof this.disableWatermark}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof SettingsServiceBrandingSettings)) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.lightTheme,
        (other as SettingsServiceBrandingSettings).lightTheme,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.darkTheme,
        (other as SettingsServiceBrandingSettings).darkTheme,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.fontUrl,
        (other as SettingsServiceBrandingSettings).fontUrl,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.hideLoginNameSuffix,
        (other as SettingsServiceBrandingSettings).hideLoginNameSuffix,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.disableWatermark,
        (other as SettingsServiceBrandingSettings).disableWatermark,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.resourceOwnerType,
        (other as SettingsServiceBrandingSettings).resourceOwnerType,
      )
    ) {
      return false;
    }
    if (
      !SettingsServiceBrandingSettings.__deepEquals(
        this.themeMode,
        (other as SettingsServiceBrandingSettings).themeMode,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        SettingsServiceBrandingSettings.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => SettingsServiceBrandingSettings.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !SettingsServiceBrandingSettings.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          SettingsServiceBrandingSettings.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
