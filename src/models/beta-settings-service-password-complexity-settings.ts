// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaSettingsServiceResourceOwnerType } from "./beta-settings-service-resource-owner-type.js";
import { Expose } from "class-transformer";

export class BetaSettingsServicePasswordComplexitySettings {
  /** @example null */
  @Expose({ name: "minLength" })
  minLength?: unknown;
  /** @example null */
  @Expose({ name: "requiresUppercase" })
  requiresUppercase?: boolean;
  /** @example null */
  @Expose({ name: "requiresLowercase" })
  requiresLowercase?: boolean;
  /** @example null */
  @Expose({ name: "requiresNumber" })
  requiresNumber?: boolean;
  /** @example null */
  @Expose({ name: "requiresSymbol" })
  requiresSymbol?: boolean;
  /** @example null */
  @Expose({ name: "resourceOwnerType" })
  resourceOwnerType?: BetaSettingsServiceResourceOwnerType;

  constructor(data?: Partial<BetaSettingsServicePasswordComplexitySettings>) {
    Object.assign(this, data);
    if (
      this.requiresUppercase != null &&
      typeof this.requiresUppercase !== "boolean"
    ) {
      throw new TypeError(
        `requiresUppercase must be a boolean, got ${typeof this.requiresUppercase}`,
      );
    }
    if (
      this.requiresLowercase != null &&
      typeof this.requiresLowercase !== "boolean"
    ) {
      throw new TypeError(
        `requiresLowercase must be a boolean, got ${typeof this.requiresLowercase}`,
      );
    }
    if (
      this.requiresNumber != null &&
      typeof this.requiresNumber !== "boolean"
    ) {
      throw new TypeError(
        `requiresNumber must be a boolean, got ${typeof this.requiresNumber}`,
      );
    }
    if (
      this.requiresSymbol != null &&
      typeof this.requiresSymbol !== "boolean"
    ) {
      throw new TypeError(
        `requiresSymbol must be a boolean, got ${typeof this.requiresSymbol}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaSettingsServicePasswordComplexitySettings)) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.minLength,
        (other as BetaSettingsServicePasswordComplexitySettings).minLength,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.requiresUppercase,
        (other as BetaSettingsServicePasswordComplexitySettings)
          .requiresUppercase,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.requiresLowercase,
        (other as BetaSettingsServicePasswordComplexitySettings)
          .requiresLowercase,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.requiresNumber,
        (other as BetaSettingsServicePasswordComplexitySettings).requiresNumber,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.requiresSymbol,
        (other as BetaSettingsServicePasswordComplexitySettings).requiresSymbol,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
        this.resourceOwnerType,
        (other as BetaSettingsServicePasswordComplexitySettings)
          .resourceOwnerType,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaSettingsServicePasswordComplexitySettings.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaSettingsServicePasswordComplexitySettings.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaSettingsServicePasswordComplexitySettings.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaSettingsServicePasswordComplexitySettings.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
