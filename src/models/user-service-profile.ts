// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { UserServiceGender } from "./user-service-gender.js";
import { Expose } from "class-transformer";

export class UserServiceProfile {
  /**
   * The given name is the first name of the user.  For example, it can be used to personalize notifications and login UIs.
   * @example null
   */
  @Expose({ name: "givenName" })
  givenName?: string;
  /**
   * The family name is the last name of the user.  For example, it can be used to personalize user interfaces and notifications.
   * @example null
   */
  @Expose({ name: "familyName" })
  familyName?: string;
  /**
   * The nick name is the users short name.  For example, it can be used to personalize user interfaces and notifications.
   * @example null
   */
  @Expose({ name: "nickName" })
  nickName?: string;
  /**
   * The display name is how a user should primarily be displayed in lists.  It can also for example be used to personalize user interfaces and notifications.
   * @example null
   */
  @Expose({ name: "displayName" })
  displayName?: string;
  /**
   * The users preferred language is the language that systems should use to interact with the user.  It has the format of a [BCP-47 language tag](https://datatracker.ietf.org/doc/html/rfc3066).  It is used by Zitadel where no higher prioritized preferred language can be used.  For example, browser settings can overwrite a users preferred_language.  Notification messages and standard login UIs use the users preferred language if it is supported and allowed on the instance.  Else, the default language of the instance is used.
   * @example null
   */
  @Expose({ name: "preferredLanguage" })
  preferredLanguage?: string;
  /** @example null */
  @Expose({ name: "gender" })
  gender?: UserServiceGender;

  constructor(data?: Partial<UserServiceProfile>) {
    Object.assign(this, data);
    if (this.givenName != null && typeof this.givenName !== "string") {
      throw new TypeError(
        `givenName must be a string, got ${typeof this.givenName}`,
      );
    }
    if (this.familyName != null && typeof this.familyName !== "string") {
      throw new TypeError(
        `familyName must be a string, got ${typeof this.familyName}`,
      );
    }
    if (this.nickName != null && typeof this.nickName !== "string") {
      throw new TypeError(
        `nickName must be a string, got ${typeof this.nickName}`,
      );
    }
    if (this.displayName != null && typeof this.displayName !== "string") {
      throw new TypeError(
        `displayName must be a string, got ${typeof this.displayName}`,
      );
    }
    if (
      this.preferredLanguage != null &&
      typeof this.preferredLanguage !== "string"
    ) {
      throw new TypeError(
        `preferredLanguage must be a string, got ${typeof this.preferredLanguage}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof UserServiceProfile)) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.givenName,
        (other as UserServiceProfile).givenName,
      )
    ) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.familyName,
        (other as UserServiceProfile).familyName,
      )
    ) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.nickName,
        (other as UserServiceProfile).nickName,
      )
    ) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.displayName,
        (other as UserServiceProfile).displayName,
      )
    ) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.preferredLanguage,
        (other as UserServiceProfile).preferredLanguage,
      )
    ) {
      return false;
    }
    if (
      !UserServiceProfile.__deepEquals(
        this.gender,
        (other as UserServiceProfile).gender,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => UserServiceProfile.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => UserServiceProfile.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !UserServiceProfile.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          UserServiceProfile.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
