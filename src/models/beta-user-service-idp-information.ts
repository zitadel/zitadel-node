// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaUserServiceIDPLDAPAccessInformation } from "./beta-user-service-idpldap-access-information.js";
import { BetaUserServiceIDPOAuthAccessInformation } from "./beta-user-service-idpo-auth-access-information.js";
import { BetaUserServiceIDPSAMLAccessInformation } from "./beta-user-service-idpsaml-access-information.js";
import { Expose, Type } from "class-transformer";

export class BetaUserServiceIDPInformation {
  /** @example null */
  @Expose({ name: "idpId" })
  idpId?: string;
  /** @example null */
  @Expose({ name: "userId" })
  userId?: string;
  /** @example null */
  @Expose({ name: "userName" })
  userName?: string;
  /**
   * `Struct` represents a structured data value, consisting of fields  which map to dynamically typed values. In some languages, `Struct`  might be supported by a native representation. For example, in  scripting languages like JS a struct is represented as an  object. The details of that representation are described together  with the proto support for the language.   The JSON representation for `Struct` is JSON object.
   * @example null
   */
  @Expose({ name: "rawInformation" })
  rawInformation?: { [key: string]: unknown };
  /** @example null */
  @Expose({ name: "ldap" })
  @Type(() => BetaUserServiceIDPLDAPAccessInformation)
  ldap?: BetaUserServiceIDPLDAPAccessInformation;
  /** @example null */
  @Expose({ name: "oauth" })
  @Type(() => BetaUserServiceIDPOAuthAccessInformation)
  oauth?: BetaUserServiceIDPOAuthAccessInformation;
  /** @example null */
  @Expose({ name: "saml" })
  @Type(() => BetaUserServiceIDPSAMLAccessInformation)
  saml?: BetaUserServiceIDPSAMLAccessInformation;

  constructor(data?: Partial<BetaUserServiceIDPInformation>) {
    Object.assign(this, data);
    if (this.idpId != null && typeof this.idpId !== "string") {
      throw new TypeError(`idpId must be a string, got ${typeof this.idpId}`);
    }
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.userName != null && typeof this.userName !== "string") {
      throw new TypeError(
        `userName must be a string, got ${typeof this.userName}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaUserServiceIDPInformation)) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.idpId,
        (other as BetaUserServiceIDPInformation).idpId,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.userId,
        (other as BetaUserServiceIDPInformation).userId,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.userName,
        (other as BetaUserServiceIDPInformation).userName,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.rawInformation,
        (other as BetaUserServiceIDPInformation).rawInformation,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.ldap,
        (other as BetaUserServiceIDPInformation).ldap,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.oauth,
        (other as BetaUserServiceIDPInformation).oauth,
      )
    ) {
      return false;
    }
    if (
      !BetaUserServiceIDPInformation.__deepEquals(
        this.saml,
        (other as BetaUserServiceIDPInformation).saml,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaUserServiceIDPInformation.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaUserServiceIDPInformation.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaUserServiceIDPInformation.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaUserServiceIDPInformation.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
