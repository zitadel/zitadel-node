// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { UserServiceDetails } from "./user-service-details.js";
import { Expose, Type } from "class-transformer";

export class UserServiceRegisterPasskeyResponse {
  /** @example null */
  @Expose({ name: "details" })
  @Type(() => UserServiceDetails)
  details?: UserServiceDetails;
  /** @example null */
  @Expose({ name: "passkeyId" })
  passkeyId?: string;
  /**
   * `Struct` represents a structured data value, consisting of fields  which map to dynamically typed values. In some languages, `Struct`  might be supported by a native representation. For example, in  scripting languages like JS a struct is represented as an  object. The details of that representation are described together  with the proto support for the language.   The JSON representation for `Struct` is JSON object.
   * @example null
   */
  @Expose({ name: "publicKeyCredentialCreationOptions" })
  publicKeyCredentialCreationOptions?: { [key: string]: unknown };

  constructor(data?: Partial<UserServiceRegisterPasskeyResponse>) {
    Object.assign(this, data);
    if (this.passkeyId != null && typeof this.passkeyId !== "string") {
      throw new TypeError(
        `passkeyId must be a string, got ${typeof this.passkeyId}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof UserServiceRegisterPasskeyResponse)) {
      return false;
    }
    if (
      !UserServiceRegisterPasskeyResponse.__deepEquals(
        this.details,
        (other as UserServiceRegisterPasskeyResponse).details,
      )
    ) {
      return false;
    }
    if (
      !UserServiceRegisterPasskeyResponse.__deepEquals(
        this.passkeyId,
        (other as UserServiceRegisterPasskeyResponse).passkeyId,
      )
    ) {
      return false;
    }
    if (
      !UserServiceRegisterPasskeyResponse.__deepEquals(
        this.publicKeyCredentialCreationOptions,
        (other as UserServiceRegisterPasskeyResponse)
          .publicKeyCredentialCreationOptions,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        UserServiceRegisterPasskeyResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          UserServiceRegisterPasskeyResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !UserServiceRegisterPasskeyResponse.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          UserServiceRegisterPasskeyResponse.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
