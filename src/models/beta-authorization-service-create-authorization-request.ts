// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class BetaAuthorizationServiceCreateAuthorizationRequest {
  /**
   * UserID is the ID of the user who should be granted the authorization.
   * @example null
   */
  @Expose({ name: "userId" })
  userId?: string;
  /**
   * Project ID is the ID of the project the user should be authorized for.
   * @example null
   */
  @Expose({ name: "projectId" })
  projectId?: string;
  /**
   * OrganizationID is the ID of the organization on which the authorization should be created.  The organization must either own the project or have a grant for the project.  If omitted, the authorization is created on the projects organization.
   * @example null
   */
  @Expose({ name: "organizationId" })
  organizationId?: string;
  /**
   * RoleKeys are the keys of the roles the user should be granted.
   * @example null
   */
  @Expose({ name: "roleKeys" })
  roleKeys?: Array<string>;

  constructor(
    data?: Partial<BetaAuthorizationServiceCreateAuthorizationRequest>,
  ) {
    Object.assign(this, data);
    if (this.userId != null && typeof this.userId !== "string") {
      throw new TypeError(`userId must be a string, got ${typeof this.userId}`);
    }
    if (this.projectId != null && typeof this.projectId !== "string") {
      throw new TypeError(
        `projectId must be a string, got ${typeof this.projectId}`,
      );
    }
    if (
      this.organizationId != null &&
      typeof this.organizationId !== "string"
    ) {
      throw new TypeError(
        `organizationId must be a string, got ${typeof this.organizationId}`,
      );
    }
    if (
      this.roleKeys != null &&
      !Array.isArray(this.roleKeys) &&
      !((this.roleKeys as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `roleKeys must be an array, got ${typeof this.roleKeys}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (
      !(other instanceof BetaAuthorizationServiceCreateAuthorizationRequest)
    ) {
      return false;
    }
    if (
      !BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
        this.userId,
        (other as BetaAuthorizationServiceCreateAuthorizationRequest).userId,
      )
    ) {
      return false;
    }
    if (
      !BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
        this.projectId,
        (other as BetaAuthorizationServiceCreateAuthorizationRequest).projectId,
      )
    ) {
      return false;
    }
    if (
      !BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
        this.organizationId,
        (other as BetaAuthorizationServiceCreateAuthorizationRequest)
          .organizationId,
      )
    ) {
      return false;
    }
    if (
      !BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
        this.roleKeys,
        (other as BetaAuthorizationServiceCreateAuthorizationRequest).roleKeys,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
          v,
          b[i],
        ),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
            v,
            b.get(k),
          )
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaAuthorizationServiceCreateAuthorizationRequest.__deepEquals(
            ar[k],
            br[k],
          ),
      );
    }
    return false;
  }
}
