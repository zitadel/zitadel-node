/* tslint:disable */
/* eslint-disable */
/**
 * Zitadel SDK
 * The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { SettingsServiceResourceOwnerType } from './settings-service-resource-owner-type.js';
import {
    SettingsServiceResourceOwnerTypeFromJSON,
    SettingsServiceResourceOwnerTypeFromJSONTyped,
    SettingsServiceResourceOwnerTypeToJSON,
    SettingsServiceResourceOwnerTypeToJSONTyped,
} from './settings-service-resource-owner-type.js';
import type { SettingsServiceMultiFactorType } from './settings-service-multi-factor-type.js';
import {
    SettingsServiceMultiFactorTypeFromJSON,
    SettingsServiceMultiFactorTypeFromJSONTyped,
    SettingsServiceMultiFactorTypeToJSON,
    SettingsServiceMultiFactorTypeToJSONTyped,
} from './settings-service-multi-factor-type.js';
import type { SettingsServicePasskeysType } from './settings-service-passkeys-type.js';
import {
    SettingsServicePasskeysTypeFromJSON,
    SettingsServicePasskeysTypeFromJSONTyped,
    SettingsServicePasskeysTypeToJSON,
    SettingsServicePasskeysTypeToJSONTyped,
} from './settings-service-passkeys-type.js';
import type { SettingsServiceSecondFactorType } from './settings-service-second-factor-type.js';
import {
    SettingsServiceSecondFactorTypeFromJSON,
    SettingsServiceSecondFactorTypeFromJSONTyped,
    SettingsServiceSecondFactorTypeToJSON,
    SettingsServiceSecondFactorTypeToJSONTyped,
} from './settings-service-second-factor-type.js';

/**
 * 
 * @export
 * @interface SettingsServiceLoginSettings
 */
export interface SettingsServiceLoginSettings {
    /**
     * If enabled, users can log in locally with their username and passkeys or password.
     *  Disabling this option will require users to log in with an external identity provider.
     *  Be sure to allow at least one external identity provider if this option is disabled.
     *  Deprecated: check allow_local_authentication instead.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     * @deprecated
     */
    allowUsernamePassword?: boolean;
    /**
     * If enabled, users can log in locally with their username and passkeys or password.
     *  Disabling this option will require users to log in with an external identity provider.
     *  Be sure to allow at least one external identity provider if this option is disabled.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    allowLocalAuthentication?: boolean;
    /**
     * If enabled, users can register a local account by themself.
     *  This option does not effect external identity providers.
     *  Each identity provider can be configured to allow or disallow registration.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    allowRegister?: boolean;
    /**
     * If enabled, users will generally be allowed to use an external identity provider to log in.
     *  Be sure to allow at least one external identity provider if this option is enabled.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    allowExternalIdp?: boolean;
    /**
     * If enabled, users will be forced to use a multi-factor to log in.
     *  This also applies to federated logins through an external identity provider.
     *  Users will be required to set up a second factor if they have not done so already.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    forceMfa?: boolean;
    /**
     * 
     * @type {SettingsServicePasskeysType}
     * @memberof SettingsServiceLoginSettings
     */
    passkeysType?: SettingsServicePasskeysType;
    /**
     * If enabled, the password reset link will be hidden on the login screen.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    hidePasswordReset?: boolean;
    /**
     * If enabled, an unknown username on the login screen will not return an error directly,
     *  but will always display the password screen.
     *  This prevents user enumeration attacks.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    ignoreUnknownUsernames?: boolean;
    /**
     * Defines where the user will be redirected to if the login is started without app context (e.g. from mail).
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    defaultRedirectUri?: string;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    passwordCheckLifetime?: string;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    externalLoginCheckLifetime?: string;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    mfaInitSkipLifetime?: string;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    secondFactorCheckLifetime?: string;
    /**
     * A Duration represents a signed, fixed-length span of time represented
     *  as a count of seconds and fractions of seconds at nanosecond
     *  resolution. It is independent of any calendar and concepts like "day"
     *  or "month". It is related to Timestamp in that the difference between
     *  two Timestamp values is a Duration and it can be added or subtracted
     *  from a Timestamp. Range is approximately +-10,000 years.
     * 
     *  # Examples
     * 
     *  Example 1: Compute Duration from two Timestamps in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Timestamp end = ...;
     *      Duration duration = ...;
     * 
     *      duration.seconds = end.seconds - start.seconds;
     *      duration.nanos = end.nanos - start.nanos;
     * 
     *      if (duration.seconds < 0 && duration.nanos > 0) {
     *        duration.seconds += 1;
     *        duration.nanos -= 1000000000;
     *      } else if (duration.seconds > 0 && duration.nanos < 0) {
     *        duration.seconds -= 1;
     *        duration.nanos += 1000000000;
     *      }
     * 
     *  Example 2: Compute Timestamp from Timestamp + Duration in pseudo code.
     * 
     *      Timestamp start = ...;
     *      Duration duration = ...;
     *      Timestamp end = ...;
     * 
     *      end.seconds = start.seconds + duration.seconds;
     *      end.nanos = start.nanos + duration.nanos;
     * 
     *      if (end.nanos < 0) {
     *        end.seconds -= 1;
     *        end.nanos += 1000000000;
     *      } else if (end.nanos >= 1000000000) {
     *        end.seconds += 1;
     *        end.nanos -= 1000000000;
     *      }
     * 
     *  Example 3: Compute Duration from datetime.timedelta in Python.
     * 
     *      td = datetime.timedelta(days=3, minutes=10)
     *      duration = Duration()
     *      duration.FromTimedelta(td)
     * 
     *  # JSON Mapping
     * 
     *  In JSON format, the Duration type is encoded as a string rather than an
     *  object, where the string ends in the suffix "s" (indicating seconds) and
     *  is preceded by the number of seconds, with nanoseconds expressed as
     *  fractional seconds. For example, 3 seconds with 0 nanoseconds should be
     *  encoded in JSON format as "3s", while 3 seconds and 1 nanosecond should
     *  be expressed in JSON format as "3.000000001s", and 3 seconds and 1
     *  microsecond should be expressed in JSON format as "3.000001s".
     * @type {string}
     * @memberof SettingsServiceLoginSettings
     */
    multiFactorCheckLifetime?: string;
    /**
     * The list of allowed second factors.
     * @type {Array<SettingsServiceSecondFactorType>}
     * @memberof SettingsServiceLoginSettings
     */
    secondFactors?: Array<SettingsServiceSecondFactorType>;
    /**
     * The list of allowed multi factors.
     * @type {Array<SettingsServiceMultiFactorType>}
     * @memberof SettingsServiceLoginSettings
     */
    multiFactors?: Array<SettingsServiceMultiFactorType>;
    /**
     * Allow discovery of the organization and its authentication option by domain.
     *  If set to true, the suffix (@domain.com) of an unknown username input on the login screen
     *  will be matched against the organization domains and will redirect to the registration of that organization on success.
     *  The registration can either be locally (requires allow_register to be true) or through an external identity provider.
     *  In case only one identity provider is configured for the organization, the user will be redirected directly to the identity provider.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    allowDomainDiscovery?: boolean;
    /**
     * By default, users can login with their verified email address additionally to their login name.
     *  Setting this to true disables the email login.
     *  Note: If the email is set as the login name, this setting has no effect.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    disableLoginWithEmail?: boolean;
    /**
     * By default, users can login with their verified phone number additionally to their login name.
     *  Setting this to true disables the phone number login.
     *  Note: If the phone number is set as the login name, this setting has no effect.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    disableLoginWithPhone?: boolean;
    /**
     * 
     * @type {SettingsServiceResourceOwnerType}
     * @memberof SettingsServiceLoginSettings
     */
    resourceOwnerType?: SettingsServiceResourceOwnerType;
    /**
     * If enabled, users will be forced to use a multi-factor to log in if they authenticated locally.
     *  This does not apply to federated logins through an external identity provider.
     *  Users will be required to set up a second factor if they have not done so already.
     *  If both force_mfa and force_mfa_local_only are enabled, force_mfa takes precedence and
     *  all logins will require a second factor.
     * @type {boolean}
     * @memberof SettingsServiceLoginSettings
     */
    forceMfaLocalOnly?: boolean;
}



/**
 * Check if a given object implements the SettingsServiceLoginSettings interface.
 */
export function instanceOfSettingsServiceLoginSettings(value: object): value is SettingsServiceLoginSettings {
    return true;
}

export function SettingsServiceLoginSettingsFromJSON(json: any): SettingsServiceLoginSettings {
    return SettingsServiceLoginSettingsFromJSONTyped(json, false);
}

export function SettingsServiceLoginSettingsFromJSONTyped(json: any, ignoreDiscriminator: boolean): SettingsServiceLoginSettings {
    if (json == null) {
        return json;
    }
    return {
        
        'allowUsernamePassword': json['allowUsernamePassword'] == null ? undefined : json['allowUsernamePassword'],
        'allowLocalAuthentication': json['allowLocalAuthentication'] == null ? undefined : json['allowLocalAuthentication'],
        'allowRegister': json['allowRegister'] == null ? undefined : json['allowRegister'],
        'allowExternalIdp': json['allowExternalIdp'] == null ? undefined : json['allowExternalIdp'],
        'forceMfa': json['forceMfa'] == null ? undefined : json['forceMfa'],
        'passkeysType': json['passkeysType'] == null ? undefined : SettingsServicePasskeysTypeFromJSON(json['passkeysType']),
        'hidePasswordReset': json['hidePasswordReset'] == null ? undefined : json['hidePasswordReset'],
        'ignoreUnknownUsernames': json['ignoreUnknownUsernames'] == null ? undefined : json['ignoreUnknownUsernames'],
        'defaultRedirectUri': json['defaultRedirectUri'] == null ? undefined : json['defaultRedirectUri'],
        'passwordCheckLifetime': json['passwordCheckLifetime'] == null ? undefined : json['passwordCheckLifetime'],
        'externalLoginCheckLifetime': json['externalLoginCheckLifetime'] == null ? undefined : json['externalLoginCheckLifetime'],
        'mfaInitSkipLifetime': json['mfaInitSkipLifetime'] == null ? undefined : json['mfaInitSkipLifetime'],
        'secondFactorCheckLifetime': json['secondFactorCheckLifetime'] == null ? undefined : json['secondFactorCheckLifetime'],
        'multiFactorCheckLifetime': json['multiFactorCheckLifetime'] == null ? undefined : json['multiFactorCheckLifetime'],
        'secondFactors': json['secondFactors'] == null ? undefined : ((json['secondFactors'] as Array<any>).map(SettingsServiceSecondFactorTypeFromJSON)),
        'multiFactors': json['multiFactors'] == null ? undefined : ((json['multiFactors'] as Array<any>).map(SettingsServiceMultiFactorTypeFromJSON)),
        'allowDomainDiscovery': json['allowDomainDiscovery'] == null ? undefined : json['allowDomainDiscovery'],
        'disableLoginWithEmail': json['disableLoginWithEmail'] == null ? undefined : json['disableLoginWithEmail'],
        'disableLoginWithPhone': json['disableLoginWithPhone'] == null ? undefined : json['disableLoginWithPhone'],
        'resourceOwnerType': json['resourceOwnerType'] == null ? undefined : SettingsServiceResourceOwnerTypeFromJSON(json['resourceOwnerType']),
        'forceMfaLocalOnly': json['forceMfaLocalOnly'] == null ? undefined : json['forceMfaLocalOnly'],
    };
}

export function SettingsServiceLoginSettingsToJSON(json: any): SettingsServiceLoginSettings {
    return SettingsServiceLoginSettingsToJSONTyped(json, false);
}

export function SettingsServiceLoginSettingsToJSONTyped(value?: SettingsServiceLoginSettings | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'allowUsernamePassword': value['allowUsernamePassword'],
        'allowLocalAuthentication': value['allowLocalAuthentication'],
        'allowRegister': value['allowRegister'],
        'allowExternalIdp': value['allowExternalIdp'],
        'forceMfa': value['forceMfa'],
        'passkeysType': SettingsServicePasskeysTypeToJSON(value['passkeysType']),
        'hidePasswordReset': value['hidePasswordReset'],
        'ignoreUnknownUsernames': value['ignoreUnknownUsernames'],
        'defaultRedirectUri': value['defaultRedirectUri'],
        'passwordCheckLifetime': value['passwordCheckLifetime'],
        'externalLoginCheckLifetime': value['externalLoginCheckLifetime'],
        'mfaInitSkipLifetime': value['mfaInitSkipLifetime'],
        'secondFactorCheckLifetime': value['secondFactorCheckLifetime'],
        'multiFactorCheckLifetime': value['multiFactorCheckLifetime'],
        'secondFactors': value['secondFactors'] == null ? undefined : ((value['secondFactors'] as Array<any>).map(SettingsServiceSecondFactorTypeToJSON)),
        'multiFactors': value['multiFactors'] == null ? undefined : ((value['multiFactors'] as Array<any>).map(SettingsServiceMultiFactorTypeToJSON)),
        'allowDomainDiscovery': value['allowDomainDiscovery'],
        'disableLoginWithEmail': value['disableLoginWithEmail'],
        'disableLoginWithPhone': value['disableLoginWithPhone'],
        'resourceOwnerType': SettingsServiceResourceOwnerTypeToJSON(value['resourceOwnerType']),
        'forceMfaLocalOnly': value['forceMfaLocalOnly'],
    };
}

