// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { FeatureServiceDetails } from "./feature-service-details.js";
import { FeatureServiceFeatureFlag } from "./feature-service-feature-flag.js";
import { FeatureServiceImprovedPerformanceFeatureFlag } from "./feature-service-improved-performance-feature-flag.js";
import { FeatureServiceLoginV2FeatureFlag } from "./feature-service-login-v2-feature-flag.js";
import { Expose, Type } from "class-transformer";

export class FeatureServiceGetSystemFeaturesResponse {
  /** @example null */
  @Expose({ name: "details" })
  @Type(() => FeatureServiceDetails)
  details?: FeatureServiceDetails;
  /** @example null */
  @Expose({ name: "loginDefaultOrg" })
  @Type(() => FeatureServiceFeatureFlag)
  loginDefaultOrg?: FeatureServiceFeatureFlag;
  /** @example null */
  @Expose({ name: "userSchema" })
  @Type(() => FeatureServiceFeatureFlag)
  userSchema?: FeatureServiceFeatureFlag;
  /** @example null */
  @Expose({ name: "oidcTokenExchange" })
  @Type(() => FeatureServiceFeatureFlag)
  oidcTokenExchange?: FeatureServiceFeatureFlag;
  /** @example null */
  @Expose({ name: "improvedPerformance" })
  @Type(() => FeatureServiceImprovedPerformanceFeatureFlag)
  improvedPerformance?: FeatureServiceImprovedPerformanceFeatureFlag;
  /** @example null */
  @Expose({ name: "oidcSingleV1SessionTermination" })
  @Type(() => FeatureServiceFeatureFlag)
  oidcSingleV1SessionTermination?: FeatureServiceFeatureFlag;
  /** @example null */
  @Expose({ name: "enableBackChannelLogout" })
  @Type(() => FeatureServiceFeatureFlag)
  enableBackChannelLogout?: FeatureServiceFeatureFlag;
  /** @example null */
  @Expose({ name: "loginV2" })
  @Type(() => FeatureServiceLoginV2FeatureFlag)
  loginV2?: FeatureServiceLoginV2FeatureFlag;
  /** @example null */
  @Expose({ name: "permissionCheckV2" })
  @Type(() => FeatureServiceFeatureFlag)
  permissionCheckV2?: FeatureServiceFeatureFlag;

  constructor(data?: Partial<FeatureServiceGetSystemFeaturesResponse>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof FeatureServiceGetSystemFeaturesResponse)) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.details,
        (other as FeatureServiceGetSystemFeaturesResponse).details,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.loginDefaultOrg,
        (other as FeatureServiceGetSystemFeaturesResponse).loginDefaultOrg,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.userSchema,
        (other as FeatureServiceGetSystemFeaturesResponse).userSchema,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.oidcTokenExchange,
        (other as FeatureServiceGetSystemFeaturesResponse).oidcTokenExchange,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.improvedPerformance,
        (other as FeatureServiceGetSystemFeaturesResponse).improvedPerformance,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.oidcSingleV1SessionTermination,
        (other as FeatureServiceGetSystemFeaturesResponse)
          .oidcSingleV1SessionTermination,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.enableBackChannelLogout,
        (other as FeatureServiceGetSystemFeaturesResponse)
          .enableBackChannelLogout,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.loginV2,
        (other as FeatureServiceGetSystemFeaturesResponse).loginV2,
      )
    ) {
      return false;
    }
    if (
      !FeatureServiceGetSystemFeaturesResponse.__deepEquals(
        this.permissionCheckV2,
        (other as FeatureServiceGetSystemFeaturesResponse).permissionCheckV2,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        FeatureServiceGetSystemFeaturesResponse.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          FeatureServiceGetSystemFeaturesResponse.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !FeatureServiceGetSystemFeaturesResponse.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          FeatureServiceGetSystemFeaturesResponse.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
