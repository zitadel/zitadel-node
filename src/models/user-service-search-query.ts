// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { UserServiceAndQuery } from "./user-service-and-query.js";
import { UserServiceDisplayNameQuery } from "./user-service-display-name-query.js";
import { UserServiceEmailQuery } from "./user-service-email-query.js";
import { UserServiceFirstNameQuery } from "./user-service-first-name-query.js";
import { UserServiceInUserEmailsQuery } from "./user-service-in-user-emails-query.js";
import { UserServiceInUserIDQuery } from "./user-service-in-user-id-query.js";
import { UserServiceLastNameQuery } from "./user-service-last-name-query.js";
import { UserServiceLoginNameQuery } from "./user-service-login-name-query.js";
import { UserServiceMetadataKeyFilter } from "./user-service-metadata-key-filter.js";
import { UserServiceMetadataValueFilter } from "./user-service-metadata-value-filter.js";
import { UserServiceNickNameQuery } from "./user-service-nick-name-query.js";
import { UserServiceNotQuery } from "./user-service-not-query.js";
import { UserServiceOrQuery } from "./user-service-or-query.js";
import { UserServiceOrganizationIdQuery } from "./user-service-organization-id-query.js";
import { UserServicePhoneQuery } from "./user-service-phone-query.js";
import { UserServiceStateQuery } from "./user-service-state-query.js";
import { UserServiceTypeQuery } from "./user-service-type-query.js";
import { UserServiceUserNameQuery } from "./user-service-user-name-query.js";
import { Expose, Type } from "class-transformer";
import { Email } from "../brand.js";

export class UserServiceSearchQuery {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"weightKg":"12.345"` (a JSON string) instead of the spec-required
   * `"weightKg":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "andQuery" })
  @Type(() => UserServiceAndQuery)
  andQuery?: UserServiceAndQuery;
  @Expose({ name: "displayNameQuery" })
  @Type(() => UserServiceDisplayNameQuery)
  displayNameQuery?: UserServiceDisplayNameQuery;
  @Expose({ name: "emailQuery" })
  @Type(() => UserServiceEmailQuery)
  emailQuery?: UserServiceEmailQuery;
  @Expose({ name: "firstNameQuery" })
  @Type(() => UserServiceFirstNameQuery)
  firstNameQuery?: UserServiceFirstNameQuery;
  @Expose({ name: "inUserEmailsQuery" })
  @Type(() => UserServiceInUserEmailsQuery)
  inUserEmailsQuery?: UserServiceInUserEmailsQuery;
  @Expose({ name: "inUserIdsQuery" })
  @Type(() => UserServiceInUserIDQuery)
  inUserIdsQuery?: UserServiceInUserIDQuery;
  @Expose({ name: "lastNameQuery" })
  @Type(() => UserServiceLastNameQuery)
  lastNameQuery?: UserServiceLastNameQuery;
  @Expose({ name: "loginNameQuery" })
  @Type(() => UserServiceLoginNameQuery)
  loginNameQuery?: UserServiceLoginNameQuery;
  @Expose({ name: "metadataKeyFilter" })
  @Type(() => UserServiceMetadataKeyFilter)
  metadataKeyFilter?: UserServiceMetadataKeyFilter;
  @Expose({ name: "metadataValueFilter" })
  @Type(() => UserServiceMetadataValueFilter)
  metadataValueFilter?: UserServiceMetadataValueFilter;
  @Expose({ name: "nickNameQuery" })
  @Type(() => UserServiceNickNameQuery)
  nickNameQuery?: UserServiceNickNameQuery;
  @Expose({ name: "notQuery" })
  @Type(() => UserServiceNotQuery)
  notQuery?: UserServiceNotQuery;
  @Expose({ name: "orQuery" })
  @Type(() => UserServiceOrQuery)
  orQuery?: UserServiceOrQuery;
  @Expose({ name: "organizationIdQuery" })
  @Type(() => UserServiceOrganizationIdQuery)
  organizationIdQuery?: UserServiceOrganizationIdQuery;
  @Expose({ name: "phoneQuery" })
  @Type(() => UserServicePhoneQuery)
  phoneQuery?: UserServicePhoneQuery;
  @Expose({ name: "stateQuery" })
  @Type(() => UserServiceStateQuery)
  stateQuery?: UserServiceStateQuery;
  @Expose({ name: "typeQuery" })
  @Type(() => UserServiceTypeQuery)
  typeQuery?: UserServiceTypeQuery;
  @Expose({ name: "userNameQuery" })
  @Type(() => UserServiceUserNameQuery)
  userNameQuery?: UserServiceUserNameQuery;

  constructor(data?: Partial<UserServiceSearchQuery>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof UserServiceSearchQuery)) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.andQuery,
        (other as UserServiceSearchQuery).andQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.displayNameQuery,
        (other as UserServiceSearchQuery).displayNameQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.emailQuery,
        (other as UserServiceSearchQuery).emailQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.firstNameQuery,
        (other as UserServiceSearchQuery).firstNameQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.inUserEmailsQuery,
        (other as UserServiceSearchQuery).inUserEmailsQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.inUserIdsQuery,
        (other as UserServiceSearchQuery).inUserIdsQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.lastNameQuery,
        (other as UserServiceSearchQuery).lastNameQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.loginNameQuery,
        (other as UserServiceSearchQuery).loginNameQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.metadataKeyFilter,
        (other as UserServiceSearchQuery).metadataKeyFilter,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.metadataValueFilter,
        (other as UserServiceSearchQuery).metadataValueFilter,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.nickNameQuery,
        (other as UserServiceSearchQuery).nickNameQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.notQuery,
        (other as UserServiceSearchQuery).notQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.orQuery,
        (other as UserServiceSearchQuery).orQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.organizationIdQuery,
        (other as UserServiceSearchQuery).organizationIdQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.phoneQuery,
        (other as UserServiceSearchQuery).phoneQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.stateQuery,
        (other as UserServiceSearchQuery).stateQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.typeQuery,
        (other as UserServiceSearchQuery).typeQuery,
      )
    ) {
      return false;
    }
    if (
      !UserServiceSearchQuery.__deepEquals(
        this.userNameQuery,
        (other as UserServiceSearchQuery).userNameQuery,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => UserServiceSearchQuery.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => UserServiceSearchQuery.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !UserServiceSearchQuery.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          UserServiceSearchQuery.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
