// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose, Transform } from "class-transformer";

/**
 * Contains an arbitrary serialized message along with a @type that describes the type of the serialized message, with an additional debug field for ConnectRPC error details.
 */
export class BetaInstanceServiceAny {
  /**
   * A URL that acts as a globally unique identifier for the type of the serialized message. For example: `type.googleapis.com/google.rpc.ErrorInfo`. This is used to determine the schema of the data in the `value` field and is the discriminator for the `debug` field.
   * @example null
   */
  @Expose({ name: "type" })
  type?: string;
  /**
   * The Protobuf message, serialized as bytes and base64-encoded. The specific message type is identified by the `type` field.
   * @example null
   */
  @Expose({ name: "value" })
  value?: Buffer;
  /**
   * Deserialized error detail payload. The 'type' field indicates the schema. This field is for easier debugging and should not be relied upon for application logic.
   * @example null
   */
  @Expose({ name: "debug" })
  debug?: unknown;

  [key: string]: unknown;

  constructor(data?: Partial<BetaInstanceServiceAny>) {
    Object.assign(this, data);
    if (this.type != null && typeof this.type !== "string") {
      throw new TypeError(`type must be a string, got ${typeof this.type}`);
    }
  }

  /**
   * Value-equality across all declared fields (including the
   * additionalProperties index keys). Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaInstanceServiceAny)) {
      return false;
    }
    if (
      !BetaInstanceServiceAny.__deepEquals(
        this.type,
        (other as BetaInstanceServiceAny).type,
      )
    ) {
      return false;
    }
    if (
      !BetaInstanceServiceAny.__deepEquals(
        this.value,
        (other as BetaInstanceServiceAny).value,
      )
    ) {
      return false;
    }
    if (
      !BetaInstanceServiceAny.__deepEquals(
        this.debug,
        (other as BetaInstanceServiceAny).debug,
      )
    ) {
      return false;
    }
    {
      const declared = new Set<string>(["type", "value", "debug"]);
      const selfRec = this as unknown as Record<string, unknown>;
      const otherRec = other as unknown as Record<string, unknown>;
      const selfKeys = Object.keys(selfRec).filter((k) => !declared.has(k));
      const otherKeys = Object.keys(otherRec).filter((k) => !declared.has(k));
      if (selfKeys.length !== otherKeys.length) {
        return false;
      }
      for (const k of selfKeys) {
        if (!Object.prototype.hasOwnProperty.call(otherRec, k)) {
          return false;
        }
        if (!BetaInstanceServiceAny.__deepEquals(selfRec[k], otherRec[k])) {
          return false;
        }
      }
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => BetaInstanceServiceAny.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => BetaInstanceServiceAny.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !BetaInstanceServiceAny.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaInstanceServiceAny.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
