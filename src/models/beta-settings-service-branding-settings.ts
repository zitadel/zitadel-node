// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { BetaSettingsServiceResourceOwnerType } from "./beta-settings-service-resource-owner-type.js";
import { BetaSettingsServiceTheme } from "./beta-settings-service-theme.js";
import { BetaSettingsServiceThemeMode } from "./beta-settings-service-theme-mode.js";
import { Expose, Type } from "class-transformer";

export class BetaSettingsServiceBrandingSettings {
  /** @example null */
  @Expose({ name: "lightTheme" })
  @Type(() => BetaSettingsServiceTheme)
  lightTheme?: BetaSettingsServiceTheme;
  /** @example null */
  @Expose({ name: "darkTheme" })
  @Type(() => BetaSettingsServiceTheme)
  darkTheme?: BetaSettingsServiceTheme;
  /** @example null */
  @Expose({ name: "fontUrl" })
  fontUrl?: string;
  /**
   * hides the org suffix on the login form if the scope \\\"urn:zitadel:iam:org:domain:primary:{domainname}\\\" is set
   * @example null
   */
  @Expose({ name: "hideLoginNameSuffix" })
  hideLoginNameSuffix?: boolean;
  /** @example null */
  @Expose({ name: "disableWatermark" })
  disableWatermark?: boolean;
  /** @example null */
  @Expose({ name: "resourceOwnerType" })
  resourceOwnerType?: BetaSettingsServiceResourceOwnerType;
  /** @example null */
  @Expose({ name: "themeMode" })
  themeMode?: BetaSettingsServiceThemeMode;

  constructor(data?: Partial<BetaSettingsServiceBrandingSettings>) {
    Object.assign(this, data);
    if (this.fontUrl != null && typeof this.fontUrl !== "string") {
      throw new TypeError(
        `fontUrl must be a string, got ${typeof this.fontUrl}`,
      );
    }
    if (
      this.hideLoginNameSuffix != null &&
      typeof this.hideLoginNameSuffix !== "boolean"
    ) {
      throw new TypeError(
        `hideLoginNameSuffix must be a boolean, got ${typeof this.hideLoginNameSuffix}`,
      );
    }
    if (
      this.disableWatermark != null &&
      typeof this.disableWatermark !== "boolean"
    ) {
      throw new TypeError(
        `disableWatermark must be a boolean, got ${typeof this.disableWatermark}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof BetaSettingsServiceBrandingSettings)) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.lightTheme,
        (other as BetaSettingsServiceBrandingSettings).lightTheme,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.darkTheme,
        (other as BetaSettingsServiceBrandingSettings).darkTheme,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.fontUrl,
        (other as BetaSettingsServiceBrandingSettings).fontUrl,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.hideLoginNameSuffix,
        (other as BetaSettingsServiceBrandingSettings).hideLoginNameSuffix,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.disableWatermark,
        (other as BetaSettingsServiceBrandingSettings).disableWatermark,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.resourceOwnerType,
        (other as BetaSettingsServiceBrandingSettings).resourceOwnerType,
      )
    ) {
      return false;
    }
    if (
      !BetaSettingsServiceBrandingSettings.__deepEquals(
        this.themeMode,
        (other as BetaSettingsServiceBrandingSettings).themeMode,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) =>
        BetaSettingsServiceBrandingSettings.__deepEquals(v, b[i]),
      );
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) =>
          BetaSettingsServiceBrandingSettings.__deepEquals(v, w),
        ),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (
          !b.has(k) ||
          !BetaSettingsServiceBrandingSettings.__deepEquals(v, b.get(k))
        ) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          BetaSettingsServiceBrandingSettings.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
