// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import type { Authenticator } from "./auth/authenticator.js";
import { isHttpAwareAuthenticator } from "./auth/http-aware-authenticator.js";
import { BearerAuthenticator } from "./auth/bearer-authenticator.js";
import type { ApiClient } from "./api-client.js";
import { Configuration } from "./configuration.js";
import { DefaultApiClient } from "./default-api-client.js";
import { TransportOptions } from "./transport-options.js";
import { ActionServiceApi } from "./api/action-service-api.js";
import { ApplicationServiceApi } from "./api/application-service-api.js";
import { AuthorizationServiceApi } from "./api/authorization-service-api.js";
import { BetaActionServiceApi } from "./api/beta-action-service-api.js";
import { BetaAppServiceApi } from "./api/beta-app-service-api.js";
import { BetaAuthorizationServiceApi } from "./api/beta-authorization-service-api.js";
import { BetaFeatureServiceApi } from "./api/beta-feature-service-api.js";
import { BetaInstanceServiceApi } from "./api/beta-instance-service-api.js";
import { BetaInternalPermissionServiceApi } from "./api/beta-internal-permission-service-api.js";
import { BetaOIDCServiceApi } from "./api/beta-oidc-service-api.js";
import { BetaOrganizationServiceApi } from "./api/beta-organization-service-api.js";
import { BetaProjectServiceApi } from "./api/beta-project-service-api.js";
import { BetaSessionServiceApi } from "./api/beta-session-service-api.js";
import { BetaSettingsServiceApi } from "./api/beta-settings-service-api.js";
import { BetaTelemetryServiceApi } from "./api/beta-telemetry-service-api.js";
import { BetaUserServiceApi } from "./api/beta-user-service-api.js";
import { BetaWebKeyServiceApi } from "./api/beta-web-key-service-api.js";
import { FeatureServiceApi } from "./api/feature-service-api.js";
import { IdentityProviderServiceApi } from "./api/identity-provider-service-api.js";
import { InstanceServiceApi } from "./api/instance-service-api.js";
import { InternalPermissionServiceApi } from "./api/internal-permission-service-api.js";
import { OIDCServiceApi } from "./api/oidc-service-api.js";
import { OrganizationServiceApi } from "./api/organization-service-api.js";
import { ProjectServiceApi } from "./api/project-service-api.js";
import { SAMLServiceApi } from "./api/saml-service-api.js";
import { SessionServiceApi } from "./api/session-service-api.js";
import { SettingsServiceApi } from "./api/settings-service-api.js";
import { UserServiceApi } from "./api/user-service-api.js";
import { WebKeyServiceApi } from "./api/web-key-service-api.js";

/**
 * Unified entry point for all API services.
 *
 * Takes an {@link Authenticator} and optionally {@link TransportOptions},
 * then exposes each API group as a typed property. If the authenticator
 * implements `HttpAwareAuthenticator`, the shared {@link ApiClient}
 * is injected so that authentication HTTP calls (token exchange, discovery)
 * use the same transport configuration as regular API calls.
 *
 * Usage:
 *
 * ```typescript
 * // Default transport
 * const client = new Client(authenticator);
 *
 * // Custom transport (proxy, timeouts, etc.)
 * const transport = TransportOptions.builder()
 *   .proxy('http://proxy:3128')
 *   .timeout(5000)
 *   .build();
 * const client = new Client(authenticator, transport);
 * ```
 */
export class Client {
  /** API operations for the ActionServiceApi group. */
  public readonly actionService: ActionServiceApi;
  /** API operations for the ApplicationServiceApi group. */
  public readonly applicationService: ApplicationServiceApi;
  /** API operations for the AuthorizationServiceApi group. */
  public readonly authorizationService: AuthorizationServiceApi;
  /** API operations for the BetaActionServiceApi group. */
  public readonly betaActionService: BetaActionServiceApi;
  /** API operations for the BetaAppServiceApi group. */
  public readonly betaAppService: BetaAppServiceApi;
  /** API operations for the BetaAuthorizationServiceApi group. */
  public readonly betaAuthorizationService: BetaAuthorizationServiceApi;
  /** API operations for the BetaFeatureServiceApi group. */
  public readonly betaFeatureService: BetaFeatureServiceApi;
  /** API operations for the BetaInstanceServiceApi group. */
  public readonly betaInstanceService: BetaInstanceServiceApi;
  /** API operations for the BetaInternalPermissionServiceApi group. */
  public readonly betaInternalPermissionService: BetaInternalPermissionServiceApi;
  /** API operations for the BetaOIDCServiceApi group. */
  public readonly betaOIDCService: BetaOIDCServiceApi;
  /** API operations for the BetaOrganizationServiceApi group. */
  public readonly betaOrganizationService: BetaOrganizationServiceApi;
  /** API operations for the BetaProjectServiceApi group. */
  public readonly betaProjectService: BetaProjectServiceApi;
  /** API operations for the BetaSessionServiceApi group. */
  public readonly betaSessionService: BetaSessionServiceApi;
  /** API operations for the BetaSettingsServiceApi group. */
  public readonly betaSettingsService: BetaSettingsServiceApi;
  /** API operations for the BetaTelemetryServiceApi group. */
  public readonly betaTelemetryService: BetaTelemetryServiceApi;
  /** API operations for the BetaUserServiceApi group. */
  public readonly betaUserService: BetaUserServiceApi;
  /** API operations for the BetaWebKeyServiceApi group. */
  public readonly betaWebKeyService: BetaWebKeyServiceApi;
  /** API operations for the FeatureServiceApi group. */
  public readonly featureService: FeatureServiceApi;
  /** API operations for the IdentityProviderServiceApi group. */
  public readonly identityProviderService: IdentityProviderServiceApi;
  /** API operations for the InstanceServiceApi group. */
  public readonly instanceService: InstanceServiceApi;
  /** API operations for the InternalPermissionServiceApi group. */
  public readonly internalPermissionService: InternalPermissionServiceApi;
  /** API operations for the OIDCServiceApi group. */
  public readonly oIDCService: OIDCServiceApi;
  /** API operations for the OrganizationServiceApi group. */
  public readonly organizationService: OrganizationServiceApi;
  /** API operations for the ProjectServiceApi group. */
  public readonly projectService: ProjectServiceApi;
  /** API operations for the SAMLServiceApi group. */
  public readonly sAMLService: SAMLServiceApi;
  /** API operations for the SessionServiceApi group. */
  public readonly sessionService: SessionServiceApi;
  /** API operations for the SettingsServiceApi group. */
  public readonly settingsService: SettingsServiceApi;
  /** API operations for the UserServiceApi group. */
  public readonly userService: UserServiceApi;
  /** API operations for the WebKeyServiceApi group. */
  public readonly webKeyService: WebKeyServiceApi;

  /**
   * Creates a new client with the given authenticator and default transport settings.
   *
   * @param authenticator provides host URL and auth credentials
   */
  constructor(authenticator: Authenticator);

  /**
   * Creates a new client with the given authenticator and transport options.
   *
   * If the authenticator implements `HttpAwareAuthenticator`, the
   * shared {@link ApiClient} is injected so that token exchange and
   * discovery requests use the same proxy, TLS, and timeout settings.
   *
   * @param authenticator provides host URL and auth credentials
   * @param transportOptions HTTP transport configuration (proxy, TLS, timeouts, etc.)
   */
  constructor(authenticator: Authenticator, transportOptions: TransportOptions);

  constructor(
    authenticator: Authenticator,
    transportOptions?: TransportOptions,
  ) {
    const transport = transportOptions ?? TransportOptions.builder().build();
    const apiClient: ApiClient = new DefaultApiClient(transport);

    if (isHttpAwareAuthenticator(authenticator)) {
      authenticator.setApiClient(apiClient);
    }

    const config = Configuration.builder()
      .baseUrl(authenticator.getHost())
      .build();
    this.actionService = new ActionServiceApi(apiClient, config, authenticator);
    this.applicationService = new ApplicationServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.authorizationService = new AuthorizationServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaActionService = new BetaActionServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaAppService = new BetaAppServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaAuthorizationService = new BetaAuthorizationServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaFeatureService = new BetaFeatureServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaInstanceService = new BetaInstanceServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaInternalPermissionService = new BetaInternalPermissionServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaOIDCService = new BetaOIDCServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaOrganizationService = new BetaOrganizationServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaProjectService = new BetaProjectServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaSessionService = new BetaSessionServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaSettingsService = new BetaSettingsServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaTelemetryService = new BetaTelemetryServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaUserService = new BetaUserServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.betaWebKeyService = new BetaWebKeyServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.featureService = new FeatureServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.identityProviderService = new IdentityProviderServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.instanceService = new InstanceServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.internalPermissionService = new InternalPermissionServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.oIDCService = new OIDCServiceApi(apiClient, config, authenticator);
    this.organizationService = new OrganizationServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.projectService = new ProjectServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.sAMLService = new SAMLServiceApi(apiClient, config, authenticator);
    this.sessionService = new SessionServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.settingsService = new SettingsServiceApi(
      apiClient,
      config,
      authenticator,
    );
    this.userService = new UserServiceApi(apiClient, config, authenticator);
    this.webKeyService = new WebKeyServiceApi(apiClient, config, authenticator);
  }

  /**
   * Creates a client authenticated with a static Bearer token and default transport.
   *
   * @param host API base URL
   * @param accessToken Bearer token
   * @param transportOptions optional HTTP transport configuration (proxy, TLS, timeouts, etc.)
   * @returns configured client instance
   */
  static withToken(
    host: string,
    accessToken: string,
    transportOptions?: TransportOptions,
  ): Client {
    return transportOptions
      ? new Client(new BearerAuthenticator(host, accessToken), transportOptions)
      : new Client(new BearerAuthenticator(host, accessToken));
  }
}
