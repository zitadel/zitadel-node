// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import type { ApiClient } from "../api-client.js";
import { ApiError } from "../api-error.js";
import type { ApiResult } from "../api-result.js";
import type { Authenticator } from "../auth/authenticator.js";
import { BaseApi } from "./base-api.js";
import { Configuration } from "../configuration.js";
import { ObjectSerializer } from "../object-serializer.js";
import { ValueSerializer } from "../value-serializer.js";
import type { DeepInput } from "../deep-input.js";
import {
  BetaTelemetryServiceConnectError,
  BetaTelemetryServiceReportBaseInformationRequest,
  BetaTelemetryServiceReportBaseInformationResponse,
  BetaTelemetryServiceReportResourceCountsRequest,
  BetaTelemetryServiceReportResourceCountsResponse,
} from "../models/index.js";

/**
 * BetaTelemetryServiceApi provides methods for the BetaTelemetryService API group.
 */
export class BetaTelemetryServiceApi extends BaseApi {
  constructor(
    apiClient?: ApiClient,
    config?: Configuration,
    authenticator?: Authenticator | null,
  ) {
    super(apiClient, config, authenticator);
  }

  /**
   * ReportBaseInformation is used to report the base information of the ZITADEL system,  including the version, instances, their creation date and domains.  The response contains a report ID to link it to the resource counts or other reports.  The report ID is only valid for the same system ID.
   * ReportBaseInformation is used to report the base information of the ZITADEL system,  including the version, instances, their creation date and domains.  The response contains a report ID to link it to the resource counts or other reports.  The report ID is only valid for the same system ID.
   * @param betaTelemetryServiceReportBaseInformationRequest  (required)
   * @return BetaTelemetryServiceReportBaseInformationResponse
   * @throws {ApiError} if fails to make API call
   */
  async reportBaseInformation(
    betaTelemetryServiceReportBaseInformationRequest: DeepInput<BetaTelemetryServiceReportBaseInformationRequest>,
  ): Promise<BetaTelemetryServiceReportBaseInformationResponse> {
    const reportBaseInformationResult =
      await this.reportBaseInformationWithHttpInfo(
        betaTelemetryServiceReportBaseInformationRequest,
      );
    /* convenience-empty-body-handling: a body-returning operation that
     * receives no decodable body (204 / empty / null) must surface a
     * typed ApiError, never a silently-cast `undefined`. */
    if (reportBaseInformationResult.data == null) {
      throw new ApiError(
        reportBaseInformationResult.statusCode,
        "Expected a response body for reportBaseInformation but received none",
        reportBaseInformationResult.headers,
        reportBaseInformationResult.rawBody,
        null,
      );
    }
    return reportBaseInformationResult.data as BetaTelemetryServiceReportBaseInformationResponse;
  }

  /**
   * ReportBaseInformation is used to report the base information of the ZITADEL system,  including the version, instances, their creation date and domains.  The response contains a report ID to link it to the resource counts or other reports.  The report ID is only valid for the same system ID. (with HTTP info)
   * @throws {ApiError} if fails to make API call
   */
  async reportBaseInformationWithHttpInfo(
    betaTelemetryServiceReportBaseInformationRequest: DeepInput<BetaTelemetryServiceReportBaseInformationRequest>,
  ): Promise<ApiResult<BetaTelemetryServiceReportBaseInformationResponse>> {
    if (betaTelemetryServiceReportBaseInformationRequest == null) {
      throw new Error(
        'Missing required parameter "betaTelemetryServiceReportBaseInformationRequest" when calling reportBaseInformation',
      );
    }
    const path = `/zitadel.analytics.v2beta.TelemetryService/ReportBaseInformation`;
    const queryParams: Record<string, unknown> = {};
    const headerParams: Record<string, string> = {};
    return await this.invokeApiForResult(
      "POST",
      path,
      queryParams,
      headerParams,
      betaTelemetryServiceReportBaseInformationRequest,
      ["application/json"],
      "application/json",
      (json: unknown) =>
        ObjectSerializer.deserialize(
          json,
          BetaTelemetryServiceReportBaseInformationResponse,
        )!,
      null,
    );
  }

  /**
   * ReportResourceCounts is used to report the resource counts such as amount of organizations  or users per organization and much more.  Since the resource counts can be reported in multiple batches,  the response contains a report ID to continue reporting.  The report ID is only valid for the same system ID.
   * ReportResourceCounts is used to report the resource counts such as amount of organizations  or users per organization and much more.  Since the resource counts can be reported in multiple batches,  the response contains a report ID to continue reporting.  The report ID is only valid for the same system ID.
   * @param betaTelemetryServiceReportResourceCountsRequest  (required)
   * @return BetaTelemetryServiceReportResourceCountsResponse
   * @throws {ApiError} if fails to make API call
   */
  async reportResourceCounts(
    betaTelemetryServiceReportResourceCountsRequest: DeepInput<BetaTelemetryServiceReportResourceCountsRequest>,
  ): Promise<BetaTelemetryServiceReportResourceCountsResponse> {
    const reportResourceCountsResult =
      await this.reportResourceCountsWithHttpInfo(
        betaTelemetryServiceReportResourceCountsRequest,
      );
    /* convenience-empty-body-handling: a body-returning operation that
     * receives no decodable body (204 / empty / null) must surface a
     * typed ApiError, never a silently-cast `undefined`. */
    if (reportResourceCountsResult.data == null) {
      throw new ApiError(
        reportResourceCountsResult.statusCode,
        "Expected a response body for reportResourceCounts but received none",
        reportResourceCountsResult.headers,
        reportResourceCountsResult.rawBody,
        null,
      );
    }
    return reportResourceCountsResult.data as BetaTelemetryServiceReportResourceCountsResponse;
  }

  /**
   * ReportResourceCounts is used to report the resource counts such as amount of organizations  or users per organization and much more.  Since the resource counts can be reported in multiple batches,  the response contains a report ID to continue reporting.  The report ID is only valid for the same system ID. (with HTTP info)
   * @throws {ApiError} if fails to make API call
   */
  async reportResourceCountsWithHttpInfo(
    betaTelemetryServiceReportResourceCountsRequest: DeepInput<BetaTelemetryServiceReportResourceCountsRequest>,
  ): Promise<ApiResult<BetaTelemetryServiceReportResourceCountsResponse>> {
    if (betaTelemetryServiceReportResourceCountsRequest == null) {
      throw new Error(
        'Missing required parameter "betaTelemetryServiceReportResourceCountsRequest" when calling reportResourceCounts',
      );
    }
    const path = `/zitadel.analytics.v2beta.TelemetryService/ReportResourceCounts`;
    const queryParams: Record<string, unknown> = {};
    const headerParams: Record<string, string> = {};
    return await this.invokeApiForResult(
      "POST",
      path,
      queryParams,
      headerParams,
      betaTelemetryServiceReportResourceCountsRequest,
      ["application/json"],
      "application/json",
      (json: unknown) =>
        ObjectSerializer.deserialize(
          json,
          BetaTelemetryServiceReportResourceCountsResponse,
        )!,
      null,
    );
  }
}
