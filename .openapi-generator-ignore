# OpenAPI Generator Ignore
# Generated by openapi-generator https://github.com/openapitools/openapi-generator

# Use this file to prevent files from being overwritten by the generator.
# The patterns follow closely to .gitignore or .dockerignore.

# As an example, the C# client generator defines ApiClient.cs.
# You can make changes and tell OpenAPI Generator to ignore just this file by uncommenting the following line:
#ApiClient.cs

# You can match any string of characters against a directory, file or extension with a single asterisk (*):
#foo/*/qux
# The above matches foo/bar/qux and foo/baz/qux, but not foo/bar/baz/qux

# You can recursively match patterns against a directory, file or extension with a double asterisk (**):
#foo/**/qux
# This matches foo/bar/qux, foo/baz/qux, and foo/bar/baz/qux

# You can also negate patterns with an exclamation (!).
# For example, you can ignore all files in a docs folder with the file extension .md:
#docs/*.md
# Then explicitly reverse the ignore rule for a single file:
#!docs/README.md

# Preserve hand-maintained package manifest across regenerations
package.json

# The Makefile carries the bespoke `generate`/`prune` regeneration targets
# (the generator is invoked *through* it). The generator emits its own minimal
# Makefile; keep-listing ours prevents a regen from erasing the very targets
# that drive regeneration.
Makefile

# Hand-maintained public Zitadel facade and the barrel that re-exports it as
# the package default export. The generator emits a plain barrel for
# src/index.ts and a separate generated `Client`; these bespoke files add the
# `Zitadel` facade the integration specs require and must not be overwritten.
src/zitadel.ts
src/index.ts

# Bespoke compatibility shims (not emitted by the generator). The integration
# specs import `ApiException`/`ZitadelException`; these files re-export the
# generated typed `ApiError` base under those names. Tracked but absent from
# the generator's FILES manifest, so without this entry `prune` would delete
# them.
src/api-exception.ts
src/zitadel-exception.ts

# Bespoke authentication layer. The generator emits only the auth scaffolding
# (the `Authenticator` interface, `BaseAuthenticator`, `BearerAuthenticator`,
# `HttpAwareAuthenticator`); every concrete Zitadel authenticator (OAuth,
# client-credentials, private-key/JWT, PAT, no-auth, OpenID discovery, and
# their builders) plus the auth barrel are hand-maintained and absent from the
# generator's FILES manifest. Ignore the whole directory so `prune` spares
# them, then re-enable overwriting for the four generated scaffolding files so
# the generator stays the source of truth for those.
src/auth/**
!src/auth/authenticator.ts
!src/auth/base-authenticator.ts
!src/auth/bearer-authenticator.ts
!src/auth/http-aware-authenticator.ts

# Bespoke integration specs and the Zitadel server fixtures they drive. All
# team-authored and absent from the generator's FILES manifest; without these
# entries `prune` would delete them.
spec/
etc/

# Bespoke test files the generator does not emit: the public-facade test, the
# bespoke ApiException shim test, the Zitadel authenticator unit tests, and the
# wiremock/squid/cert fixtures. The six spec-independent unit tests
# (value-serializer, header-selector, configuration, transport-options,
# trace-context-util, default-api-client-unit) are now generator-owned, so the
# blanket `test/` keep is gone — keep-list only the bespoke files.
test/index.test.ts
test/api-exception.test.ts
test/setup.js
test/fixtures/
test/auth/oauth-authenticator-test.ts
test/auth/client-credentials-authenticator.test.ts
test/auth/no-auth-authenticator.test.ts
test/auth/personal-access-authenticator.test.ts
test/auth/web-token-authenticator.test.ts

# Repo identity, docs, CI, and house tooling — team-owned, not generated.
# These are tracked but never appear in the generator's FILES manifest, so
# they must be keep-listed or `prune` deletes them on the next regen.
README.md
SKILLS.md
LICENSE
SECURITY.md
VERSIONING.md
Dockerfile
.github/
.idea/
.run/
.nvmrc
.dockerignore
.editorconfig
.gitignore
.npmrc
.pre-commit-config.yaml
commitlint.config.mjs
eslint.config.mjs
prettier.config.mjs
jest.config.mjs
tsconfig.json
tsconfig.jest.json
knip.config.js
release.config.mjs
lefthook.yml
qodana.yaml
typedoc.json
devbox.json
devbox.lock
package-lock.json

# Generation tooling we maintain (the generator config and this ignore file).
proc.yml
.openapi-generator-ignore

# The generator's own metadata (the FILES manifest and VERSION stamp). These
# are rewritten on every run but are absent from the FILES manifest they live
# beside, so without this entry `prune` would delete them after each regen.
.openapi-generator/

# Lint/format config carries the generator's defaults verbatim — ESLint flat
# config = `@eslint/js` recommended + `typescript-eslint` recommended (no
# relaxations); Prettier = defaults (`{}`). The only deviation is the filename:
# the package is "type": "module", so the config lives in ESM `eslint.config.mjs`
# / `prettier.config.mjs` (keep-listed above) rather than the generator's
# CommonJS `eslint.config.js` and `.prettierrc.json`. Suppress those CommonJS
# variants so a regen cannot shadow the ESM config the package actually loads.
eslint.config.js
.prettierrc.json

# Hand-maintained Prettier ignore list. It excludes build outputs and YAML/JSON
# fixtures from the format check; the generated SDK surface is formatted by the
# same default Prettier config and is no longer excluded. The generator emits a
# minimal `.prettierignore`; keep the curated one.
.prettierignore
