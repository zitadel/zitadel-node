// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { injectTraceContext } from "../src/trace-context-util.js";

describe("TraceContextUtil", () => {
  test("is a no-op without tracer", async () => {
    const headers: Record<string, string> = {};
    await injectTraceContext(headers);
    expect(Object.keys(headers).length).toBe(0);
  });

  test("empty headers do not cause exception", () => {
    const headers: Record<string, string> = {};
    expect(() => injectTraceContext(headers)).not.toThrow();
    expect(Object.keys(headers).length).toBe(0);
  });

  test("does not inject traceparent without OTel", async () => {
    const headers: Record<string, string> = {};
    await injectTraceContext(headers);
    expect(headers["traceparent"]).toBeUndefined();
  });

  test("does not inject tracestate without OTel", async () => {
    const headers: Record<string, string> = {};
    await injectTraceContext(headers);
    expect(headers["tracestate"]).toBeUndefined();
  });

  test("preserves Authorization header", async () => {
    const headers: Record<string, string> = {
      Authorization: "Bearer token123",
    };
    await injectTraceContext(headers);
    expect(headers["Authorization"]).toBe("Bearer token123");
  });

  test("preserves Content-Type header", async () => {
    const headers: Record<string, string> = {
      "Content-Type": "application/json",
    };
    await injectTraceContext(headers);
    expect(headers["Content-Type"]).toBe("application/json");
  });

  test("preserves X-Request-ID header", async () => {
    const headers: Record<string, string> = { "X-Request-ID": "req-12345" };
    await injectTraceContext(headers);
    expect(headers["X-Request-ID"]).toBe("req-12345");
  });

  test("preserves all existing headers", async () => {
    const headers: Record<string, string> = {
      Authorization: "Bearer token",
      "Content-Type": "application/json",
      "X-Request-ID": "abc-123",
    };
    await injectTraceContext(headers);
    expect(Object.keys(headers).length).toBe(3);
    expect(headers["Authorization"]).toBe("Bearer token");
    expect(headers["Content-Type"]).toBe("application/json");
    expect(headers["X-Request-ID"]).toBe("abc-123");
  });

  // .NET-specific scenario: Node has no ambient tracer like .NET Activity.Current;
  // injecting a real active span requires a fully configured OpenTelemetry SDK.
  test.skip("injects traceparent when a span is active", () => {});

  // .NET-specific scenario: setting tracestate on an active span requires a fully
  // configured OpenTelemetry SDK, which is out of scope for this unit test.
  test.skip("includes tracestate when present on the active span", () => {});

  // .NET-specific scenario: exercising an empty tracestate on an active span
  // requires a fully configured OpenTelemetry SDK, which is out of scope here.
  test.skip("omits tracestate when empty on the active span", () => {});

  // .NET-specific scenario: verifying the recorded trace-flags byte requires a
  // fully configured OpenTelemetry SDK with an active span.
  test.skip("formats trace flags correctly on the active span", () => {});
});
